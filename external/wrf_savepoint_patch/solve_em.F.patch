--- a/dyn_em/solve_em.F
+++ b/dyn_em/solve_em.F
@@ -55,0 +56,16 @@
+#ifdef WRF_SAVEPOINT
+   USE savepoint_wrapper, ONLY: sp_calc_coef_w_pre, sp_calc_coef_w_post, &
+      sp_small_step_prep_post, sp_advance_mu_t_pre, sp_advance_mu_t_post, &
+      sp_advance_uv_post, sp_advance_w_rhs_ready, sp_advance_w_raw_w, &
+      sp_t_2ave_update_pre, sp_t_2ave_update_post, &
+      sp_ww_update_pre, sp_ww_update_post, &
+      sp_muave_update_pre, sp_muave_update_post, &
+      sp_ph_tend_accumulate_pre, sp_ph_tend_accumulate_post, &
+      sp_substep_save_state_pre, sp_substep_save_state_post, &
+      sp_advance_w_tridiag_fwd_pre, sp_advance_w_tridiag_fwd_post, &
+      sp_advance_w_tridiag_back_pre, sp_advance_w_tridiag_back_post, &
+      sp_advance_w_rayleigh, sp_advance_w_ph_final, sp_calc_p_rho_post, &
+      sp_small_step_finish_post, sp_acoustic_substep_boundary, &
+      sp_acoustic_substep_complete, sp_acoustic_loop_complete, &
+      sp_dycore_step_complete, sp_coupled_step_complete, sp_rk_stage_boundary
+#endif
@@ -2675,0 +2692,3 @@
+#ifdef WRF_SAVEPOINT
+           CALL sp_calc_coef_w_pre()
+#endif
@@ -2687,0 +2707,3 @@
+#ifdef WRF_SAVEPOINT
+           CALL sp_calc_coef_w_post()
+#endif
@@ -2704,0 +2727,3 @@
+#ifdef WRF_SAVEPOINT
+         CALL sp_calc_coef_w_pre()
+#endif
@@ -2716,0 +2742,3 @@
+#ifdef WRF_SAVEPOINT
+         CALL sp_calc_coef_w_post()
+#endif
@@ -3397,0 +3426,4 @@
+#ifdef WRF_SAVEPOINT
+         CALL sp_advance_mu_t_pre(rk_step, iteration, grid%mu_2, grid%mut, grid%mudf, &
+                          grid%muts, muave, grid%ww, grid%t_2)
+#endif
@@ -3415,0 +3448,4 @@
+#ifdef WRF_SAVEPOINT
+         CALL sp_advance_mu_t_post(rk_step, iteration, grid%mu_2, grid%mut, grid%mudf, &
+                          grid%muts, muave, grid%ww, grid%t_2, ph_tend)
+#endif
@@ -4362,0 +4399,5 @@
+#ifdef WRF_SAVEPOINT
+     CALL sp_acoustic_substep_complete(rk_step, iteration, grid%mu_2, grid%mut, grid%mudf, &
+          grid%muts, muave, grid%ww, grid%t_2, ph_tend, grid%u_2, grid%v_2, &
+          grid%w_2, grid%ph_2, grid%p, t_2save)
+#endif
@@ -4363,0 +4405,5 @@
+#ifdef WRF_SAVEPOINT
+     CALL sp_acoustic_loop_complete(rk_step, grid%mu_2, grid%mut, grid%mudf, &
+          grid%muts, muave, grid%ww, grid%t_2, ph_tend, grid%u_2, grid%v_2, &
+          grid%w_2, grid%ph_2, grid%p, t_2save)
+#endif
@@ -6765,0 +6812,6 @@
+#ifdef WRF_SAVEPOINT
+  CALL sp_dycore_step_complete(grid%itimestep, grid%mu_2, grid%mut, grid%mudf, &
+       grid%muts, muave, grid%ww, grid%t_2, ph_tend, grid%u_2, grid%v_2, &
+       grid%w_2, grid%ph_2, grid%p, t_2save)
+#endif
+
@@ -8191,0 +8244,9 @@
+#ifdef WRF_SAVEPOINT
+  CALL sp_coupled_step_complete(grid%itimestep, grid%mu_2, grid%mut, grid%mudf, &
+       grid%muts, muave, grid%ww, grid%t_2, ph_tend, grid%u_2, grid%v_2, &
+       grid%w_2, grid%ph_2, grid%p, t_2save, moist(ims,kms,jms,P_QV), &
+       moist(ims,kms,jms,P_QC), moist(ims,kms,jms,P_QR), &
+       moist(ims,kms,jms,P_QI), moist(ims,kms,jms,P_QS), &
+       moist(ims,kms,jms,P_QG), moist(ims,kms,jms,P_QV), t_tend, &
+       moist_tend(ims,kms,jms,P_QV), grid%mudf)
+#endif
