"""Small placeholder used by smoke tests before model code exists."""

from __future__ import annotations


def proof_object_kind() -> str:
    """Return the only accepted completion style for project work."""
    return "evidence-backed-proof-object"
