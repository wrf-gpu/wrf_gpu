# build artifacts (regenerable via build_and_run.sh)
*.o
*.mod
kf_oracle
fort.*
# verbatim copy of the pristine WRF source (provenance recorded in FINDINGS.md);
# do not vendor the 120 KB WRF file into this repo
module_cu_kfeta.F
