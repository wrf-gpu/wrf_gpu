# v0.6.0 per-scheme WRF savepoint oracle build scratch (union across all lanes).
# Compiled Fortran oracle artifacts and intermediate column dumps are not
# committed; the extracted savepoint JSON under proofs/v060/savepoints* is.
build/
build_*/
*.o
*.mod
*.smod
*.exe
dudhia_oracle
rrtm_lw_oracle
RRTM_DATA
fort.*
_wrfstubs.*
_modstubs.*
case_*.txt
boulac_case_*.txt
# Per-scheme oracle driver binaries
morrison_oracle
kf_oracle
wsm6_oracle
kessler_oracle
ysu_oracle
acm2_oracle
tiedtke_oracle
gf_oracle
sfclayrev_oracle
pxsfclay_oracle
boulac_oracle
noahclassic_columns.in
noahclassic_savepoints.out
# Verbatim copies of pristine WRF physics source vendored at build time only
# (provenance recorded in the per-scheme v060 reports; not committed here).
module_cu_kfeta.F
module_*.F
# Land/soil parameter tables copied next to the offline driver at build time
VEGPARM.TBL
SOILPARM.TBL
GENPARM.TBL
LANDUSE.TBL
MPTABLE.TBL
