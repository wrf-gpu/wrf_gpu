{
  "status": "PASS",
  "ac5": "no regression; nothing weakened; no clamps/caps/sanitizers/epssm-fudge added",
  "production_code_changed": "NONE — git diff (tracked, non-proof) is empty; all F7I work is diagnostic scripts (scripts/f7i_*.py) + proofs/f7i/* + a separate WRF build tree under /home/enric/src/wrf_ideal_f7i (not in repo).",
  "m4_bare_core_tests": {
    "command": "PYTHONPATH=src taskset -c 0-3 python -m pytest tests/test_m4_acoustic.py tests/test_m4_dycore_step.py tests/test_m4_tier2_invariants.py -q",
    "result": "10 passed",
    "runs": 2,
    "device": "cuda:0",
    "fp64": true
  },
  "grid_p_refresh_fix_preserved": "the F7D/F7H grid%p-refresh + full-p pg_buoy_w fixes (tip 86a334a) are untouched; warm-bubble buoyancy still RISES (centroid +~170 m before detonation) and max|w|@100s stays ~4.3 (10x below the pre-F7H 44.7 runaway).",
  "no_masking": "no diffusion/epssm/damping tuning was LANDED. Diffusion and epssm sweeps were diagnostic-only (monkeypatch/dataclass-replace in throwaway scripts) and none is committed to production config."
}
