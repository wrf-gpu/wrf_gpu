# V0.6.0 CLOSE — physics-suite expansion (consolidate + integration-test + scope-doc)

> **SUPERSEDED IN PART (2026-06-04 consolidation).** This file is the original
> `worker/opus/v060-close` close-candidate snapshot. It was authored BEFORE the
> later v0.6.0 lanes (MYJ, WSM3/WSM5, YSU/ACM2-GPU-op, GF/Tiedtke-GPU) were
> merged onto the consolidation branch. The scheme/wiring tables below are
> therefore a point-in-time snapshot, NOT current truth: post-consolidation,
> Tiedtke(6) is now GPU-scan-wired, WSM3(3)/WSM5(4) are added + scan-wired, and
> MYJ(2)/Janjic(2) are added as parity-only fail-closed. The authoritative
> post-consolidation status table lives in
> `.agent/reviews/2026-06-04-opus-v060-consolidation.md` and the regenerated
> `proofs/v060/{scanwire_report,forecast_gate_readiness,consolidation_integration_matrix}.json`.

Status: **CLOSE CANDIDATE = YES**
Date: 2026-06-03
Branch: `worker/opus/v060-close`
Base trunk: `e998250` (consolidated 0.9.0 trunk — 12-scheme operational scan)
Proof object: [`proofs/v060/v060_close_proof.json`](../../proofs/v060/v060_close_proof.json)
Generator (reproducible): `proofs/v060/gen_v060_close_proof.py`

This close is a **CONSOLIDATION**, not a re-port. The schemes were already
savepoint-parity PASS and on the trunk. v0.6.0 closes by (1) verifying wiring,
(2) confirming the fail-closed namelist matrix, (3) running the integration
matrix, (4) documenting scope. No mask/tol-loosening, no JAX-vs-JAX
self-compare, no synthetic happy-path.

---

## 1. Wiring verification — wired vs accepted-but-not-GPU-scan-wired

Cross-referenced live from `coupling.physics_dispatch._FAMILY_TABLE`,
`coupling.scan_adapters.*_SCAN_ADAPTERS`, and the operational dispatch in
`runtime.operational_mode._physics_boundary_step_with_limiter_diagnostics`
(lines ~2069-2148: each of MP / surface-layer / PBL / cumulus / land slots is
dispatcher-routed at trace time — a static Python branch, no per-step lax.cond
overhead — and `_resolve_operational_suite` fail-closes any non-wired option at
every public entry).

**19 / 22 accepted options are operational-scan-wired.** Every GPU-runnable
scheme that belongs in the scan IS reachable.

| Family | Option | Scheme | gpu_runnable | Scan-wired | Operational adapter |
|---|---|---|---|---|---|
| mp_physics | 0 | passive qv | yes | yes | passive (no microphysics) |
| mp_physics | 1 | Kessler | yes | yes | `scan_adapters.kessler_adapter` |
| mp_physics | 6 | WSM6 | yes | yes | `scan_adapters.wsm6_adapter` |
| mp_physics | 8 | Thompson (default) | yes | yes | `physics_couplers.thompson_adapter` |
| mp_physics | 10 | Morrison | yes | yes | `scan_adapters.morrison_adapter` |
| mp_physics | 16 | WDM6 | yes | yes | `scan_adapters.wdm6_adapter` |
| bl_pbl_physics | 0 | off | yes | yes | no PBL mixing |
| bl_pbl_physics | 1 | YSU | yes | yes | `scan_adapters.ysu_pbl_adapter` |
| bl_pbl_physics | 5 | MYNN (default) | yes | yes | `physics_couplers.mynn_adapter` |
| bl_pbl_physics | 7 | ACM2 | yes | yes | `scan_adapters.acm2_pbl_adapter` |
| sf_sfclay_physics | 0 | off | yes | yes | surface layer off |
| sf_sfclay_physics | 1 | revised-MM5 | yes | yes | `scan_adapters.sfclay_revised_mm5_adapter` |
| sf_sfclay_physics | 5 | MYNN-SL (default) | yes | yes | `physics_couplers.surface_adapter` |
| sf_sfclay_physics | 7 | Pleim-Xiu | yes | yes | `scan_adapters.pleim_xiu_sfclay_adapter` |
| cu_physics | 0 | none (default) | yes | yes | no cumulus |
| cu_physics | 1 | Kain-Fritsch | yes | yes | `scan_adapters.kf_adapter` (+ `OperationalCarry.cumulus_carry`) |
| **cu_physics** | **3** | **Grell-Freitas** | **no** | **NO** | **FAIL-CLOSED (CPU-NumPy reference port)** |
| **cu_physics** | **6** | **Tiedtke** | **no** | **NO** | **FAIL-CLOSED (CPU-NumPy reference port)** |
| **cu_physics** | **16** | **New Tiedtke** | **no** | **NO** | **FAIL-CLOSED (CPU-NumPy reference port)** |
| sf_surface_physics | 0 | off | yes | yes | land off |
| sf_surface_physics | 2 | Noah classic | yes | yes | `noahclassic_surface_hook.noahclassic_surface_step` (explicit static/land bundle) |
| sf_surface_physics | 4 | Noah-MP | yes | yes | `noahmp_surface_hook.noahmp_surface_step` (use_noahmp=True) |

**Gaps fixed:** none required. The 3 not-wired options (Grell-Freitas + the two
Tiedtke variants) are `gpu_runnable=False` CPU-NumPy reference ports: they are
namelist-accepted and per-scheme parity-gated, but **fail-close loudly** in the
GPU operational scan (`_resolve_operational_suite` raises
`UnsupportedSchemeSelection` with a scheme-specific reason). That is the
intended v0.6.0 boundary — no scheme has a PASS report yet is *silently*
unreachable.

---

## 2. Namelist matrix (`io/namelist_check.py`, &physics scheme-number section)

`validate_supported_namelist` is registry-driven and fail-closes loudly on any
value outside the accept set. Verified: all 26 accepted &physics values pass; a
representative set of unsupported values (mp 2/4/28, bl 2, sfclay 2, cu 2/5,
sf_surface 1/3, ra 1/3/5) all raise `UnsupportedNamelistOption`.

| Key | Accept |
|---|---|
| `mp_physics` | 0, 1, 6, 8, 10, 16 |
| `bl_pbl_physics` | 0, 1, 5, 7 |
| `sf_sfclay_physics` | 0, 1, 5, 7 |
| `cu_physics` | 0, 1, 3, 6, 16 |
| `sf_surface_physics` | 0, 2, 4 |
| `ra_sw_physics` | 0, 4 |
| `ra_lw_physics` | 0, 4 |

This edit only touched the &physics scheme-number `SupportedOption` entries
(`mp/bl/sfclay/cu/sf_surface` descriptions made honest about GPU-vs-CPU status
and PBL↔sfclay pairing). The sibling v0.7.0 lane owns `&dynamics`/`&time_control`
controls in the same file via separate entries; those were left untouched.

> Note: the dispatch task text listed `mp_physics 1/2/3/4/6/10/16`. The frozen
> S0 contract (authoritative) is `0,1,6,8,10,16`. mp 2/3/4 (Lin/WSM3/WSM5) were
> never ported and correctly fail-close. Followed the frozen contract.

---

## 3. Integration matrix (`proofs/v060/multicfg_operational_smoke.py`)

Executed end-to-end through the REAL operational coupler (CPU, 4 steps, x64).
Report: [`proofs/v060/multicfg_smoke_report.json`](../../proofs/v060/multicfg_smoke_report.json).

- **14 / 14 RUN configs PASS** — finite + physical + JIT-traceable (==GPU-runnable)
  + every expected scheme ACTIVE (non-trivial tendency, no silent no-op).
- **2 / 2 FAIL-CLOSED configs OK** — Grell-Freitas (cu=3) and Tiedtke (cu=6) are
  rejected loudly by the coupler, never silently no-op.
- Valid PBL↔sfclay pairings exercised: MYNN(5)↔MYNN-SL(5), YSU(1)↔revised-MM5(1),
  ACM2(7)↔Pleim-Xiu(7). Plus the canonical operational suite
  (Thompson/MYNN/MYNN-SL/Noah-MP, no cumulus) on real d02 Canary warm-start.
- Covering set (not the full cross-product): every wired scheme appears in ≥1 config.

This is an INTEGRATION smoke (stability/activation/traceability through the
coupler), NOT a WRF/obs skill comparison — that is the separate per-scheme
savepoint-parity lane reports + the operational d02/d03 validation.

---

## 4. Scope documentation (README.md)

- Added `## Supported physics schemes (v0.6.0 menu)` — the rows-1-9 matrix
  (6 families) with GPU-scan status and valid PBL↔sfclay pairings.
- Added `## Not yet supported (post-0.9.0 TODO)`, naming: spectral-bin MP
  (HUJI Fast/Full-SBM), P3, NSSL 2-moment, aerosol-aware MP
  (Thompson-aerosol / Morrison-aerosol), CAM/Goddard/FLG/RRTMG-K radiation,
  CLM4/CTSM/SSiB land, urban canopy (SLUCM/BEP/BEM), lake model, WRF-Chem, DFI,
  spectral + obs FDDA nudging.

---

## Close verdict

**v0.6.0 CLOSE = YES.** Wiring verified (no gap), namelist fail-closed matrix
verified for the full menu, integration matrix all-PASS with every scheme
active, scope documented. GF/Tiedtke GPU-batching is a documented post-0.6.0
TODO (the intended boundary), not a v0.6.0 regression.

## Risks carried forward

1. **GF / Tiedtke are CPU-reference only** (gpu_runnable=False, fail-closed in
   the GPU scan). GPU jit/vmap batching is post-0.6.0.
2. **Integration smoke is CPU + short** (proves finite/physical/active/traceable,
   not WRF/obs skill). Per-scheme skill lives in the lane parity reports + d02/d03
   operational validation.
3. **README status banner still narrates v0.1.0/v0.4.0**; the scheme matrix is
   additive. A full banner refresh to 0.9.0 is a release-worker task (out of
   this close's file ownership).
