# v0.1.0 Tag Checklist — the single gate to `git tag v0.1.0`

Authoritative pre-tag checklist, updated 2026-05-31 after the GPT-5.5 readiness review
(`.agent/reviews/2026-05-31-gpt-hfx-and-proof-review.md`) and the d03 24h PASS. Supersedes
the scattered status; the binding machine contract remains `publish/VERIFICATION.md` (11 rows)
and the paper cross-ref is `publish/paper/missing_elements.md`.

**Tag rule (unchanged):** tag only when VERIFICATION.md rows 1–11 are all PASS *on the release
commit*, claims are narrowed to what the proofs support, and the docs/paper carry no stale or
over-claimed numbers. No row is relaxed to manufacture a pass.

## Status snapshot (2026-05-31 17:30)

| Gate | State | Owner |
|------|-------|-------|
| d03 1km 24h | ✅ **D03_1KM_VALIDATED / PASS / finite** (`0c45e17`); T2 RMSE 1.92 K (gate 3.0) beats persistence (skill +0.16); V10 mostly beats persistence; **U10 honest weak spot** — wins early leads, loses at longer leads but within 7.5 gate | campaign agent ✅ |
| HFX/MYNN z_t fix | ✅ committed (`d1c373b`) + validated end-to-end; ⚠️ **claim must be narrowed** (see G1) + moisture no-regression unverified (G3) | manager + campaign |
| d02 3km re-validate on final commit | 🔄 **running** (pid 593636, ~75 min) | campaign agent |
| Real n=3 GPU TOST | ⏳ queued (after d02) | campaign agent |
| verify_all GPU rows on release commit | ⏳ queued | campaign agent |
| counted D2H + repeatability + restart | ⏳ queued or drop-claim | campaign agent |
| Paper claim-narrowing + stale purge + citations | 🔄 **dispatched (parallel, non-GPU)** | doc worker |
| Figure renders (6 PNGs) | 🔄 **dispatched (parallel, non-GPU)** | figure worker |
| env manifest + repo URL + tag commit | ⏳ final (human + manager) | manager + human |

## GPT BLOCKS-TAG items → resolution plan

- **G1 — Narrow the HFX claim.** The z_t block is an *empirical partial MYNN-inspired* land
  thermal-roughness repair, **not** a faithful `module_sf_mynn.F` port (3 formula mismatches:
  zol solved with momentum znt before the z_t block; restar from blended ustar = 1-step
  look-ahead; psih2/psih10 use thermal not momentum baseline). The refinement commit `d1c373b`
  post-dates the review — re-check whether it closed any mismatch, but **default to the narrowed
  wording** in paper §6.3/§6.5, DYCORE_STATUS, README, and surface_layer.py comments. → doc worker
  (paper/docs) + manager (code comments, after campaign releases surface_layer.py).
- **G2 — HFX oracle is partial.** Keep it as supporting (not root-cause-proof) evidence; the d03
  24h end-to-end PASS is now the primary d03 evidence. Acceptable for v0.1.0 as worded.
- **G3 — Moisture/PBL no-regression.** The fix moves psiq onto z_t → Q2/LH/QFX/MOL/PBLH regression
  risk. The d02/d03 re-runs must confirm Q2/LH/U10/V10/PBLH did not regress (not just T2). → campaign.
- **G4 — d03 gate.** ✅ **RESOLVED** by the 24h PASS on `0c45e17`. d03 1km enters the positive
  claim *with field qualifiers* (T2 ✅ beats persistence; V10 ✅ mostly; U10 wins early / loses late,
  within gate). No contract amend-away needed.
- **G5 — Proof table on release commit.** Regenerate `proofs/PROOF_TABLE.md` via
  `VERIFY_RUN_GPU=1 scripts/verify_all.sh` on the *final* commit; verify_all must not exit 0 with
  deferred GPU rows for the release run. → campaign.
- **G6 — TOST row is a self-test.** Replace the CPU-vs-CPU plumbing PASS with the real n=3 GPU
  paired-delta (per-case deltas, empirical sigma, 90% CIs, TOST p-values, exclusion log), labeled
  **"UNDERPOWERED SINGLE-SEASON DESCRIPTIVE CHECK," never "equivalence PASS."** → campaign.

## NON-BLOCKING (fold in opportunistically)

- VERIFICATION.md row-6 wording → rename to "TOST machinery + underpowered n=3 descriptive check."
- d03 3.0 K threshold → cite predeclared origin; keep field-qualified persistence statements.
- Land branch comments must not imply general MYNN coverage (no snow/ice Andreas_2002, no stochastic).
- GPT-suggestion hedges: AEMET/HARMONIE-AROME citation (#24), prior-art "first" audit (#25),
  author-reflection placement (#28) — keep softened wording if uncited.

## Parallelism plan (constrained: 28 CPU cores busy with the last CPU-WRF run ~8h; ~4 cores free; GPU serial)

- **GPU (serial, campaign agent):** d02 re-validate → n=3 TOST → verify_all GPU rows → D2H/repeat/restart.
- **CPU ≤4 cores, parallel now (non-GPU, no campaign-file overlap):**
  - doc worker → claim-narrowing (paper §6.3/§6.5, DYCORE_STATUS, README), stale-text purge
    (README 22.26×, M7-era tables, honesty_audit.md), citation-check (29 keys), publication-audit
    script, GPT-suggestion hedges. Does **not** touch publish/tables/, proofs/, VERIFICATION.md,
    surface_layer.py (campaign-owned).
  - figure worker → render 6 PNGs from existing PPMs/tables (matplotlib, light).
- **Manager (after campaign finishes):** number-fill paper §6.2/§6.3 from final proofs, code-comment
  narrowing on surface_layer.py, env manifest, final docs refresh, tag.
