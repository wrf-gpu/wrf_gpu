# v0.4.0 CLOSE — native standalone init + LBC

**Status: CLOSED (honest). 2026-06-03.**
**Closed by:** Opus 4.8 release-consolidation worker on `worker/opus/v040-close`.
**Proof object:** [`proofs/v040/v040_close_proof.json`](../../proofs/v040/v040_close_proof.json).

## What v0.4.0 is (and is NOT)

v0.4.0 adds a **native standalone init + LBC** path: the model can assemble its own
`wrfinput`/`wrfbdy` from met_em-stage forcing (S1 dynamics + S2 surface/soil + S3 LBC) instead of
consuming a CPU-WRF `wrfinput`/`wrfbdy` produced by `real.exe`.

- **CLAIM (honest):** *standalone native init equivalence + a stable 24 h forecast, with a
  documented near-surface wind-bias limitation under investigation.*
- **DO NOT CLAIM:** "full standalone forecast skill", or that the wind bias is fixed.

## Close criterion — MET

The close criterion is **native-init equivalence at t=0 + 24 h stability + an honestly documented
forecast-skill limitation** (not a fidelity bug). All three are satisfied:

1. **Native init PROVEN equivalent to `real.exe` at t=0.**
   [`proofs/v040/s5_native_init_parity_report.json`](../../proofs/v040/s5_native_init_parity_report.json)
   — every dynamics / surface / soil-numeric / `wrfbdy` field passes the FROZEN tolerances vs
   `real.exe` `wrfinput_d0{1,2,3}` + `wrfbdy_d01`. The **only** residual is a single static
   `start_em` cell `ISLTYP`/`IVGTYP` at `(j=22, i=86)` — a documented categorical LSM-init artifact
   (manager decision: **track, do not weaken the exact-categorical gate**).

2. **24 h standalone forecast is STABLE/FINITE/PHYSICAL.**
   [`proofs/v040/forecast_gate_24h_report.json`](../../proofs/v040/forecast_gate_24h_report.json)
   — verdict `STABLE_BUT_CORE_FIELD_MISMATCH`; 4 cases scored, **all stable, finite, physical**,
   with h2+ core skill. The "core field mismatch" is the wind bias below, NOT instability.

3. **The remaining gap is an HONESTLY DOCUMENTED forecast-skill limitation, not a fidelity bug**
   (see the next section).

## The open item — standalone 24 h near-surface westerly bias (DOCUMENTED, NOT FIXED)

The single open item after 10 debug rounds is a **domain-uniform near-surface WESTERLY excess** in
the 24 h **standalone** forecast:

- +1.2 m/s (20260429), +0.75 m/s (20260521).
- **T2 is correct**; the excess is column-wide, **stable and finite**.
- It is **NOT** a fidelity bug: it has been ruled out, vs unmodified WRF, against every faithful
  ported operator and scheme. It is **dynamical**, not traceable to any ported component, and
  possibly sample/config-specific but untestable without new met_em-preserving CPU-WRF runs.

### The 10 rounds — what each ruled out

| # | Angle | Outcome (vs unmodified WRF) |
|---|-------|------------------------------|
| 1 | Missing KF cumulus (agy lead) | **FALSIFIED.** JAX cu0-vs-cu1 moves U10 <0.001 m/s; independent unmodified-CPU-WRF cu0-vs-cu1 +0.012 m/s symmetric (no domain-uniform westerly); RAINC≡0; KF-eta has no momentum tendency. |
| 2 | Native-init equivalence at t=0 | **PROVEN EQUIVALENT.** S5 parity PASS (only the 1-cell categorical LSM residual). Init is not the source. |
| 3 | Near-surface / PBL momentum mixing + surface stress | Adjudicated WRF-faithful; not the source. |
| 4 | MYNN `s_aw` momentum floor | **Real WRF omission found + fixed** (apply `s_aw` floor to `kmdz` even with `bl_mynn_edmf_mom=0`, `module_bl_mynnedmf.F:3990-3997`); savepoint PASS; does **not** collapse the bias. |
| 5 | Split-explicit `php` (advance_uv 4th PGF term) | `php` STAGE-CONSTANT fix is WRF-faithful (`calc_php` is `INTENT(IN)`); savepoint PASS; does **not** collapse the bias. |
| 6 | Dry-mass continuity for specified/nested LBCs | **WRF-faithful fix** (BC-conditional `advance_mu_t` bounds); collapses the standalone PSFC−/U10+ *drift*; savepoint PASS. A residual uniform westerly excess remains. |
| 7 | PGF `al`(alb/muts) + `dpn`(cfn/cfn1) large-step terms | WRF-faithful (`calc_p_rho_phi` + top_lid `dpn` faces); savepoint PASS. Adjudication: bias **NOT** in the large-step PGF/al terms. |
| 8 | Horizontal diffusion + GWD | Sensitivity negligible. |
| 9 | Nest feedback | `feedback=0`; not a contributor. |
| 10 | Kain-Fritsch cumulus (decisive, independent) | JAX KF-wire gate **and** unmodified-CPU-WRF `cu_physics=0`-vs-`1` both <0.01 m/s. KF never fires in the trade-wind subsidence regime. |

**Expand-dates falsification is DATA_BLOCKED:** only 2 native-runnable dates remain; the rest have
purged met_em. Carried as a tracked cross-version forecast-skill item:
[`.agent/tasks/V0.4.0-WIND-BIAS-CARRYOVER.md`](../tasks/V0.4.0-WIND-BIAS-CARRYOVER.md).

## What was consolidated onto the integration trunk

Consolidated onto trunk `350a7c6` (the v0.6.0 12-scheme operational-scan integration; shared
merge-base `96eb4e4` with the v0.4.0 lineage). This is the v040↔v060 consolidation the roadmap
flagged.

### The 4 kept dycore fixes (all WRF-faithful, all savepoint-PASS)

| Fix | Source | Consolidated | Savepoint parity |
|-----|--------|--------------|------------------|
| Dry-mass continuity (specified/nested LBCs, BC-conditional) | `a1def01` | `f3a0241` | `mu_continuity_savepoint_parity.json` PASS |
| PGF `al`(alb/muts) + `dpn`(cfn/cfn1) | `b827469` | `6fb6a51` | `boundary_transport_savepoint_parity.json` PASS |
| MYNN `s_aw` stability floor on `kmdz` | `770287a` | `adfd3a9` | `r4_saw_floor_savepoint_parity.json` PASS |
| Split-explicit `php`-freeze (advance_uv 4th PGF) | `a668238` | `b6f610f` | `r5_php_freeze_savepoint_parity.json` PASS |

**Conflict resolution (WRF-fidelity-preserving):** in `operational_mode.py` the only conflict was
the placement of the new `_acoustic_lateral_bc_flags` helper; the v0.4.0-base-only
`_runtime_base_state`/`_base_theta_for_state`/`_t0_for_state` helpers were **discarded** (trunk
v0.6.0 builds `BaseState` inline and never calls them — reintroducing them would be dead code). The
s_aw and php-freeze fixes auto-merged cleanly against the v0.6.0-diverged `physics_couplers.py` /
`mynn_pbl.py` / `acoustic.py`; the merged `s_aw` floor and `php_stage` threading were verified
present.

### Kain-Fritsch `cu_physics=1` capability — ALREADY on trunk (v0.6.0), the older v0.4.0 wiring NOT re-applied

Trunk `350a7c6` **already** wires KF-eta into the operational scan via
`CU_SCAN_ADAPTERS = {1: kf_adapter}` (`scan_adapters.py`), with `initial_kf_carry` / `kf_adapter` /
`step_kf_column` (`cumulus_kf.py`) and full NCA bookkeeping. This v0.6.0 integration is **more
complete and more recent** than the parallel v0.4.0 KF-wire (`828ff90`/`075131e`) that was built
only to run the bias-falsification gate. Re-applying the older wiring would conflict with and
regress the unified 12-scheme scan, so it was **not** re-applied.

- **KF oracle gate: 6/6 PASS** — `tests/test_kf_cumulus_oracle.py` (single-column Fortran driver
  linked against unmodified WRF `module_cu_kfeta.F`).
- **KF savepoint parity: PASS (5 cases)** — `proofs/v060/kf_savepoint_parity_report.json`;
  adapter+savepoint test `tests/test_v060_cumulus_kf.py` 2/2 PASS.
- **cu-off (`cu_physics=0`) replay-safe:** `cumulus_carry=None`, the scan cumulus slot is skipped
  (`if cu_opt in CU_SCAN_ADAPTERS`), structurally identical to the pre-v0.6.0 carry.

The v0.4.0 **KF-falsification proof artifacts** were carried for the record:
`forecast_gate_kf2date6h_{COMPARE,cu0_BEFORE,cu1_AFTER}.json` + `kf_before_after_gate.py`.

## No-regression evidence (regenerated on the consolidated branch)

- **Idealized periodic path: BIT-IDENTICAL** (`max_abs = 0.0` on mu/mudf/muts/muave/ww/theta) vs
  the pre-fix diagnosis branch — `proofs/v040/idealized_no_regression_report.json`
  (`periodic_advance_mu_t_bit_identity_vs_diagnosis_branch`). The BC-conditional fixes default to
  `periodic_x=True / specified=False / nested=False` and `php_stage=None`, so the idealized path is
  unchanged by construction and proven bit-identical. (The warm-bubble / density-current verdicts in
  the same report are cached GPU diagnostics; GPU was not available to this CPU-only worker.)
- **Replay path: PASS / unchanged** — `proofs/v040/replay_path_impact_check.json`. BC-flag routing
  verified: idealized→(T,F,F); d02 replay specified→(F,T,F); d03 nested→(F,F,T).
- **CPU tests pass:** KF oracle 6/6; KF adapter+savepoint 2/2; mu_t LBC bounds 4/4; advance_mu_t
  parity 2/2; MYNN oracle+tier1 3/3. All touched modules compile; `gpuwrf` imports clean.
- **GPU-gated tests not run:** `test_m4_acoustic.py` / `test_m4_dycore_step.py` call
  `State.zeros` which **mandates a GPU device** (`contracts/state.py:_gpu_device`) and fail
  identically on unmodified trunk in a CPU-only sandbox — **not** a consolidation regression. (This
  worker was restricted to cores 0-3, no GPU, per the resource lock.)

## Unresolved risk

The standalone 24 h near-surface westerly bias (+0.75…+1.2 m/s, 2-date MAM sample) is unexplained
after 10 debug rounds — ruled out vs all faithful ported operators/schemes but **not root-caused**.
Expand-dates falsification is DATA_BLOCKED (purged met_em). Tracked for re-test once
met_em-preserving CPU-WRF runs are available (the 0.9.0 backfill gate).
