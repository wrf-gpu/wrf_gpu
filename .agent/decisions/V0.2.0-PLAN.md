# v0.2.0 Execution Plan — "all gap items except native init" (GPT-reviewed rev.2)

Status: **ACTIVE, principal-directed (nightly job 2026-05-31→).** Builds on
`POST-0.1.0-ROADMAP.md` (sequencing + effort) and `publish/GPU_PORT_GAPS_TODO.md` (gap
inventory). **Revised 2026-05-31 after the GPT-5.5 xhigh independent plan critique**
(`.agent/reviews/2026-05-31-gpt-v020-plan-review.md`) — its amendments are adopted. This doc is
the *execution* contract: what runs, in what order, in parallel with what, each item's proof
gate, owner model, and 0.1.x bundle. Manager keeps it updated after every closed item and outputs
the closed/running/remaining table.

## Honest scope reality (stated up front, no slop)

v0.2.0 = **P0-1, P0-3, P0-4, P0-5, P0-6, P0-7 + the P1 quality items** all closed; **S1 multi-GPU
is now an OPTIONAL v0.2.x scalability milestone** (GPT #7: low forecast-quality value, high
effort — it must NOT hold the quality paper hostage); native init (P0-2) excluded → post-0.2.0.
At swarm pace this is **~3–5 weeks**, multi-session — not one night. The nightly drive: land
**v0.1.0 committed + pushed**, start **Wave −1 corpus backfill + Wave 1 proof-debt items**, and
report honestly each tick. **0.2.0 pushes only when every in-scope item has a proof object and the
GPT-5.5 gap audit is clean.** No item is marked closed without its committed proof.

## Work mode (per item — operating model)

1. **Core/difficult** (dycore operators, LSM, nesting, sharding) → **Opus 4.8 MAX** in-process agent.
2. **Medium / writing / I/O / harness** → **Opus 4.8 xhigh**.
3. **Second opinion / debug / plan-or-gap review** → **GPT-5.5 xhigh** (codex, tmux).
4. Stuck past 2 hunts → split + empirical bisection, or GPT+agy council.
- **ONE GPU job at a time.** GPU items serialize; CPU items fan out — BUT (GPT #5) only parallel
  *diagnostic scaffolding* is safe. **Final proof closures that freeze tolerances, restart schemas,
  precision mode, or whole-run budgets serialize through a manager merge + rerun shared end-to-end
  gates.** Shared-core files (`operational_mode.py`, `state.py`, `contracts/precision.py`,
  `grid.py`, `halo.py`, `dynamics/**`, `physics_couplers.py`) are manager-merge-only.
- Commit each proof the moment it lands (hibernation-robust). Detached `nohup setsid` for GPU runs >10 min.
- Every item closes with a **falsifiable, non-gameable proof object** — see hardened gates below.

## Revised wave sequence (GPT-adopted)

**Wave 0 — finish v0.1.0 (in flight).** d03 ✅ → d02 per-case re-validate (case1 ✅ no-regression)
→ n=3 MAM GPU TOST (underpowered-single-season, never "equivalence PASS") → verify_all 11-row
PROOF_TABLE on release commit → conservation/repeat/restart/device. Then docs refresh → tag
`v0.1.0` → push `release/v0.1.0` branch (B).

**Wave −1 — CPU-WRF corpus / TOST manifest (START NOW, CPU, independent of GPU; gated on cores
freeing after the nightly CPU-WRF run ~01:30).** *(GPT #1 — the calendar long-pole.)* Build the
case manifest: selected/rejected cases, season labels, same-grid check, complete-pair masks,
CPU-WRF runtime/disk status, **frozen ADR-029 margins**. Backfill MAM members toward n≈15 (note
ADR-029 wants n≈27 for a 10% RMSE diff at 20% σ → if n=15 and empirical σ exceeds the planning
value, the release says **underpowered**). If corpus is all MAM, the gate is **single-season MAM
equivalence**, never "seasonal." Owner: Opus xhigh + CPU-WRF batch.

**Wave 1 — low-risk diagnostics + known proof debt (parallel *scaffolding*; final gates serialize):**
| Item | What | Owner | Hardened proof gate |
|---|---|---|---|
| **P1-4a** *(moved EARLY, GPT #4)* | MYNN/HFX parity debt: `z_t` in `zolrib`, `PSIH2/PSIH10` momentum baseline, prior-step UST; close GPT G1/G3 | **Opus MAX** (+GPT) | surface-layer oracle parity land/water × stable/unstable for HFX/LH/QFX/Q2/MOL/PBLH/U10/V10; **no wind/TOST claim closes before this** |
| **P1-5** | Thompson precip/water parity (adaptive-nstep, rr>1e-9, cloud-w sed) | Opus xhigh | **predeclared** precip+hydrometeor tol, WRF savepoints, accumulators, water closure, invalid-column fallback counts |
| **P1-8a** | **Declare fp64 operational release mode** (GPT #6: if fp32 has no speed win, fp64 is the smart default + documented non-goal) | Opus xhigh | per-mode gate table; mode declared **before** scoring |
| **P0-7a** | Conservation budget *instrumentation* (dry mass, total water, precip sink/source, heat/enthalpy, limiter/guard accounting) | Opus xhigh | budget terms defined before running; guards-off finite on real d02 |
| **P0-5a** | wrfout variable/metadata coverage inventory + current-state output proof | Opus xhigh | downstream diagnostic completeness vs WRF field list |

**Wave 2 — P0-6 + P1-6 MERGED (GPT #2): real-terrain / map-factor / boundary-dynamics + boundary-order advection.**
Owner **Opus 4.8 MAX** (+GPT if dicey). Hardened gate (GPT #6): **WRF-savepoint/analytic evidence
for each real-grid operator** (map factors, terrain slope/diffusion/PGF, specified+nested boundary
treatment, boundary-order degradation) **+ absolute** CPU-WRF/persistence station gates for
U10/V10/T2/Q2/PBLH, guard/limiter engagement counts, **no d03 boundary pump**. (Not merely
"improves vs v0.1.0.")

**Wave 3 — live nesting, SPLIT (GPT #3):**
- **P0-1a** nesting scheduler/interp/subcycling — prove parent→child boundary construction from
  recorded/controlled parent states; **no skill claim**; no in-loop host transfer.
- **P0-4** d01 Kain-Fritsch cumulus — WRF call/savepoint parity for tendencies + convective precip
  accumulators (or declare a documented physics-contract deviation).
- **P0-1b** live d01→d02→d03 skill vs CPU-WRF — **only after P0-6 + P0-4 closed**; compare boundary-
  strip U/V/W/T/QV/P/PH/MU, interpolation conservation, subcycling cadence, d03 skill on same masks.

**Wave 4 — radiation + land:** **P1-3** RRTMG fidelity (SWDOWN/GLW/toposhade/slope/aspect/lat-lon
WRF-oracle across solar geometry + clear/cloudy) · **P0-3** prognostic Noah-MP *(GPT #7: highest
overrun risk after native init — sharply scope soil/snow/canopy/hydrology option mapping;
energy/water closure, restart state, HFX/LH/TSK/T2/Q2/PBLH diurnal vs WRF over ≥24 h land-memory)*.
Freeze surface/radiation/land interfaces before final proof.

**Wave 5 — final I/O/restart + statistics:** **P0-5b** final `wrfrst` schema + true-state resume
proof (after P0-3/P1-3/P1-4/P1-5 final) · rerun conservation/precision/restart/transfer audits ·
**TOST** if the Wave −1 corpus is sufficient · **P1-7** GWD (CPU-WRF sensitivity check first; if
non-load-bearing on supported domains, document + skip) · **P2-2** namelist fail-fast checker
(cheap, prevents silent unsupported-option false fidelity — include before 0.2.0).

**Wave 6 (OPTIONAL, v0.2.x) — S1 multi-GPU domain decomposition.** Gate: no host-mediated halo in
the timestep loop; identical/predeclared-tol parity vs single GPU; transfer audit; strong/weak
scaling with compile-time separated. **Does not block the v0.2.0 quality paper.**

## 0.1.x release cadence (reconciled: principal's "version per major step" = GPT's coherent bundles)

Each **externally-meaningful bundle** (a "major step") gets an **annotated** tag + push to a
`release/0.1.x` branch with release notes + a regenerated proof table. **v0.1.0 stays immutable;
tags never move.** Bundles (GPT #10):
- **0.1.1** — MYNN/HFX parity debt + moisture/PBL no-regression + fp64-mode declaration (P1-4a, P1-8a)
- **0.1.2** — Thompson precip/water + conservation/non-masking budgets (P1-5, P0-7)
- **0.1.3** — P0-6 real-terrain/boundary dycore closure (+P1-6)
- **0.1.4** — output/diagnostic completeness (P0-5a)
- later tags for nesting / Noah-MP / S1 when each is independently usable
- **0.2.0** — all in-scope closed + GPT gap audit clean.

## Smartest 0.1.x publication subset (for the principal's tomorrow-afternoon decision)

If publishing the paper on a 0.1.x base rather than waiting for full 0.2.0, GPT's quality/effort
ranking: **(1) P1-4a MYNN/HFX/moisture parity** (highest correctness-risk per effort; cleans G1/G3)
→ **(2) P0-6 terrain/boundary** (largest wind lever, high risk — publish after if it lands, don't
block forever) → **(3) P1-5 Thompson precip/water** → **(4) P0-7 conservation/non-masking** (high
publication credibility; prevents "finite because guarded") → **(5) P1-8 precision declaration**
(cheap) → **(6) P1-3 RRTMG topo/slope** → **(7) P0-5a output completeness**. Defer for a paper base:
P0-1 nesting, P0-4 KF, P0-3 Noah-MP, S1 — higher effort/risk, proofs depend on the quality chain.
**Recommendation:** a 0.1.1–0.1.2 base (parity debt + precip + conservation + precision), ideally
+ P0-6 if it lands, is a strong, honest, publishable artifact well before full 0.2.0.

## The 3-case real GPU-vs-CPU compare (principal requirement)

Re-run the 3 Canary cases (05-09, 05-21, 05-29) as a **real compare**: GPU vs same-workstation
CPU-WRF corpus truth (NOT persistence-only, NOT self-compare) — paired RMSE/bias T2/U10/V10 + TOST
paired-delta. Folded into Wave 0 (d02 re-validate ✅case1 + n=3 TOST) and **re-confirmed after every
wave touching dycore/physics** (continuous-proof; nothing breaks silently).

## Remote / push — B CONFIRMED (principal 2026-05-31)

Public repo = **`git@github.com:wrf-gpu/wrf_gpu.git`** (remote `wrfgpu`; holds only the frozen,
retracted v0.0.1; **unrelated history** to this 897-commit rebuild). **B:** push `release/v0.1.0`
branch + `v0.1.0` tag to `wrfgpu`; **principal promotes to main** (do NOT force-overwrite public
main autonomously). 0.1.x cadence per the bundles above; review tomorrow afternoon.

## Zenodo paper finale (after v0.2.0 push, or on a 0.1.x base if principal chooses)

Opus xhigh rewrites `publish/paper/paper.md` to the released state (drop now-unneeded
relativization, honor every `[Enric:]`/`[GPT suggestion]` comment, follow Zenodo guidelines
exactly), clean `publish/paper/` to an upload-ready folder (PDF + metadata + `CITATION.cff` +
figures + bib), then GPT-5.5 reviews (edit-if-certain / else comment) → principal final read →
upload. Authors: principal as creator; **credit Claude + GPT as contributors as far as Zenodo
metadata allows** (Zenodo is lenient on creators/contributors — verify exact policy at finale;
honor the principal's wish to credit the AI honestly).
