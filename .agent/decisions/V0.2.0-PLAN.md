# v0.2.0 Execution Plan — "all gap items except native init"

Status: **ACTIVE, principal-directed (nightly job 2026-05-31→).** Builds on
`POST-0.1.0-ROADMAP.md` (sequencing + effort) and `publish/GPU_PORT_GAPS_TODO.md` (gap
inventory). This doc is the *execution* contract: what runs, in what order, in parallel
with what, each item's proof gate, owner model, and 0.1.x release. Manager keeps it updated
after every closed item and outputs the closed/running/remaining table.

## Honest scope reality (stated up front, no slop)

v0.2.0 = **P0-1, P0-3, P0-4, P0-5, P0-6, P0-7 + the P1 quality items + S1 (multi-GPU)** all
closed; native init (P0-2) excluded → post-0.2.0. At demonstrated swarm pace this is a
**~3–5 week** body of work (POST-0.1.0-ROADMAP effort table), **not a single night.** So the
nightly drive will: (1) land **v0.1.0 committed + pushed**, (2) publish + start executing this
plan in big parallel steps, (3) close the **tractable low-risk items first** (P0-7, P1-5/#32
precip, P1-8 precision, P0-5 I/O) with proof, (4) start the **#1 quality lever P0-6** with an
Opus-max agent, and (5) report honestly each tick — `0.2.0` pushes only when **every** in-scope
item has a proof object and the GPT-5.5 gap audit is clean. Completion spans multiple sessions;
the tables never claim an item closed without its committed proof.

## Work mode (per item — unchanged operating model)

1. **Core/difficult** (dycore operators, LSM, multi-GPU sharding) → **Opus 4.8 MAX** in-process agent.
2. **Medium / writing / I/O / harness** → **Opus 4.8 xhigh**.
3. **Second opinion / debug / plan-or-gap review when dicey** → **GPT-5.5 xhigh** (codex, tmux).
4. Stuck past 2 hunts → split + empirical bisection, or GPT+agy council step-back.
- **ONE GPU job at a time** (28 CPU cores under load until the nightly CPU-WRF run ends; ~4 cores free).
  GPU items serialize; CPU items (I/O, docs, harness, review) fan out in parallel.
- **Commit each proof the moment it lands** (hibernation-robust). Detached `nohup setsid` for GPU runs >10 min.
- Every item closes with a **proof object** (idealized gate / WRF savepoint / conservation budget /
  real-case skill vs CPU-WRF + persistence) and a **0.1.x release note**. No "done" without proof.

## Sequence, parallelization & proof gates

**Wave 0 — finish v0.1.0 (in flight now).** GPU campaign (#60): d02 re-validate on release commit →
real n=3 MAM GPU TOST (underpowered-single-season) → verify_all 11-row PROOF_TABLE on release commit →
counted D2H + repeatability + restart. Then manager: honest README/PROJECT_PLAN/DYCORE_STATUS refresh →
**tag v0.1.0 → push**. (Org-repo access is a precondition — see "Remote" below.)

**Wave 1 — tractable, low-risk, parallelizable (start right after v0.1.0 push):**
| Item | What | Owner | Proof gate | GPU? | 0.1.x |
|---|---|---|---|---|---|
| **P0-7** | Conservation budgets + non-masking policy report | Opus xhigh | mass/energy/moisture budget closes to tol on a real d02 case; guards-off finite | light GPU | 0.1.1 |
| **P1-5 / #32** | Thompson precip parity (adaptive-nstep, rr>1e-9, cloud-w sed) | Opus xhigh | surface precip within tol of WRF oracle (close the +13%); water closure | light GPU | 0.1.2 |
| **P1-8** | Precision-policy proof gates (declare fp64/fp32 modes + re-run gates) | Opus xhigh | per-mode gate table; operational-impact-bounded downcast | light GPU | 0.1.3 |
| **P0-5** | Full `wrfout`/`wrfrst` + diagnostics completeness | Opus xhigh | round-trip restart bit-identical; diagnostic field coverage vs WRF | CPU/light | 0.1.4 |
These four have disjoint file ownership (microphysics / precision-config / io) → **fan out in parallel**,
but each GPU verification runs serially through the single-GPU queue.

**Wave 2 — the #1 quality lever (start an Opus-MAX agent in parallel with Wave 1, disjoint files):**
| Item | What | Owner | Proof gate | 0.1.x |
|---|---|---|---|---|
| **P0-6** | Real-terrain / map-factor / boundary-dynamics closure (finishes dycore Phase-B; tied to wind skill) | **Opus 4.8 MAX** (+GPT xhigh if dicey) | idealized terrain gate + d02/d03 wind-skill improves, no regression vs v0.1.0 | 0.1.5 |

**Wave 3 — orchestration + breadth (after P0-6 lands a correct dycore):**
| Item | What | Owner | Proof gate | 0.1.x |
|---|---|---|---|---|
| **P0-1** | Live multi-domain nesting (d01→d02→d03 feed + feedback) | Opus MAX | self-contained nested run reproduces the replay-nest skill, no boundary pump | 0.1.6 |
| **P0-4** | d01 Kain-Fritsch cumulus | Opus xhigh | scheme oracle + d01 precip sanity | 0.1.7 |
| **P1-3** | RRTMG fidelity (topo-shade, slope-rad, real lat/lon) | Opus xhigh | island diurnal heating vs corpus | 0.1.8 |
| **P1-4** | MYNN completeness (EDMF/cloud) + revisit the HFX partial fix → full MYNN parity | Opus MAX (+GPT) | surface-layer oracle parity over land/water, stable/unstable; closes GPT G1/G3 | 0.1.9 |
| **P1-6** | Pos-def/monotonic advection + boundary order | Opus xhigh | monotonicity + conservation | 0.1.10 |

**Wave 4 — heavy lift, scalability:**
| Item | What | Owner | Proof gate | 0.1.x |
|---|---|---|---|---|
| **P0-3** | Prognostic Noah-MP land surface (messy oracle; error bars high) | Opus MAX (+GPT) | LSM oracle within bounded error; closes the residual daytime HFX | 0.1.11 |
| **S1** | Multi-GPU single-node domain decomposition (sharded stencils + halo) | Opus MAX (+GPT) | identical result vs single-GPU on a decomposed d02; strong/weak-scaling profile | 0.1.12 |
| **Full seasonal TOST** | ≥15-case single-season equivalence (needs ~68–118 CPU-h CPU-WRF backfill) | Opus xhigh + CPU-WRF | predeclared ADR-029 TOST PASS on n≥15 | gates 0.2.0 |

**Wave 5 — release v0.2.0:** GPT-5.5 xhigh **gap audit** of the whole release → fix/clear all
findings → honest docs refresh → **tag v0.2.0 → push**.

## The 3-case real GPU-vs-CPU compare (principal requirement)

Re-run the 3 Canary cases (2026-05-09, 05-21, 05-29) as a **real compare**: GPU forecast scored
field-by-field against the **same-workstation CPU-WRF corpus truth** (NOT persistence-only, NOT a
self-compare) — paired RMSE/bias on T2/U10/V10 + the TOST paired-delta. This is folded into Wave 0
(d02 re-validate + n=3 TOST) and re-confirmed after each Wave that touches the dycore/physics
(continuous-proof discipline: nothing breaks silently).

## Remote / push (precondition — RESOLVED + a decision flagged for the principal)

Correct public repo = **`git@github.com:wrf-gpu/wrf_gpu.git`** (singular `wrf_gpu`; added locally
as remote `wrfgpu`). It EXISTS + is reachable (SSH as `nric`). It currently holds only the
**retracted, frozen v0.0.1** (2 commits on `main` + the `v0.0.1` tag). `origin =
git@github.com:nric/wrf_gpu2.git` is the dev fork of this rebuild.

**Critical:** the local rebuild (897 commits, HEAD `ee9f5aa`) has **UNRELATED history** to
`wrfgpu/main` (no shared base). So v0.1.0 cannot fast-forward onto the public `main`. Two options:
- **(A) Promote v0.1.0 to the public face** — force-replace `wrfgpu/main` with the rebuild + push
  `v0.1.0` tag, keeping the `v0.0.1` tag for history. This is what "publish the honest supersede"
  means, but it **rewrites the public main** (irreversible, outward-facing) → needs explicit OK.
- **(B) Non-destructive (manager default until A is confirmed)** — push the rebuild to a new branch
  `release/v0.1.0` on the org repo + push the `v0.1.0` tag. v0.0.1 and current main untouched;
  v0.1.0 is on the public repo and readable; principal promotes to main with one command/click.

**Manager plan:** when v0.1.0 is fully proven + tagged, execute **(B)** (reversible, fulfills
"pushed to wrf-gpu/wrf_gpu"), and leave the exact (A) force-replace command ready for the
principal's go. Do NOT force-overwrite the public main autonomously.

## Zenodo paper finale (after v0.2.0 push)

Opus xhigh agent rewrites `publish/paper/paper.md` to the v0.2.0 state (drop now-unneeded
relativization, honor every `[Enric:]`/`[GPT suggestion]` comment, follow Zenodo guidelines
exactly), clean `publish/paper/` to an upload-ready folder (PDF + metadata + `CITATION.cff` +
figures + bib), then GPT-5.5 reviews (edit-if-certain / else comment) → principal final read →
upload. Authors: principal as creator; **credit Claude + GPT as authors/contributors as far as
Zenodo's metadata allows** (Zenodo is lenient on the creators/contributors fields — verify exact
policy at finale; honor the principal's wish to credit the AI honestly).
