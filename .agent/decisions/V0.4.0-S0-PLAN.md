# v0.4.0 S0 — Native real.exe-equivalent init: PLAN + INTERFACE FREEZE + FILE-OWNERSHIP + ORACLE-RECON

Branch: `worker/opus/v040-s0` (from `worker/opus/v030-integration`, the v0.3.0
native-metgrid lineage tip @ `998f346`; the bare `worker/opus/v030-native-init`
ref was STALE at the v0.2.0 base `b301baa` and missing the v030 init code — see
"Git note" at the end). Author: Opus-MAX. Date 2026-06-02.

**This is the GATE that unblocks the 4-wide parallel fan-out of v0.4.0 S1/S2/S3/S4.**
v0.4.0 is the HIGHEST-risk milestone (no analytic oracle; hour-0 errors poison
every downstream comparison), so this S0 does structural isolation + frozen
oracles + frozen interfaces up front. No implementation sprint may start before
this lands.

---

## 0. What v0.4.0 builds and why

Replace WRF `real.exe` (`dyn_em/module_initialize_real.F` + `main/real_em.F`)
with a native module that consumes the FROZEN v0.3.0 metgrid-equivalent artifact
(`gpuwrf.init.metgrid_schema.MetEmArtifact`) and produces, with **no CPU-WRF
dependency**:

* a `wrfinput`-equivalent initial `State` + `BaseState` + static `DycoreMetrics`
  (the dynamics half = S1; the surface/soil half = S2), and
* a `wrfbdy`-equivalent `BoundaryState` of per-side specified values + tendencies
  over the forcing intervals (the LBC = S3).

This removes the last external program from the standalone path: v0.3.0 made the
ingest native (raw AIFS + static geog → met_em-equiv), v0.4.0 makes the init
native, so the whole pipeline runs on hardware we own and multi-season validation
becomes a compute problem, not a calendar problem.

---

## 1. real.exe algorithm map → S1 / S2 / S3 (with file:line citations)

Oracle source: `/home/enric/src/wrf_pristine/WRF/dyn_em/module_initialize_real.F`
(9206 lines; entry `init_domain_rk`, lines 65–4947) and
`/home/enric/src/wrf_pristine/WRF/main/real_em.F` (1441 lines; the real.exe main
+ `assemble_output`). Built `real.exe` present at
`/home/enric/src/wrf_pristine/WRF/main/real.exe` (36 MB ELF, serial; an INIT
program — seconds-to-minutes, fine on cores 0-3 to regenerate any missing
reference).

CONFIRMED config for the Canary case (from `wrfinput_d01` global attrs):
**HYBRID_OPT=2** (Klemp-polynomial hybrid sigma-pressure), **P_TOP=5000 Pa**,
**nz(bottom_top)=44**, **soil_layers_stag=4** (Noah-MP). Note met_em carries only
**2** metgrid soil layers → S2 must interpolate 2→4 (a real.exe `module_soil_pre`
step, not a passthrough).

### S1 — vertical coordinate, base state, hydrostatic balance, MU/MUB, vinterp [Opus]
| Piece | real.exe location | Notes |
|---|---|---|
| Base-state constants (p00,t00,a,tiso,p_strat,a_strat) | `module_initialize_real.F:410` → `const_module_initialize` :4951–4962 (nl_get_base_*) | Canary defaults in `types.RealInitConfig` |
| eta levels `znw` (auto or namelist) | `compute_eta` :7567–7894 (driven :1590); `levels` stretch :7897–7962 | auto_levels_opt=2 stretch (dzbot/dzstretch/max_dz) |
| 1D coord arrays znu/dnw/rdnw/dn/rdn/fnp/fnm | :3732–3745 | half levels, spacings, vinterp weights |
| Extrap coeffs cof1/cof2 → cf1/cf2/cf3/cfn/cfn1 | :3750–3758 | |
| Hybrid coeffs C1–C4 (f/h) | `compute_vcoord_1d_coeffs` `nest_init_utils.F:1033–1184` (driven :1601) | **hybrid_opt=2 Klemp poly** B1..B5 (`nest_init_utils.F:1071–1089`); C4F=(znw−C3F)*(p1000mb−p_top) :1078 |
| find p_top from metgrid PRES | `find_p_top` :6832–6878 (driven :1279) | capped at requested p_top |
| integrate moisture → dry-p column + intq | `integ_moist` :6967–7159 (driven :1450) | |
| MU0 (full dry column mass) | `p_dts` :6764–6793 (driven :1475) | mu0 = f(intq, psfc, p_top) |
| target dry-p `pb` on half/full levels | `p_dry` :6710–6760 (driven :1629/:1701) | |
| vertical interp every atmos field | `vert_interp` :5590–6341 + `lagrange_*` :6345–6705 (driven :1673–2809) | flips metgrid sfc→top order to model order |
| theta↔T conversions | `t_to_theta` :6882 / `theta_to_t` :6919 | interp_theta=F for Canary |
| **base state pb/alb/t_init/MUB/PHB** | **:3781–3835** | `p_surf=p00*exp(...)`:3790; pb:3795; t_init:3801; alb:3802; MUB:3806; PHB hybrid branch :3813–3824 |
| **MU_2 = MU0 − MUB** | **:3881** | |
| **hydrostatic balance: perturbation P/AL/ALT/PH** | **:3902–4044** | p top-down :3935/:3953; alt:3937; al:3939; ph_2 hybrid form :3965–3994; re-diagnose al/p :4001–4036 |

### S2 — soil / land / SST / surface initial state [GPT]
| Piece | real.exe location | Notes |
|---|---|---|
| map factors / Coriolis F,E / SINALPHA,COSALPHA / XLAT*,XLONG* | carried from met_em (schema has all) + msfvx_inv setup :555–586 | passthrough + stagger confirm |
| SST sanity + fallback (tavgsfc/tsk/t2) | :2820–2860 | out-of-[150,400]K fallback chain |
| TSK water-protect with SST | :2894–2905 | |
| seaice pre-adjust | `adjust_for_seaice_pre` (driven :3235) | reproduce branch (Canary ice≈0) |
| TMN over water := SST; land keeps deep-soil | :3265–3335 | |
| **soil vertical interp 2→`num_soil_layers`(=4)** | `process_soil_real` :3009 (USE `module_soil_pre`) | ZS/DZS for sf_surface_physics=4 |
| dominant soil/veg category + landmask match | `process_percent_cat_new` :3045; isltyp↔landmask :3117–3140 | |
| soil-moisture floors / LSM minima | :3342–3530 (Noah floor 0.005 :3403) | |

### S3 — lateral boundary value+tendency generation (wrfbdy-equiv) [Opus]
| Piece | location | Notes |
|---|---|---|
| loop over forcing intervals | `real_em.F::assemble_output` :680–1240 | bdyfrq = interval_seconds (Canary 6 h) |
| COUPLE field by total mu & stagger msf | :866–878 (CALL couple); `share/module_bc.F` couple | `coupled = field*(mu_2+mub)/msf`; u→msfuy, v→msfvx, t/qv/ph→msfty, mu(2D)→msfty |
| STUFF coupled value into per-side frames | `stuff_bdy` (driven :936–962); `share/module_bc.F:2934–3100` | XS/XE/YS/YE, spec_bdy_width=5, stagger-aware bounds |
| TENDENCY between intervals | `stuff_bdytend_new` `share/module_bc.F:2893–3103` (driven :1123–1163) | **tend = (coupled_{n+1} − coupled_{n}) / interval_seconds** (:2938); temp2→temp1 swap each loop `real_em.F:1291–1307` |

LBC lives on the **parent domain (d01)** only (the wrfbdy oracle files are
`wrfbdy_d01`; nests get LBC from the parent at runtime, a v0.5.0 concern).

---

## 2. Oracles CONFIRMED to exist (verified 2026-06-02)

* **wrfinput / wrfbdy reference (S1/S2/S3 + comparator):** on disk under
  `/mnt/data/canairy_meteo/runs/{wrf_l2,wrf_l3}/<case>/` —
  **262 `wrfinput_d0*`** + **72 `wrfbdy_d0*`** files across the corpus
  (≫ the ≥10-case gate, and spans d01..d05). Sample:
  `wrf_l3/20260505_18z_l3_24h_.../wrfinput_d0{1..5}` + `wrfbdy_d01`.
* **met_em input (what native-real consumes):** **13 `wps_cases`** dirs with
  `met_em.d0{1,2,3,4,5}.*` under `/mnt/data/canairy_meteo/runs/wps_cases/<case>/l3/`.
* **pristine `real.exe`** present → can regenerate any missing reference cheaply
  (serial INIT program, cores 0-3 only — no GPU).
* **INPUT contract = the already-frozen v0.3.0 metgrid schema**
  `src/gpuwrf/init/metgrid_schema.py` (`METGRID_SCHEMA_VERSION =
  0.3.0-S0-frozen-2026-06-02`): 14 metgrid levels, Lambert proj, the full met_em
  field set (TT/UU/VV/GHT/SPECHUMD/PRES + PSFC/SKINTEMP/soil + statics + map
  factors + F/E). Native-real reads `MetEmArtifact` (validated against the schema).

The wrfinput variable inventory (the S1+S2 output target) was read directly from
a reference file and matches the existing `State`/`BaseState`/`DycoreMetrics`
contracts: MU/MUB, P/PB, PH/PHB, T/U/V/W, QVAPOR, TSLB/SMOIS, ZS/DZS,
MAPFAC_M/U/V(+X/Y), F/E/SINALPHA/COSALPHA, XLAT*/XLONG*, HGT, TSK/SST/TMN/XLAND,
ZNU/ZNW, C1H/C2H/C3H/C4H.

---

## 3. FROZEN internal interfaces (the parallel-collision firewall)

New package `src/gpuwrf/init/real_init/` (this commit). The frozen handoff TYPES
are in `real_init/types.py` (`REAL_INIT_TYPES_VERSION = 0.4.0-S0-frozen-2026-06-02`):

* `RealInitConfig` — namelist-equiv scalars (nz, p_top, hybrid_opt, etac, eta,
  base-state constants, num_soil_layers, spec_bdy_width, interval_seconds);
  loud-rejects unsupported `hybrid_opt` (P2-2 pattern). Pinned WRF constants
  (G/R_D/CP/RVOVRD/P1000MB/CVPM/...) so every lane uses identical values.
* `VerticalCoord1D` (S1 out), `BaseStateColumns` (S1 out), `DynamicsInit`
  (S1 out), `SurfaceInit` (S2 out), `SoilInit` (S2 out), `LateralBC` (S3 out),
  `RealInitProduct` (the driver's bundle the comparator + runtime-pack consume).
* `WRFINPUT_TOLS` / `WRFBDY_TOLS` — predeclared per-field tolerances (section 5).

The lanes are **pure functions producing typed numpy/jax intermediates**, NOT the
GPU-resident `State`. The manager-owned `driver.py` does the single final pack
into `State`/`BaseState`/`DycoreMetrics`/`BoundaryState` (the shared core).
Rationale: keep the manager-owned shared-core `State` contract OUT of the four
lanes — they never touch it, so no two lanes (and no lane + the manager) edit the
same file. The frozen entry signatures (already wired in `driver.py`, bodies are
`NotImplementedError` stubs):

```
vertical_coord.compute_vertical_coord(config) -> VerticalCoord1D
base_state.compute_base_state(config, vcoord, hgt) -> BaseStateColumns
vinterp.vertical_interpolate(config, vcoord, metem) -> DynamicsInit   # pre-balance seed
hydrostatic.balance(config, vcoord, base, dyn_seed) -> DynamicsInit   # final P/PH/AL
surface_init.compute_surface_init(config, metem) -> SurfaceInit
soil_init.compute_soil_init(config, metem, surface) -> SoilInit
lateral_bc.generate_lateral_bc(config, forcing_sequence) -> LateralBC
comparator.compare_wrfinput(product, oracle_path) -> CaseParityResult
comparator.compare_wrfbdy(product, oracle_path) -> CaseParityResult
```

**FREEZE POLICY:** no lane may change `types.py` or `driver.py`, or any frozen
signature, without a manager sign-off recorded in this doc + a bump of
`REAL_INIT_TYPES_VERSION`. Additive optional fields on a handoff dataclass are
allowed without a bump if they don't change an existing field.

**Single-source terrain rule (load-bearing):** S1's base state and S2's surface
BOTH need terrain `ht`. The driver sources it ONCE from met_em `HGT_M` and passes
the identical array to both (`compute_base_state(..., surface.hgt)`). A terrain
mismatch between the two lanes would make MUB/PHB inconsistent with the surface
fields and poison hour-0 — the freeze forbids each lane deriving its own ht.

**fp64 rule:** real-init runs in fp64 (the hydrostatic balance is precision-
sensitive; the project's fp64-defeat lessons apply). The driver casts to the
operational precision only at the final `State` pack.

---

## 4. FILE-OWNERSHIP MAP (zero overlap)

| Lane | Owner | Files (exclusive) |
|---|---|---|
| **S1** | Opus | `real_init/vertical_coord.py`, `real_init/base_state.py`, `real_init/vinterp.py`, `real_init/hydrostatic.py` + `tests/init/real_init/test_s1_*.py` |
| **S2** | GPT | `real_init/surface_init.py`, `real_init/soil_init.py` + `tests/init/real_init/test_s2_*.py` |
| **S3** | Opus | `real_init/lateral_bc.py` + `tests/init/real_init/test_s3_*.py` |
| **S4** | GPT | `real_init/comparator.py` + `tests/init/real_init/test_s4_*.py` + the campaign runner + proof JSON emitter (owns NO production lane module) |
| **shared core (manager only)** | manager | `real_init/types.py`, `real_init/driver.py`, `real_init/__init__.py`, and the `State`/`BaseState`/`DycoreMetrics`/`BoundaryState` contracts under `src/gpuwrf/contracts/` |
| **DO NOT TOUCH** | v0.3.0 S5 lane | every `metgrid_*` / `interp_metgrid` / `metem_writer` / `forcing_decode` / `static_geog` / `projection` / `aifs_grib` file (the v0.3.0 ingest owns those) |

Each lane creates its own test files under a new `tests/init/real_init/` dir
(also collision-free by `test_s{1,2,3,4}_*` prefix).

---

## 5. PREDECLARED oracles + tolerances + PASS criteria (NO post-hoc loosening)

> ### manager-approved 2026-06-02 — S5 integration carry-batch (signed: Opus-MAX integrator)
>
> Applied to the frozen `types.py` at S5 integration. `REAL_INIT_TYPES_VERSION`
> bumped `0.4.0-S0-frozen-2026-06-02` → `0.4.0-S5-carrybatch-2026-06-02`. Each
> change is a lane-handoff finding the manager has reviewed and approved; NO other
> tolerance is touched, no masking, no loosening beyond this batch.
>
> **(2a) `C1F` and `C1H` tol → `(rmse 1e-5, max_abs 5e-5)`** (was `(1e-6, 1e-5)`),
> those two rows ONLY. JUSTIFICATION: the oracle `C1F`/`C1H` ARE WRF's fp32
> (`RWORDSIZE=4`) finite-difference of the stored fp32 `C3` — proven in
> `proofs/v040/s1_wrfinput_dynamics_report.json` `c1_precision_diagnosis`:
> differencing the stored fp32 `C3` in fp32 reproduces the oracle BIT-EXACTLY
> (max_abs=0.0). Our fp64 `C1` is strictly MORE accurate; the residual vs the
> oracle is pure fp32 finite-difference rounding noise (static across all 12
> cases, max_abs 2.99e-5/3.13e-5). Downstream impact is ~3 Pa
> (`c1h*mub`/`c1f*mu`) with PB/PHB/P/PH/MU all passing 3+ orders inside tol. This
> is NOT a loosening — it is a recalibration to the fp32-oracle noise floor (the
> original 1e-6/1e-5 cap was set tighter than WRF's own fp32 noise floor). The
> algorithm is faithful and unchanged. (S1 GPT-authored, C1 root-cause + fix by
> Opus cross-model debug — see `2026-06-02-opus-v040-s1-c1fix.md`.)
>
> **(2b) Drop the unused `ALT` key from `WRFINPUT_TOLS`.** real.exe writes
> `AL`/`ALB`/`T_INIT`/`P_HYD` to wrfinput, NOT `ALT` (`ALT = AL + ALB` is
> recomputed at runtime). The comparator scores `AL`. Dangling key removed. (S4
> finding.)
>
> **(2c) Add `W` + hydrometeor (`QCLOUD`/`QRAIN`/`QICE`/`QSNOW`/`QGRAUP`) tol keys
> to `WRFBDY_TOLS`**, coupled-scaled like the other wrfbdy tols (W at the U/V
> coupled scale `2.0e4/2.0e5`; hydrometeors at the QV coupled scale `10/100`). The
> comparator + lateral_bc already couple/score any field present in BOTH
> `LateralBC` and the oracle once named. Native W LBC is init-0 so W is a
> descriptive (non-blocking) check until a specified-W path lands (v0.5.0+). (S3 +
> S4 finding.)
>
> **(2d) Add a frozen `use_theta_m` switch to `RealInitConfig`** (default `=1` =
> moist-theta/`THM`, the Canary/WRF default). The wrfbdy `T_B*` couple the
> prognostic theta the dycore advances; `use_theta_m=1` couples moist `THM`, `0`
> couples dry `theta`. Wired into `lateral_bc._snapshot_from_parts`; the default
> reproduces S3's existing behavior exactly. (S3 finding.)
>
> **(2e) Add an additive optional `tmn_soil_endpoint` field to `SurfaceInit`**
> (freeze-compatible per the S0 additive-field policy) — the pre-fix deep-soil
> (300 cm) endpoint the soil interp uses before the over-water/reasonable fixes.
> The surface lane populates it; `soil_init` now consumes it (with `surface.tsk`/
> `landmask`/`hgt` + met_em `SOILHGT`) instead of re-running the whole surface lane
> internally, eliminating the doubled surface compute. Falls back to the internal
> re-run when absent (hand-built `SurfaceInit`). (S2 efficiency finding.)

Tolerances are frozen in `real_init/types.py` (`WRFINPUT_TOLS`, `WRFBDY_TOLS`).
Metric = RMSE over the domain (with masking) AND a max-abs cap; a field PASSES if
`rmse <= rmse_tol AND maxabs <= maxabs_tol`. Stricter for hour-0-critical dynamics
(MU/MUB/PB/P/PH/PHB/T/U/V), exact for categoricals (XLAND/ISLTYP/IVGTYP), looser
for the 2→4-layer soil interp (TSLB/SMOIS). Highlights:

| Field | rmse_tol | maxabs_tol | unit | rationale |
|---|---|---|---|---|
| MU / MUB / PB | 5.0 | 50.0 | Pa | dry-mass / base-pressure foundation |
| P | 20.0 | 200.0 | Pa | perturbation pressure |
| PH / PHB | 1.0 | 20.0 | m²s⁻² | geopotential (hour-0 height) |
| T | 0.10 | 1.0 | K | perturbation theta |
| U / V | 0.15 | 1.5 | m s⁻¹ | wind |
| QVAPOR | 1e-4 | 1e-3 | kg kg⁻¹ | moisture |
| ZNW/ZNU/C1H/C3H | 1e-6 | 1e-5 | – | 1D coord near-exact |
| F / E | 1e-9 | 1e-8 | s⁻¹ | Coriolis |
| MAPFAC_* | 1e-5 | 1e-4 | – | metric passthrough |
| HGT | 0.5 | 5.0 | m | terrain (matches met_em HGT_M tol) |
| TSK / SST | 0.30 | 3.0 | K | skin/SST (protected) |
| TMN | 0.50 | 5.0 | K | deep soil |
| TSLB | 0.50 | 5.0 | K | soil temp (2→4 interp) |
| SMOIS | 0.03 | 0.30 | m³m⁻³ | soil moisture (2→4 interp) |
| wrfbdy U/V (coupled) | 2.0e4 | 2.0e5 | m s⁻¹·Pa | coupled = field·(mu+mub)/msf |
| wrfbdy T (coupled) | 1.0e4 | 1.0e5 | K·Pa | |
| wrfbdy PH (coupled) | 1.0e5 | 1.0e6 | m²s⁻²·Pa | |
| wrfbdy MU (2D) | 5.0 | 50.0 | Pa | |

**Masking policy:** soil/SST fields compared over their valid mask only (soil:
land cells; SST/skin-over-water: water cells); below-ground / fill-value points
excluded; categorical fields compared exactly on all points.

**PASS criteria, predeclared:**
* **S1 gate:** wrfinput ZNU/ZNW/C*/PB/MUB/PHB/P/PH/T/U/V/QVAPOR within tol for
  d01/d02/d03 across ≥10 cases.
* **S2 gate:** wrfinput TSK/SST/TMN/XLAND/MAPFAC_*/F/E/SINALPHA/COSALPHA/XLAT/
  XLONG/HGT + TSLB/SMOIS/ZS/DZS/ISLTYP/IVGTYP within tol, ≥10 cases, d01/d02/d03.
* **S3 gate:** wrfbdy coupled values + tendencies (U/V/T/PH/QV/MU) within tol for
  d01 across ≥10 cases.
* **S4 gate (closes the milestone):** the comparator runs all of the above as ONE
  campaign (≥10 cases, d01/d02/d03) AND a **24h native-init FORECAST gate** —
  feed the native wrfinput/wrfbdy into the existing GPU forecast pipeline (NO
  replay), run 24h, compare full-field to the CPU-WRF wrfout (reusing the
  `proofs/m20/continuous_gate.py` metric set: T2/U10/V10/PBLH/precip per lead) —
  AND conservation + restart gates still pass. The forecast gate is the only
  honest standalone proof (not a self-compare): native init → native forecast →
  vs CPU-WRF wrfout.

---

## 6. Parallelization plan (PARALLEL-BY-DEFAULT) + forced-serial points

Applying the skill's PARALLEL-BY-DEFAULT rule: decompose, then run every
file-disjoint / static-oracle-independent lane concurrently.

**S1 ∥ S2 ∥ S3 ∥ S4 can ALL start concurrently after this S0 freeze.** Each lane
develops against its STATIC oracle (the real.exe wrfinput/wrfbdy fixtures on disk
+ the frozen `MetEmArtifact` schema), NOT against a sibling lane's unmerged output:

* **S1** ⟂ S2 ⟂ S3: file-disjoint, validate each against its own wrfinput/wrfbdy
  oracle subset.
* **S3's apparent dependency on S1/S2 is NOT a real serialization.** S3 needs
  MU/MUB/MSF at each forcing time to couple — but during development it reads
  those from the per-interval oracle wrfinput (a static fixture), so it builds in
  parallel and only at INTEGRATION calls the real S1/S2 entry points (whose
  *interfaces* are frozen here). This is the skill's "develop against the static
  oracle, not the sibling's not-yet-merged output" pattern.
* **S4** builds the comparator + campaign runner against the frozen tol tables +
  the oracle files immediately; it does not need any lane implementation to write
  the harness.

**The ONLY forced-serial points (the justified serialization):**
1. **S5 integration** (manager): wire the four lanes in `driver.py`, pack into
   `State`/`BaseState`/`DycoreMetrics`/`BoundaryState`, resolve any
   single-source-terrain / precision edge. Serial because it touches the
   manager-owned shared core and consumes all four lanes' merged output.
2. **The 24h native-init FORECAST gate (GPU-bound).** ONE GPU job at a time
   (single GPU shared with the v0.3.0 S5 lane + the per-case pipeline). This
   serializes onto the GPU and runs in S5, not in the parallel S4 lane. S4's
   offline field-parity campaign (CPU, cores 0-3) is NOT GPU-bound and runs in
   parallel; only the forecast step waits for the GPU.
3. **v0.3.0 S5** (native met_em production) is a separate lineage; v0.4.0 can use
   the existing real-WPS met_em + the `wps_cases` met_em as the input oracle until
   v0.3.0's native met_em lands. So v0.4.0 does NOT serialize behind v0.3.0 S5 —
   they proceed in parallel, joining only at the final fully-native run.

**Recommended 4-wide dispatch (interchange Opus↔GPT, equal-frequency on the hard
parts; in-process background Opus agents + a tmux codex GPT lane):**

| Lane | Model | Why |
|---|---|---|
| **S1** vertical/base/hydrostatic/vinterp | **Opus 4.8 MAX** | hardest + most correctness-critical (hour-0 force balance) |
| **S2** surface/soil | **GPT-5.5 xhigh** | self-contained, oracle-clean; balances the token pool |
| **S3** LBC value+tendency | **Opus 4.8 MAX** | coupling/stagger subtlety, but smaller than S1 |
| **S4** comparator harness | **GPT-5.5 xhigh** | tooling + gate logic; runs alongside S1–S3 |

Cross-model debug rule applies at integration: an S1 (Opus) bug → GPT fixes; an
S2 (GPT) bug → Opus fixes.

---

## 7. Risks / unresolved

* **No analytic oracle** — the entire milestone leans on real.exe fixtures. The
  24h forecast gate is the only end-to-end falsifiable check; everything else is
  field-parity vs real.exe. Mitigation: tolerances frozen up front, masking
  predeclared, structural isolation so a bad lane can't silently leak.
* **hybrid_opt=2 coefficient exactness** — the Klemp polynomial branch + the
  `etac` value must match real.exe exactly or PB/PHB drift. `etac` is a namelist
  scalar; S0 leaves it in `RealInitConfig` — **S1 must read the actual `etac`
  from the case namelist / wrfinput global attr at impl** (the recon confirmed
  HYBRID_OPT=2 but did not pin etac). TRACKED for S1.
* **2→4 soil-layer interpolation** — `module_soil_pre` exact node depths for
  Noah-MP (sf_surface_physics=4); S2 confirms ZS/DZS against the wrfinput oracle.
* **moist/UM/theta-level branches** — real.exe has many `flag_ptheta/flag_prho`
  and AFWA/UM paths the Canary AIFS case does not hit; v0.4.0 supports the
  isobaric-metgrid path only and must loud-reject the others (P2-2 pattern).
* **Single GPU** — the forecast gate cannot run concurrently with the v0.3.0 S5
  GPU lane or the per-case pipeline; the manager must schedule one GPU job at a
  time.

---

## Git note

`worker/opus/v030-native-init` (the nominal base) pointed at the v0.2.0 release
commit `b301baa` and was MISSING the v0.3.0 init code (`metgrid_schema.py` et al,
which live on the `v020-validation-gate` / `worker/opus/v030-*` lineage). Per the
contract's stale-base rule, this branch was created from the true v0.3.0
native-init tip `worker/opus/v030-integration` @ `998f346` (has the frozen
metgrid schema + interp + writer). No hard-reset of any other branch. The plan
docs (`SPRINT-PLAN-V030-V040.md`, `MASTER-PLAN-POST-V020.md`) live on the
`v020-validation-gate` lineage and were read from the working tree.
