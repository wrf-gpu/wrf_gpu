You are GPT-5.5 xhigh doing an INDEPENDENT, ADVERSARIAL review for the wrf_gpu2 project (JAX-native GPU port of WRF v4 for Canary Islands forecasting). You earlier reviewed the dry dycore and caught a systemic fp32-defeat bug. The 4 physics lanes + coupling were then built + validated by Opus frontrunners vs real-WRF savepoints (single-step) and coupled into a stable real-case forecast with near-WRF +1h/+3h skill — but YOU have not independently reviewed the couplers. The principal explicitly wants your eyes on (A) the physics couplers and (B) the remaining HARDCORE TECHNICAL hurdles. Validation/publishing is trivial — SKIP it entirely; focus only on code bugs/inefficiencies and the technical closing plan. All code is on `manager-2026-05-23` (commit `17e0039`) — `git checkout manager-2026-05-23` first. READ-ONLY on code; only write your findings file. Cite file:line.

## PART A — physics-coupler bug + inefficiency review (adversarial)
Review for BUGS, WRF-faithfulness errors, and GPU INEFFICIENCIES (cite WRF source where relevant; pristine WRF at /home/enric/src/wrf_pristine/WRF, Gen2 WRF at /home/enric/src/canairy_meteo/Gen2/artifacts/wrf_gpu_src/WRF):
1. `src/gpuwrf/physics/thompson_*` + `mp_*` — Thompson microphysics. KNOWN: cross-species collection (racs/sacr/racg/gacr) is UNPORTED; validated only on a DRY/near-inactive oracle step (0 condensate). What bugs/risks in the ported processes (sedimentation, sat-adjust, ice nucleation, number concentrations)?
2. `src/gpuwrf/physics/surface_*` — WRF `sf_sfclayrev` port. KNOWN: the oracle used `sf_mynn` (sf_sfclay_physics=5) but B2 ported `sf_sfclayrev`; operational diagnostics (T2/U10/V10/HFX/LH) match but internal br/zol/qsfc differ. Is sfclayrev-vs-sf_mynn a real risk for skill, or fine?
3. `src/gpuwrf/physics/mynn_*` — MYNN PBL. KNOWN: C-grid wind reconstruction at non-periodic edges (was periodic `jnp.roll`, changed to zero-gradient wall extrapolation), seam with B4 boundaries. Bugs in the level-2/length/turbulence/predict + tendencies?
4. `src/gpuwrf/physics/rrtmg_*` — RRTMG SW/LW. KNOWN: a 180× radiation-cadence bug was just fixed (radiation rate must be held + applied every dynamics step over the radt interval, not once per cadence). VERIFY the fix is correct; check lookup-table compile/memory (HLO unroll risk).
5. `src/gpuwrf/coupling/physics_couplers.py` (adapter chain thompson→surface→mynn→rrtmg(cadence)→apply_lateral_boundaries→guards→precision) + `coupling/boundary_apply.py` (WRF relax/spec stencil). Coupling-order, double-application, decouple/recouple, or unit/stagger bugs?
6. The fp32-defeat bug class — x64-at-import (`gpuwrf/__init__.py`), `State.replace(_cast=...)` (`contracts/state.py`), scatter/scratch buffers allocated at a field dtype (`flux_advection.py`). Any REMAINING instances in the couplers/physics that silently downcast tendencies?
Output the top bugs/inefficiencies ranked, each with file:line + WRF ref + fix.

## PART B — remaining-technical-hurdles plan review (HARDCORE ONLY)
Critique the plan for closing the remaining TECHNICAL hurdles (NOT validation/publishing):
1. **Perf:** the coupled forecast is COMPILE-bound — a single fused `lax.scan` over the whole forecast has compile time + memory growing with length (+12h did not compile in 37min). Plan: SEGMENT the scan (compile one fixed-length segment, loop it) then fp32 downcast (plan at `proofs/perf/fp32_downcast_plan.md`: FP64-lock the acoustic small-step + implicit w/ph solve + EOS, fp32 the rest). Is this the right strategy? Risks (recompile per segment, equivalence, fp32 instability in the acoustic solve)? A better approach (AOT, donate, scan unroll control, custom_vjp)?
2. **Open-top w BC:** the WRF-faithful open-top w solve produces ~300 m/s spurious w at the model top (k=top) on real upper-level data; currently BYPASSED via `top_lid=True` (rigid lid + WRF w_damping/zdamp). Is rigid-lid defensible for a d02 real forecast, or must the open-top w BC be fixed for skill? What is the likely BUG in the open-top w solve (advance_w.py:345-367 vs WRF module_small_step_em.F:1421-1429)?
3. **V10 weak spot:** GPU V10 southerly under-developed (bias +1.6-1.75 m/s @ +1-3h) — most likely cause (surface/PBL momentum flux, V-component lateral boundary, or a dycore V issue)?
4. **Microphysics** validated only on a dry step (collection unported) — real risk for the Canary 3km regime, or low-priority?
State the HIGHEST-RISK remaining technical item and whether the plan is sound / what's missing.

## Output
Write to EXACTLY `/home/enric/src/wrf_gpu2/.agent/reviews/2026-05-30-gpt-coupler-and-plan-review.md`. Read-only on code. End with `GPT_COUPLER_PLAN_REVIEW_COMPLETE` + the TOP-3 must-fix technical items ranked.
