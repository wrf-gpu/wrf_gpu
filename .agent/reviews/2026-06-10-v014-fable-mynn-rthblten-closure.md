# Review: V0.14 Fable MYNN-EDMF RTHBLTEN Strict Step-1 Closure

Date: 2026-06-10 WEST · Owner: Fable/Mythos · Branch: `worker/gpt/v013-close-manager`
Base: `edaa4b1c` (sprint-open `1e4d50d9`). **UNCOMMITTED — manager reviews/gates/merges.**

Verdict: `STEP1_STRICT_RED_FORMALLY_BOUNDED_RRTMG_FIELD_DOMINANT_MYNN_KERNEL_FLOOR_GATE_UNREACHABLE`.

## Objective

Close the dominant strict Step-1 grid-parity blocker
(`NOAHMP_STEP1_STRICT_RED_..._NARROWED_TO_MYNN_EDMF_RTHBLTEN`, max 53.5 / rmse
2.54) as a whole endpoint: strict green, or a WRF-anchored formal bound narrower
than "MYNN-EDMF RTHBLTEN" with exact ownership.

## Outcome (what changed in the picture)

The contract's premise — "the residual is PBL/MYNN-EDMF RTHBLTEN dominated" — is
**correct only for the single worst CELL (the max), and REFUTED for the FIELD
(rmse/p99)**. The strict field residual is **RRTMG-radiation dominated**. The
strict gate itself is **unreachable** for the MYNN+RRTMG theta tendency without
bitwise scheme reproduction. No production change was warranted (see Decision).

All claims are regenerated by `proofs/v014/mynn_rthblten_step1_closure.py` on the
operational `_physics_step_forcing` path against the rmol-pinned one-run WRF truth.

### 1. Reconciliation (prior "MYNN kernel faithful" vs current strict red)

- The operational dry `T_TENDF` source leaf is exactly
  `theta_m_factor·mass_h·(RTHRATEN + RTHBLTEN) + Rv/Rd-coupled QV` (operational_mode.py:3172-3199).
  A hand-reassembly of this matches the runtime capture to **4.5e-13** — so the
  strict leaf is precisely the mass/theta_m-coupled MYNN+held-RTHRATEN source.
- The MYNN `RTHBLTEN` it carries is WRF-faithful to **raw ~3e-4 K/s** with
  WRF-equivalent inputs + WRF-pinned QKE — the prior accepted boundary result
  (`mynn_driver_source_output_fix`, raw rmse 2.6e-6) STANDS. The strict red is not
  a MYNN sign/unit/mass-scaling error.
- **Proof artifact found:** `step1_mynn_source_coupling.build_step1_state` calls
  `noahmp_surface_step` WITHOUT `grid=` (grid-less fallback), unlike the
  operational path which passes `grid=namelist.grid`. The grid-less LAND surface
  makes the *standalone* `run_kernel_matrix` adapter overshoot `RTHBLTEN` ~2x at
  land (mass-coupled +260). The OPERATIONAL leaf does NOT carry that overshoot
  (faithful there). The legacy kernel-matrix land tail is a **proof artifact**, not
  a production bug. (Recommend the manager add `grid=` to `build_step1_state`; left
  untouched here to respect the "prerequisite proof outputs only" file scope.)

### 2. Lane decomposition (operational strict residual vs WRF after_conv, interior)

| Configuration | max | rmse | p99 |
|---|---|---|---|
| Operational (cold-start QKE) | 53.52 | 2.5445 | 16.63 |
| + WRF-pinned INIT_QKE | 27.97 | 2.5378 | 16.63 |
| + WRF-pinned QKE **+ WRF RTHRATEN (RRTMG removed)** | 40.28 | **0.5433** | **0.843** |
| RTHRATEN (RRTMG) lane only | 19.78 | 2.527 | 16.26 |

- Injecting WRF QKE drops the worst-cell **max 53.5→28** but leaves **rmse/p99
  ~unchanged** → cold-start QKE is a **rare single-cell spike** (bulk QKE exact to
  0.07%: p50 ratio 1.0003; worst water column 3.5-4% low).
- Substituting WRF RTHRATEN collapses **rmse 2.54→0.54 (4.7×)** and **p99
  16.6→0.84 (20×)** → **RRTMG radiation is 95.4% of the strict rmse variance**.
- The MYNN level-2.5 kernel alone (WRF QKE + WRF RTHRATEN) leaves **rmse 0.54 /
  max 40** — an irreducible fp/algorithmic reimplementation floor.

### 3. Ranked lanes (exact ownership)

1. **RRTMG step-1 RTHRATEN (held seed) — FIELD DOMINANT.** Owner:
   `src/gpuwrf/physics/rrtmg_lw.py`, `rrtmg_sw.py` (held `carry.rthraten` /
   `_refresh_rthraten`). Localization: `proofs/v014/rrtmg_step1_forcing_parity.*`
   (clear-sky derived optical/gas/top-buffer profile). A RRTMG clear-sky sprint
   would cut strict rmse 2.54→~0.54.
2. **MYNN level-2.5 kernel RTHBLTEN faithfulness floor — worst-cell MAX.** Owner:
   `src/gpuwrf/physics/mynn_pbl.py` (`mym_turbulence`/`_mym_length_option1`). raw
   ~3e-4 K/s mass-coupled by μ~1e5 → rmse 0.54 / max 40. Irreducible without
   bitwise level-2.5 reproduction.
3. **MYNN cold-start QKE equilibrium outlier — single-cell SPIKE.** Owner:
   `src/gpuwrf/physics/mynn_pbl.py::mynn_coldstart_init_columns`. Seed + formula +
   inputs (flt=fltv=flq=0, pmz=phh=1, rmol=0, lmax=5) match WRF
   `module_bl_mynnedmf.F:1325-1432` line-by-line, and the bulk is exact → the 3.5%
   outlier is fp-convergence sensitivity at marginal-stability columns, not a
   transcription bug. Fixing it moves the max 53.5→13.1 at that cell but not the
   field, and risks regressing validated MYNN cold-start.

## Files changed

- `proofs/v014/mynn_rthblten_step1_closure.py` (NEW) + `.json`/`.md` — authoritative
  operational-path reconciliation + lane decomposition.
- `proofs/v014/noahmp_step1_closure.py` — added `mynn_rthblten_lane_decomposition`
  cross-reference field + markdown section; corrected `fastest_next_command` (the
  prior text wrongly called MYNN "DOMINANT ... NOT radiation"). Re-run; gate numbers
  unchanged (53.5 / 2.54).
- `.agent/reviews/2026-06-10-v014-fable-mynn-rthblten-closure.md` (this file).
- **No production code changed.**

## Commands run

```
python -m py_compile src/gpuwrf/physics/mynn_pbl.py src/gpuwrf/physics/mynn_edmf.py \
  src/gpuwrf/coupling/physics_couplers.py src/gpuwrf/runtime/operational_mode.py \
  proofs/v014/noahmp_step1_closure.py proofs/v014/mynn_rthblten_step1_closure.py   # OK
git diff --check                                                                    # OK
JAX_PLATFORMS=cpu CUDA_VISIBLE_DEVICES= JAX_ENABLE_COMPILATION_CACHE=false PYTHONPATH=src \
  python proofs/v014/mynn_rthblten_step1_closure.py                                 # PROOF_EXECUTED
JAX_PLATFORMS=cpu CUDA_VISIBLE_DEVICES= JAX_ENABLE_COMPILATION_CACHE=false PYTHONPATH=src \
  python proofs/v014/noahmp_step1_closure.py                                        # re-run gate
python -m json.tool proofs/v014/{mynn_rthblten_step1_closure,noahmp_step1_closure}.json  # JSON valid
```
No production code changed, so the focused MYNN pytest gate was not required; the
proof is CPU-only (no GPU used).

## Proof objects

- `proofs/v014/mynn_rthblten_step1_closure.{json,md}` — reconciliation
  (reassembly↔runtime 4.5e-13), 4-row lane table, QKE faithfulness profile,
  worst-cell contributions, ranked lanes, gate-unreachable finding.
- `proofs/v014/noahmp_step1_closure.{json,md}` — strict gate (max 53.523, rmse
  2.5445, p99 16.63) + authoritative cross-reference.

## Unresolved risks

- The strict 1e-3/1e-5 mass-coupled gate is **mis-specified** for the MYNN+RRTMG
  theta tendency: raw ~3e-4 K/s × μ~1e5 ⇒ a ~rmse-0.5 / max-40 floor. Keeping the
  gate as-is permanently blocks Step-1 closure regardless of physics quality.
- A RRTMG clear-sky RTHRATEN fix (field-dominant lane) and the MYNN cold-start QKE
  outlier are real but each (a) leaves the gate red and (b) risk regressing
  validated MYNN/RRTMG; neither is a proven Step-1 production bug to fix here.

## Next decision needed (manager)

Re-specify the strict MYNN+RRTMG Step-1 gate to an operationally-meaningful
mass-coupled tolerance (achievable best ≈ rmse 0.5 / max 40), OR accept this
formal bound. If pursuing rmse reduction, the highest-yield single sprint is the
**RRTMG clear-sky RTHRATEN lane** (`rrtmg_step1_forcing_parity`), which alone cuts
strict rmse 2.54→0.54.

## TOST / Switzerland-GPU status

Still **BLOCKED** on the strict Step-1 gate. The field divergence is now fully
attributed and bounded (RRTMG-dominant rmse 2.54; MYNN-kernel max 40; QKE-outlier
spike), but not green. Resume after the gate is re-specified or the RRTMG lane is
closed. No GPU was used; no GPU runs requested.

## Is strict Step-1 green, bounded, or release-blocking?

**Formally bounded and release-blocking under the current gate**, with the gate
proven unreachable for non-bitwise MYNN+RRTMG. Strict max 53.523 / rmse 2.5445 /
p99 16.63 vs gate 1e-3 / 1e-5.
