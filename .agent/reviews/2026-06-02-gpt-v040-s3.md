# v0.4.0 S3 GPT Handoff — lateral BC value+tendency

## objective

Implement native real.exe-equivalent d01 lateral boundary value and tendency generation in
`src/gpuwrf/init/real_init/lateral_bc.py`, matching WRF `real_em.F::assemble_output`
and `module_big_step_utilities_em.F::couple` for U/V/T/PH/QV/MU.

## files changed

- `src/gpuwrf/init/real_init/lateral_bc.py` — implemented generation, hybrid mass coupling, WRF boundary stuffing order, interval tendencies, optional hydrometeor coupling, and initialized-snapshot support for proof isolation.
- `tests/init/real_init/test_s3_lateral_bc.py` — focused CPU tests for hybrid coupling, THM/T boundary handling, WRF side ordering, interval stacking, tendencies, and optional moisture fields.
- `proofs/v040/s3_wrfbdy_report.json` — 10-case oracle proof against retained real.exe `wrfbdy_d01` files.
- This handoff is mirrored at `/home/enric/src/wrf_gpu2/.agent/reviews/2026-06-02-gpt-v040-s3.md`.

## commands run

- `JAX_PLATFORM_NAME=cpu taskset -c 0-3 pytest -q tests/init/real_init/test_s3_lateral_bc.py`
- `JAX_PLATFORM_NAME=cpu taskset -c 0-3 python -m py_compile src/gpuwrf/init/real_init/lateral_bc.py tests/init/real_init/test_s3_lateral_bc.py`
- `taskset -c 0-3 python - <<'PY' ... proof JSON sanity check ... PY`
- `JAX_PLATFORM_NAME=cpu taskset -c 0-3 python - <<'PY' ... 10-case proof generator ... PY`
  - The proof generator ran each scratch `real.exe` subprocess as `taskset -c 0-3 ./real.exe`.
  - Scratch real.exe regenerated `all_ic_times` endpoint `wrfinput_d01*` fixtures from WPS `met_em` inputs; retained `wrfbdy_d01` files remained the comparison oracle.

## proof objects produced

- `proofs/v040/s3_wrfbdy_report.json`
  - Status: PASS
  - Cases: 10
  - Gated fields: U, V, T, PH, QVAPOR, MU
  - Aggregate worst value errors:
    - U max_abs 0.4685, RMSE 0.0778 vs tol 2.0e5 / 2.0e4
    - V max_abs 0.4729, RMSE 0.0422 vs tol 2.0e5 / 2.0e4
    - T max_abs 1.0, RMSE 0.1933 vs tol 1.0e5 / 1.0e4
    - PH max_abs 62.31, RMSE 14.15 vs tol 1.0e6 / 1.0e5
    - QVAPOR max_abs 1.78e-4, RMSE 1.82e-5 vs tol 100 / 10
    - MU max_abs 0.0, RMSE 0.0 vs tol 50 / 5
  - All tendency fields pass value-tolerance/21600 s scaled tolerances.

## unresolved risks

- S3 proof isolates lateral_bc using real.exe endpoint `wrfinput` fixtures because S1/S2 native lanes are separate and still stubbed in this worktree.
- `RealInitConfig` has no frozen `use_theta_m` switch. S3 follows Canary/WRF default moist-theta `THM` for `T_B*`; a future `use_theta_m=0` case needs a manager-owned type/config update.
- Hydrometeors beyond QVAPOR are coupled when provided on snapshots, but v0.4.0 S0 froze no `WRFBDY_TOLS` entries for those fields.

## next decision needed

Manager should decide whether to add a frozen `use_theta_m` scalar to `RealInitConfig` during S5 integration, or explicitly keep WRF default `use_theta_m=1` as the only v0.4.0-supported path.
