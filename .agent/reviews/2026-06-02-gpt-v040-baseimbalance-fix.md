# GPT v0.4.0 Base-Imbalance Fix Handoff

## objective

Cross-model fix for the v0.4.0 S6 native-init forecast smoke hour-0 pressure/wind residual. The requested discrete base-state inversion was implemented and tested on the same 1-case GPU smoke. Result: PSFC bias collapsed into margin, but foundation is still **not confirmed** because V10 and T2 remain outside core margins.

## files changed

- `proofs/v040/s5_forecast_gate_exec.py`
  - Native forecast BaseState now reuses `_wrf_base_theta_from_loaded_state(pb, phb, mub, metrics)` instead of direct `t0 + native t_init`.
  - The inverted BaseState is passed into `OperationalNamelist.from_grid(...)`.
- `src/gpuwrf/runtime/operational_mode.py`
  - Added optional resident `base_state` to `OperationalNamelist`.
  - Threaded optional BaseState into small-step/acoustic pressure refresh paths.
  - Fixed `PSFC` diagnostic: WRF `PSFC` is dry column mass + `p_top` + vapor column mass, not bottom mass-level pressure.
- `src/gpuwrf/dynamics/core/small_step_prep.py`
  - Added optional `base_state` argument and uses it for `diagnose_pressure_al_alt` when supplied.
- `proofs/v040/s5_forecast_gate_report.json`
  - Regenerated by the final GPU smoke.

## commands run

- `sed` reads of `PROJECT_CONSTITUTION.md`, `AGENTS.md`, `.agent/skills/managing-sprints/SKILL.md`, S6 handoff, S6 report, S6 executor, and `src/gpuwrf/integration/d02_replay.py`.
- `python -m py_compile proofs/v040/s5_forecast_gate_exec.py`
- `nvidia-smi`
- GPU smoke after base inversion only:
  - `taskset -c 0-3 env PYTHONPATH=src:proofs/v040:. python proofs/v040/run_forecast_gate_smoke.py --hours 6 --max-cases 1 --dt-s 60 --acoustic-substeps 4 --radiation-cadence-steps 30 --out proofs/v040/s5_forecast_gate_report.json`
  - Result: PSFC/V10 unchanged, `foundation_confirmed=False`.
- CPU diagnostic: direct `t0+t_init` vs `_wrf_base_theta_from_loaded_state`
  - direct theta differed from inverted by up to 0.491 K.
  - direct dPHB reconstruction residual max 5.23 m2/s2.
  - inverted dPHB reconstruction residual round-off (~1.8e-12).
- GPU smoke after threading BaseState into operational mode:
  - Result: PSFC/V10 still unchanged to sub-Pa/sub-mm/s, `foundation_confirmed=False`.
- CPU diagnostic: native bottom `PB+P` vs WRF t0 `PSFC`
  - native bottom `PB+P` matched WRF bottom `PB+P` to 0.027 Pa RMSE.
  - bottom `PB+P` was ~298 Pa below WRF `PSFC`, proving the report was using the wrong PSFC diagnostic.
  - `mu_total + p_top + sum(-dnw * dry_layer_mass * qv)` reproduced WRF `PSFC` to 4.49 Pa RMSE at t0.
- Final GPU smoke:
  - `taskset -c 0-3 env PYTHONPATH=src:proofs/v040:. python proofs/v040/run_forecast_gate_smoke.py --hours 6 --max-cases 1 --dt-s 60 --acoustic-substeps 4 --radiation-cadence-steps 30 --out proofs/v040/s5_forecast_gate_report.json`
  - Exit code 2 because `foundation_confirmed=False`.

## proof objects produced

- `proofs/v040/s5_forecast_gate_report.json`
  - `verdict`: `STABLE_BUT_CORE_FIELD_MISMATCH`
  - `foundation_confirmed`: `false`
  - stable finite: `true`
  - physical: `true`
  - PSFC: `worst_abs_bias=13.9465 Pa`, margin `50 Pa`, `within_margin=true`
  - V10: `worst_abs_bias=2.3025 m/s`, margin `0.2752`, `within_margin=false`
  - T2: `worst_abs_bias=0.2540 K`, margin `0.2149`, `within_margin=false`
  - U10/Q2/PBLH within margins.

## unresolved risks

- The original S6 root-cause hypothesis was only partially correct. The discrete inversion is valid and now wired, but it did **not** move the forecast residual materially.
- The large apparent PSFC offset was primarily a diagnostic bug: GPU `PSFC` was bottom model-level pressure, not WRF surface pressure. That is fixed and PSFC is now inside the continuous-gate bias margin.
- V10 remains a real core mismatch at about `+2.30 m/s`; T2 remains borderline outside margin. Foundation must stay false.
- The V10 residual signature is steady from hour 1 and now needs a wind/momentum/LBC/surface-layer localization pass, not more base-theta work.

## next decision needed

Accept this as a partial fix/escalation proof, then dispatch a focused V10/T2 residual sprint. Do not close v0.4.0 foundation on this report: `foundation_confirmed=false`.
