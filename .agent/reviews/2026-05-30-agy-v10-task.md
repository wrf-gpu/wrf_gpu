You are Gemini (agy) doing a focused, READ-ONLY surface-physics analysis for the wrf_gpu2 project (JAX-native GPU port of WRF v4 for Canary Islands forecasting). You are NOT editing code — you write ONE findings file. Cite file:line.

## THE PROBLEM
The GPU coupled real-case forecast reproduces CPU-WRF surface fields well at +1-3h EXCEPT V10 (10 m meridional/southerly wind), which is under-developed: bias +1.6 to +1.75 m/s, RMSE ~3.7 (vs T2 RMSE ~1.3 K, U10 ~2.2). GPT-5.5's review hypothesizes the most likely cause is a **surface-layer scheme mismatch**: the CPU-WRF comparator (the Canairy corpus) ran `sf_sfclay_physics=5` (the MYNN surface layer, `module_sf_mynn.F`), but our port implemented `sf_sfclayrev` (`module_sf_sfclayrev.F`). Operational T2/U10/V10/HFX/LH match closely, but internal br/zol/qsfc/u* differ, and V10 is the visible symptom.

## YOUR TASK — compare the two WRF surface schemes' MOMENTUM path
Pristine WRF source: /home/enric/src/wrf_pristine/WRF (look under phys/). Our port: /home/enric/src/wrf_gpu2/src/gpuwrf/physics/surface_layer.py (and surface_*).

Compare, with file:line citations from the WRF Fortran, the formulation differences that would specifically make the GPU **V10 (and meridional momentum flux) weaker** between `module_sf_sfclayrev.F` (what we ported) and `module_sf_mynn.F` surface-layer routine (what the comparator used):
1. The friction velocity u* / ustar computation and any warm-start/limiting.
2. The stability functions psim/psih and the z/L (zol) range/clipping — do the two schemes differ in the unstable/stable similarity functions in a way that changes momentum drag?
3. The 10 m wind diagnostic: how each scheme reconstructs U10/V10 from the lowest model level (the similarity ratio, the roughness/psim term). Is V10 = f(v_lowest, ratio) computed the same way?
4. The surface momentum flux / tau (tau_u, tau_v) and exchange coefficient (Cd / cm) — would sf_mynn produce a different (likely larger) southerly drag or 10m reconstruction?

## DELIVERABLE
- Identify the SPECIFIC formula differences (with WRF file:line for both schemes) that plausibly explain a +1.6 m/s V10 under-development.
- Recommend ONE of: (a) port the sf_mynn surface-layer momentum path to match the comparator (scope it — which routines/lines), or (b) sfclayrev can be reconciled by a specific correction (name it), or (c) V10 gap is NOT primarily the surface scheme (and what else to check — V lateral boundary, dycore V).
- Be decisive and rank by likelihood.

## OUTPUT
Write your findings to EXACTLY `/home/enric/src/wrf_gpu2/.agent/reviews/2026-05-30-agy-v10-findings.md`. Read-only on all code. End with `AGY_V10_ANALYSIS_COMPLETE` + your single top recommendation.
