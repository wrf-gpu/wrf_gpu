# GPT WSM3/WSM5 Handoff - 2026-06-03

## objective

Port the single-moment WSM3 (`mp_physics=3`) and WSM5 (`mp_physics=4`) gaps using the existing WSM6 JAX port and oracle pattern, with WRF-faithful pristine-WRF savepoint parity proof and S0 menu registration.

## files changed

- Added JAX ports: `src/gpuwrf/physics/microphysics_wsm3.py`, `src/gpuwrf/physics/microphysics_wsm5.py`.
- Added pristine-WRF oracle generation: `proofs/v060/oracle/build_wsm_sm_oracles.sh`, `wsm3_oracle_driver.f90`, `wsm5_oracle_driver.f90`, `wsm_sm_dump_to_json.py`.
- Added oracle savepoints: `proofs/v060/savepoints*/wsm{3,5}_case_*.json` and `wsm_sm_source_checksums.txt`.
- Added parity proof runner and reports: `proofs/v060/run_wsm_sm_parity.py`, `proofs/v060/wsm3_savepoint_parity_report.json`, `proofs/v060/wsm5_savepoint_parity_report.json`.
- Added tests: `tests/test_wsm_sm_savepoint_parity.py`; updated contract/namelist tests.
- Wired `mp=3,4`: physics registry/interface contracts, dispatch, scan adapters, namelist checks, operational scan menu, and integration-report lane inventory.
- Documented S0 frozen-contract extension in `.agent/decisions/V0.6.0-S0-PLAN.md` and `.agent/decisions/V0.6.0-S0-FROZEN-CONTRACT.md`.

## commands run

- `taskset -c 0-3 bash proofs/v060/oracle/build_wsm_sm_oracles.sh fp32 && taskset -c 0-3 bash proofs/v060/oracle/build_wsm_sm_oracles.sh fp64`
- `PYTHONPATH=src JAX_ENABLE_X64=1 JAX_PLATFORM_NAME=cpu JAX_PLATFORMS=cpu taskset -c 0-3 python3 proofs/v060/run_wsm_sm_parity.py`
- `PYTHONPATH=src JAX_ENABLE_X64=1 JAX_PLATFORM_NAME=cpu JAX_PLATFORMS=cpu taskset -c 0-3 python3 -m pytest tests/test_wsm_sm_savepoint_parity.py tests/contracts/test_v060_physics_interfaces.py tests/test_namelist_check.py tests/test_v060_physics_dispatch.py -q`
- `PYTHONPATH=src taskset -c 0-3 python3 -m gpuwrf.contracts.physics_registry`
- `PYTHONPATH=src JAX_ENABLE_X64=1 JAX_PLATFORM_NAME=cpu JAX_PLATFORMS=cpu taskset -c 0-3 python3 -m gpuwrf.contracts.physics_interfaces`
- `taskset -c 0-3 python3 -m py_compile src/gpuwrf/physics/microphysics_wsm3.py src/gpuwrf/physics/microphysics_wsm5.py proofs/v060/run_wsm_sm_parity.py tests/test_wsm_sm_savepoint_parity.py`

Notes: one initial `run_wsm_sm_parity.py` invocation failed before comparison because of an argparse default bug; fixed before generating reports. One interface self-check was accidentally run without CPU-only JAX environment and emitted CUDA OOM allocator warnings before passing; rerun CPU-only command above is the accepted gate.

## proof objects produced

- `proofs/v060/wsm3_savepoint_parity_report.json`: `overall_pass=true`; worst absolute field error `5.698041150026256e-09` K on case 5 temperature. All explicit `qv/qc/qr` tendency gates pass.
- `proofs/v060/wsm5_savepoint_parity_report.json`: `overall_pass=true`; worst absolute field error `7.719336281297728e-09` K on case 4 temperature. All explicit `qv/qc/qr/qi/qs` tendency gates pass.
- Both reports identify oracle source as UNMODIFIED pristine WRF at `/home/enric/src/wrf_pristine/WRF`, include fp32/fp64 source SHA256 checksums, and set `no_self_compare=true`.

## unresolved risks

- This is isolated savepoint parity for short single-column cases, not an integrated forecast parity run.
- GPU performance/profiler proof was not part of this sprint and was not produced.
- Previously generated readiness/smoke JSON reports still contain the old MP menu text; they were not hand-edited.

## next decision needed

Manager decision: accept this WSM3/WSM5 frozen-contract extension and either regenerate higher-level integration/readiness reports on trunk or defer those generated report refreshes to the next integration pass.
