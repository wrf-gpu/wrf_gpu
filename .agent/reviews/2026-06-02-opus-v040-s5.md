# v0.4.0 S5 handoff — manager integration of S1–S4 + native-init parity gate

**Author:** Opus-MAX integrator. **Date:** 2026-06-02.
**Branch:** `worker/opus/v040-integration` (worktree
`.claude/worktrees/v040-integration-opus`, base `bb728bc` = S0 freeze).
**Resource policy honored:** cores 0-3 (`taskset -c 0-3`), `JAX_PLATFORM_NAME=cpu`,
NO GPU.

## Objective

Assemble the four file-disjoint native-real-init lanes (S1 dynamics, S2
surface/soil, S3 LBC, S4 comparator) into a working pipeline via the
manager-owned `driver.build_real_init`, apply the manager-approved `types.py`
carry-batch, and run the ≥10-case wrfinput/wrfbdy field-parity gate against the
real.exe references under the frozen tolerances.

## HONEST VERDICT

**The assembled native init PASSES the parity gate on every gated field except a
single documented LSM-init categorical cell.** Across 12 cases × {d01,d02,d03}:

- **wrfinput dynamics + base-state + 1D-coord + surface + soil-numeric: ALL PASS**
  with 2–4 orders of margin (e.g. `T` worst rmse 6.8e-5 vs tol 0.1; `U`/`V`
  ~1.6e-5 vs 0.15; `P` 0.027 vs 20; `PH` 0.033 vs 1.0; `PHB` 0.046 vs 1.0;
  `MU`/`MUB`/`PB` ≤0.031 vs 5.0; `QVAPOR` 1.6e-8 vs 1e-4; `TSLB` 1.2e-5 vs 0.5;
  `SMOIS` 4.9e-9 vs 0.03; `C1F`/`C1H` at the recalibrated noise-floor tol).
- **wrfbdy (d01): ALL 12 fields PASS** — coupled VALUE + TENDENCY for
  U/V/T/PH/QV/MU on all 12 d01 cases (e.g. `T_value` worst rmse 6.8 vs tol 1e4;
  `PH_value` 4054 vs 1e5; `MU_value` 0.048 vs 5.0; tendencies ~4 orders inside).
- **ONLY FAILURE:** `ISLTYP`/`IVGTYP` exact-on-all-points miss on **ONE static
  d01 cell `(j=22,i=86)`**, identical in all 12 d01 cases; d02/d03 are
  categorically exact. This is the documented S2 residual: the oracle wrfinput is
  a post-`start_em` snapshot, and that cell (met `LU_INDEX=17`/iswater but
  `XLAND=1`/land) carries a NOAHMP_INIT inland-water reclassification (ivgtyp
  21→5, isltyp 6→8) that the real.exe-faithful pre-LSM-init dominant-category
  path cannot derive. It is a downstream-LSM-init artifact, NOT an S2
  equivalence defect; it is **surfaced, not masked** (the comparator keeps
  exact-on-all categorical scoring; the report lists the cell + per-case delta).

`campaign_pass_raw=False` (the exact-categorical gate flags the 1 cell);
`dynamics_surface_soilnumeric_wrfbdy_pass=True`;
`only_failure_is_documented_categorical_residual=True`. The proof script exits 0
on that honest state and nonzero on any OTHER (real-regression) failure.

## What was done

### Step 1 — merged the 4 file-disjoint lanes (all CLEAN, no conflicts)

- `worker/gpt/v040-s1` (fd200d0) → vertical_coord/base_state/vinterp/hydrostatic + C1 fix
- `worker/opus/v040-s2` (24d786b) → surface_init/soil_init
- `worker/gpt/v040-s3` (d0be7cf) → lateral_bc
- `worker/opus/v040-s4` (5f560ea) → comparator

Confirmed disjoint: `types.py`/`driver.py`/`__init__.py` were untouched by all
four merges (verified `git diff bb728bc..merged` on those = empty).

### Step 2 — applied the manager-approved `types.py` carry-batch (recorded with a
dated "manager-approved 2026-06-02" note in `.agent/decisions/V0.4.0-S0-PLAN.md`;
`REAL_INIT_TYPES_VERSION` bumped `0.4.0-S0-frozen-…` → `0.4.0-S5-carrybatch-…`):

- **2a** `C1F`/`C1H` tol → `(rmse 1e-5, max_abs 5e-5)` (those 2 rows only).
  Justification: the oracle C1F/C1H ARE WRF's fp32 (`RWORDSIZE=4`)
  finite-difference of stored fp32 C3 (proven bit-exact, max_abs=0.0); our fp64
  C1 is strictly more accurate; downstream ~3 Pa with PB/PHB/P/PH/MU passing 3+
  orders inside tol. Recalibration to the fp32-oracle noise floor, not a
  loosening. (Now PASS: C1F 3.0e-5, C1H 3.1e-5 max_abs vs 5e-5 cap.)
- **2b** dropped the unused `ALT` key from `WRFINPUT_TOLS` (real.exe writes
  AL/ALB/T_INIT/P_HYD, not ALT). Comparator iterates over tols keys, so ALT is
  simply no longer scored (AL is checked).
- **2c** added `W` + hydrometeor (`QCLOUD`/`QRAIN`/`QICE`/`QSNOW`/`QGRAUP`)
  `WRFBDY_TOLS` keys, coupled-scaled (W at U/V scale; hydrometeors at QV scale).
- **2d** added a frozen `use_theta_m` switch to `RealInitConfig` (default `1` =
  moist-theta), wired into `lateral_bc._snapshot_from_parts` (1→moist THM,
  0→dry theta); default reproduces S3's existing behavior exactly.
- **2e** added an additive-optional `tmn_soil_endpoint` field to `SurfaceInit`;
  `surface_init` populates it; `soil_init` now consumes it (with
  `surface.tsk`/`landmask`/`hgt` + met_em `SOILHGT`) instead of re-running the
  whole surface lane internally — eliminates the doubled surface compute. Falls
  back to the internal re-run when the field is absent (hand-built SurfaceInit).

### Step 3 — driver

`driver.build_real_init` was already wired to the correct lane call order
(S1 vcoord → S2 surface → S1 base(surface.hgt) → S2 soil(surface) → S1
vinterp+hydrostatic → S3 lbc) and the single-source-terrain rule (one `hgt`
from met_em HGT_M passed to both S1 and S2). Verified end-to-end; no change
needed beyond the carry-batch wiring it consumes.

### Step 4 — the parity gate

`proofs/v040/s5_native_init_parity.py` builds a `product_factory(case,domain)`
that loads the matching `wps_cases` met_em → `MetEmArtifact`, builds the config
from the oracle wrfinput global attrs (etac/p_top/hybrid/base-state constants —
identical to the S1 proof), supplies the d01 forcing sequence (init + next
frame) for the LBC, and calls `driver.build_real_init`; then runs
`comparator.run_campaign` over 12 LBC-able cases (d01/d02/d03 + wrfbdy_d01).
Emits `proofs/v040/s5_native_init_parity_report.json` (per-field PASS/FAIL, the
applied tol-batch, the categorical-residual audit, the verdict breakdown).

**One real integration bug found + fixed (cross-model: S4 is Opus-authored):**
the comparator's `_score_bdy_quantity` sliced the oracle wrfbdy to its first
Time frame (`var[0]`) but compared it against the full native `LateralBC` side
array, which carries a leading `(n_intervals,)` axis → every wrfbdy field
`SHAPE_MISMATCH`. Fix: when the native side array has exactly one extra leading
axis, score its first interval (`nat[0]`) to align with the comparator's own
"score the first frame" contract. After the fix all wrfbdy fields pass with ~4
orders of margin. Not a tol change, not masking — a harness slicing alignment.

Conservation + restart harnesses re-run: **9 passed** (no regression). All
merged lane tests re-run after the carry-batch + comparator fix: **36 passed**
(S1 6, S2 22, S3 6, S4 9, minus overlaps; 36 in the real_init+s1 selection).

### Step 5 — 24h forecast gate: READY, NOT RUN (GPU-bound, MANAGER-SCHEDULED)

`proofs/v040/s5_forecast_gate_ready.py` regenerates the forecast-gate spec wired
to the integrated factory and verifies readiness (NON-GPU): **6 cases** have the
oracle wrfinput/wrfbdy + matching wps met_em + a retained CPU-WRF
`wrfout_d01_<init>` (the 24h reference); the backfill root
`/mnt/data/canairy_meteo/runs/wrf_l2_backfill_output` exists; `continuous_gate.py`
present. Artifact: `proofs/v040/s5_forecast_gate_spec.json`.
`run_forecast_gate(execute=True)` remains a guarded NotImplementedError (S4's
design — the GPU forecast-pipeline body is the single-GPU serialization point).
**No GPU forecast was run** (the GPU is shared with the v0.3.0 parity campaign +
backfill replays — ONE job at a time). Manager invocation when the GPU is free:

    from gpuwrf.init.real_init import comparator as C
    from proofs.v040.s5_native_init_parity import make_factory
    C.run_forecast_gate(make_factory(lbc_intervals=1), execute=True,
                        out_path="proofs/v040/s5_forecast_gate_report.json")

## Files changed

- merged lane modules (S1/S2/S3/S4 production + tests + proofs)
- `src/gpuwrf/init/real_init/types.py` — carry-batch 2a–2e + version bump
- `src/gpuwrf/init/real_init/surface_init.py` — populate `tmn_soil_endpoint`
- `src/gpuwrf/init/real_init/soil_init.py` — consume `tmn_soil_endpoint` (2e)
- `src/gpuwrf/init/real_init/lateral_bc.py` — wire `use_theta_m` (2d)
- `src/gpuwrf/init/real_init/comparator.py` — wrfbdy native-interval slicing fix
- `.agent/decisions/V0.4.0-S0-PLAN.md` — manager-approved 2026-06-02 note
- `proofs/v040/s5_native_init_parity.py` (+ `…_report.json`) — the parity gate
- `proofs/v040/s5_forecast_gate_ready.py` (+ `…_forecast_gate_spec.json`)

## Commands run

```
git merge --no-ff worker/gpt/v040-s1 worker/opus/v040-s2 worker/gpt/v040-s3 worker/opus/v040-s4
taskset -c 0-3 env JAX_PLATFORM_NAME=cpu PYTHONPATH=src python -m pytest \
  tests/init/test_s1_real_init.py tests/init/test_s1_c1_coeffs.py tests/init/real_init/ -q   # 36 passed
taskset -c 0-3 env JAX_PLATFORM_NAME=cpu PYTHONPATH=src python -m pytest \
  tests/test_conservation_budget.py tests/test_m7_restart_checkpoint_roundtrip.py \
  tests/test_p0_5_restart_full_carry.py -q                                                   # 9 passed
taskset -c 0-3 env JAX_PLATFORM_NAME=cpu PYTHONPATH=src python \
  proofs/v040/s5_native_init_parity.py --out proofs/v040/s5_native_init_parity_report.json    # exit 0 (honest)
taskset -c 0-3 env JAX_PLATFORM_NAME=cpu PYTHONPATH=src python \
  proofs/v040/s5_forecast_gate_ready.py --out proofs/v040/s5_forecast_gate_spec.json          # n_ready_cases=6
```

## Proof objects

- `proofs/v040/s5_native_init_parity_report.json` — 12-case wrfinput/wrfbdy
  parity verdict; per-field PASS/FAIL; applied tol-batch; categorical-residual
  audit (single static cell `(22,86)`); verdict breakdown.
- `proofs/v040/s5_forecast_gate_spec.json` — forecast-gate readiness plan.

## Unresolved risks / what's left for the GPU forecast gate

1. **The 24h native-init FORECAST gate has NOT run** (GPU-bound,
   manager-scheduled). It is the only end-to-end falsifiable standalone proof
   (native init → native forecast → vs CPU-WRF wrfout). It is wired + ready (6
   cases, references present). **Next decision: schedule the single GPU job.**
2. **The 1-cell d01 ISLTYP/IVGTYP residual** is a `start_em`-snapshot LSM-init
   artifact, not an S2 defect, and is confined to one static cell with d02/d03
   exact. Manager call: keep the exact-on-all categorical gate (do NOT weaken
   it); track the cell. The forecast gate will show whether this single cell has
   any measurable T2/wind impact (expected: none — it is one interior land cell).
3. **wrfbdy multi-interval scoring:** the gate scores the FIRST forcing interval
   (t0 value + first-interval tendency), matching the comparator contract. Full
   multi-interval wrfbdy scoring is a tracked future option (S4 noted it).

## Next decision needed

Manager: schedule the single-GPU 24h native-init forecast gate
(`run_forecast_gate(execute=True)`) when the GPU is free, and confirm the
field-parity verdict above (dynamics/surface/soil-numeric/wrfbdy all PASS; one
documented d01 categorical cell) is acceptable to close the offline half of
v0.4.0 S5.
