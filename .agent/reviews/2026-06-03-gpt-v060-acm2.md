# GPT v0.6.0 ACM2 PBL Handoff

## objective

Port WRF ACM2 PBL (`bl_pbl_physics=7`) to the frozen v0.6.0 physics tendency
interface, gated against real WRF-module savepoints across convective, stable,
marine-stable, neutral, and transition regimes.

## files changed

- `src/gpuwrf/physics/pbl_acm2.py`
- `tests/test_v060_pbl_acm2.py`
- `proofs/v060/oracle/.gitignore`
- `proofs/v060/oracle/build_and_run.sh`
- `proofs/v060/oracle/dump_to_json.py`
- `proofs/v060/oracle/acm2_oracle_driver.f90`
- `proofs/v060/savepoints/acm2_case_1.json` through `acm2_case_6.json`
- `proofs/v060/savepoints/acm2_wrf_source_checksums.txt`
- `proofs/v060/acm2_savepoint_parity_report.json`
- `.agent/reviews/2026-06-03-gpt-v060-acm2.md`

## commands run

- `taskset -c 0-3 bash proofs/v060/oracle/build_and_run.sh`
- `taskset -c 0-3 env JAX_PLATFORM_NAME=cpu JAX_PLATFORMS=cpu pytest -q tests/test_v060_pbl_acm2.py`
- `taskset -c 0-3 env JAX_PLATFORM_NAME=cpu JAX_PLATFORMS=cpu pytest -q tests/contracts/test_v060_physics_interfaces.py`
- `taskset -c 0-3 env PYTHONPATH=src JAX_PLATFORM_NAME=cpu JAX_PLATFORMS=cpu python -m gpuwrf.contracts.physics_interfaces`
- `taskset -c 0-3 env PYTHONPATH=src JAX_PLATFORM_NAME=cpu JAX_PLATFORMS=cpu python -m gpuwrf.contracts.physics_registry`
- `taskset -c 0-3 env PYTHONPATH=src python -m py_compile src/gpuwrf/physics/pbl_acm2.py tests/test_v060_pbl_acm2.py`

Note: direct `python -m gpuwrf.contracts.*` without `PYTHONPATH=src` failed with
`ModuleNotFoundError`; rerun with `PYTHONPATH=src` passed.

## proof objects produced

- `proofs/v060/acm2_savepoint_parity_report.json`: verdict `PASS`.
- `proofs/v060/savepoints/acm2_case_*.json`: six WRF-module gold columns.
- `proofs/v060/savepoints/acm2_wrf_source_checksums.txt`: SHA-256 for WRF
  source used by the oracle.

Oracle provenance: WRF v4.7.1 has ACM2 in
`/home/enric/src/wrf_pristine/WRF/phys/module_bl_acm.F`; the prompt path
`module_bl_acm2.F` is absent. The oracle links unmodified `module_bl_acm.F` plus
only a local `wrf_error_fatal` stub. `full_wrf_exe=false`.

Residual summary from the report:

- Regimes: `unstable_acm2_daytime`, `strong_unstable_acm2`,
  `stable_nocturnal_local`, `stable_marine_local`, `neutral_local`,
  `weak_unstable_transition`.
- Branch coverage: cases 1, 2, and 6 use ACM2 non-local free-convective mixing;
  cases 3, 4, and 5 are local-only.
- Worst PBLH absolute residual: `0.00948722816838199 m`.
- Worst field residual: `EXCH_H` case 2, `0.003448949559810899`, passing by
  relative tolerance.
- Tendency residuals are within the predeclared absolute/relative tolerances.

## unresolved risks

- This is a standalone column adapter and proof lane, not integrated into the
  operational dispatcher.
- No GPU performance claim; JAX was run on CPU per lane instruction.
- The adapter returns only frozen S0 state tendencies (`u/v/theta/qv`); WRF also
  computes cloud-water/ice PBL tendencies internally, but the S0 ACM2 contract
  does not expose `qc/qi` writes for this lane.
- The oracle is a real WRF-module oracle, not a full coupled `wrf.exe` run.

## next decision needed

Manager review: accept the ACM2 savepoint parity lane as PASS and decide when to
wire ACM2 into the manager-owned mixed-suite physics dispatcher.
