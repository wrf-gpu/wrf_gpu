# V0.14 Memory Manager 260609 Review

Date: 2026-06-09
Manager: GPT-5.5 xhigh side manager
Recommendation: `REVIEW_ONLY`

## Objective

Advance the `memory_manager_contract_260609.md` lane without invalidating the
primary grid-parity/base-state debug. This was handled as no-source analysis:
the active tmux worker `0:4 gpt-base-boundary` owns the current source-adjacent
truth boundary, so memory and FP32 source work remain paused.

## Verdict

`REVIEW_ONLY`. There is no production source change to merge from this lane.
The previous side-manager work already merged the only non-conflicting source
cleanup (`ee6cbbe1`, WDM6 `slmsk` shape cleanup). All remaining material memory
or FP32 source work either touches locked grid-parity files or requires a
post-grid-parity exact-branch GPU preflight.

## Files Changed

- `proofs/v014/memory_manager_260609.py`
- `proofs/v014/memory_manager_260609.json`
- `proofs/v014/memory_manager_260609.md`
- `.agent/reviews/2026-06-09-v014-memory-manager-260609.md`

No `src/gpuwrf/**` files were edited.

## Commands Run

```bash
python -m py_compile proofs/v014/memory_manager_260609.py
JAX_PLATFORMS=cpu CUDA_VISIBLE_DEVICES= PYTHONPATH=src python proofs/v014/memory_manager_260609.py
python -m json.tool proofs/v014/memory_manager_260609.json >/tmp/memory_manager_260609.validated.json
git diff --check
git diff -- src/gpuwrf
```

## Proof Objects Produced

- `proofs/v014/memory_manager_260609.json`
- `proofs/v014/memory_manager_260609.md`

## Source Locks Respected

- `src/gpuwrf/dynamics/**`
- `src/gpuwrf/runtime/operational_mode.py`
- `src/gpuwrf/integration/d02_replay.py`
- `src/gpuwrf/nesting/**`
- `src/gpuwrf/boundary*`
- `src/gpuwrf/contracts/state.py`
- boundary, carry, restart, init, wrfout, live-nest, and base-state files
- any file touched by the active grid-parity sprint

## Deferred Items And Exact Blocks

- Exact-branch memory preflight: prepared only; GPU run waits for the primary
  manager to release the branch/GPU. It must use `scripts/run_gpu_lowprio.sh`.
- Moisture transport velocity reuse: blocked by
  `src/gpuwrf/runtime/operational_mode.py` and
  `src/gpuwrf/dynamics/flux_advection.py`.
- Non-radiation physics column tiling: measure-first after grid parity; may
  touch coupling/physics files depending on the selected scheme.
- Moisture limiter workspace reduction: blocked by dycore/runtime locks.
- Acoustic carry split/pad cleanup: blocked by `src/gpuwrf/dynamics/**` and
  runtime; same fault surface as current `P/MU/W` debug.
- State alias reduction: blocked by `src/gpuwrf/contracts/state.py` and
  compatibility paths; ADR required.
- FP32 R0/R1/R2 source work: blocked by runtime, dycore, boundary/restart/init,
  and carry locks while fp64 live-nest/base-state parity is unresolved.

## GPU Usage

- GPU used: no.
- Peak VRAM: not applicable.
- Lock evidence: no GPU command was launched. The only prepared GPU path is the
  exact-branch memory preflight through `scripts/run_gpu_lowprio.sh` after the
  primary grid-parity manager releases the branch/GPU.

## Top 5 Next Tasks After Grid Parity

1. Run exact-branch memory preflight on the stabilized branch.
2. Resume FP32 R0/R1 only after the live-nest/base-state lock is released.
3. Implement moisture transport velocity reuse if active moisture advection is
   on the validation path.
4. Pick a measurement-led non-radiation column-tiling pilot.
5. Co-design acoustic carry split with FP32 acoustic after fp64 parity is fixed.

## Unresolved Risks

- The current preflight is a prepared no-run plan, not a GPU memory-fit proof
  for the final branch.
- Active base-state debugging may change the exact post-grid-parity source
  surface; refresh this report if that worker lands a production patch.
- FP32 acoustic remains feasible in principle but should not be integrated while
  fp64 grid parity is still confounded.

## Next Decision

Primary manager should continue the active base-state boundary sprint. This
memory/FP32 lane should stay `REVIEW_ONLY` until grid parity releases the
runtime/dycore/init locks or the primary manager asks for a refreshed preflight.
