# GPT v0.2.0 Cleanup Handoff

## Objective

Close three small v0.2.0 cleanup items on `worker/gpt/v020-cleanup` from
`worker/opus/l1-rad-time`: Noah-MP per-cell `nroot`, fail-fast namelist support
checking, and the stale restart `State.__slots__` count.

## Files Changed

- `src/gpuwrf/physics/noahmp/noahmp_driver.py`: gathers WRF `NROOT` by per-cell
  `IVGTYP` and carries `nroot_cell`; keeps grid max only as the static loop bound.
- `src/gpuwrf/physics/noahmp/energy.py`: narrow BTRAN nroot wiring only; uses
  `nroot_cell` for active-root masking and per-cell root-depth denominator. No
  surface flux solve formulas changed.
- `tests/test_noahmp_nroot_params.py`: synthetic WRF table test proving NROOT
  varies by vegetation category (`1,4,3,2`) instead of collapsing to grid max.
- `src/gpuwrf/io/namelist_check.py`: new fail-fast supported-option registry.
- `tests/test_namelist_check.py`: supported config passes; unsupported selections
  raise actionable errors.
- `tests/test_m7_restart_checkpoint_roundtrip.py`: slot count updated from 47 to
  verified current count 53; bitwise per-field roundtrip checks unchanged.
- `proofs/v020_cleanup/`: CPU pytest proof outputs.

## WRF References

- `NROOT` source: WRF Noah-MP vegetation parameter table (`MPTABLE.TBL` /
  `VEGPARM.TBL`, MODIS `NROOT` row) read by vegetation category.
- Category source: Noah-MP driver uses `VEGTYP = IVGTYP(I,J)` before table gathers.
- BTRAN use: Noah-MP ENERGY rooting-layer water-stress block
  (`module_sf_noahmplsm.F` ENERGY BTRAN, around lines 2148-2171).

## Supported-Option Registry

Accepted selected options in this branch:

- `mp_physics`: `0`, `8` Thompson
- `cu_physics`: `0` only; KF(1) is not implemented on this branch
- `bl_pbl_physics`: `0`, `5` MYNN
- `sf_sfclay_physics`: `0`, `5` MYNN/sfclayrev path
- `sf_surface_physics`: `0`, `4` Noah-MP
- `ra_sw_physics`, `ra_lw_physics`: `0`, `4` RRTMG
- dynamics/runtime guards: `rk_order=3`, `diff_6th_opt in {0,2}`,
  `diff_opt in {0,2}`, `km_opt in {0,1}`, `w_damping in {0,1}`,
  `damp_opt in {0,3}`, `sf_urban_physics=0`

## Commands Run

- `JAX_PLATFORM_NAME=cpu OMP_NUM_THREADS=2 taskset -c 0-3 pytest -q tests/test_noahmp_nroot_params.py`
- `JAX_PLATFORM_NAME=cpu OMP_NUM_THREADS=2 taskset -c 0-3 pytest -q tests/test_namelist_check.py`
- `JAX_PLATFORM_NAME=cpu OMP_NUM_THREADS=2 taskset -c 0-3 pytest -q tests/test_m7_restart_checkpoint_roundtrip.py`
- `JAX_PLATFORM_NAME=cpu OMP_NUM_THREADS=2 taskset -c 0-3 pytest -q tests/test_noahmp_nroot_params.py tests/test_namelist_check.py tests/test_m7_restart_checkpoint_roundtrip.py`

## Proof Objects Produced

- `proofs/v020_cleanup/item1_nroot_pytest.txt`
- `proofs/v020_cleanup/item2_namelist_check_pytest.txt`
- `proofs/v020_cleanup/item3_restart_checkpoint_pytest.txt`
- `proofs/v020_cleanup/focused_pytest_all.txt`

## Unresolved Risks

- The nroot fix required a minimal `energy.py` BTRAN wiring touch; changing only
  `build_energy_params` would carry the per-cell map but leave non-root layers
  contributing under the static max-root loop.
- `cu_physics=1` is intentionally rejected in this branch because no KF module is
  present here. Add it to the registry only when the KF lane is merged and gated.

## Next Decision Needed

Manager should review/merge these three commits into the v0.2.0 daytime-T2 chain
and remeasure the Noah-MP LH/T2 impact on the target cases.
