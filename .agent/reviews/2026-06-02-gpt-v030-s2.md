# GPT v0.3.0 S2 Static Geog Handoff

## Objective
Implement the S2 static-geography lane: load WPS `geo_em.d0N.nc` static fields into
the frozen metgrid-equivalent schema subset, provide an independent WRF Lambert
projection/metric derivation, and prove d01-d03 parity against local WPS oracles
across at least three cases.

## Files Changed
- `src/gpuwrf/init/static_geog.py`
- `src/gpuwrf/init/projection.py`
- `tests/init/test_static_geog.py`
- `tests/init/test_projection.py`
- `proofs/v030/s2_static_geog_report.json`
- `.agent/reviews/2026-06-02-gpt-v030-s2.md`

## Commands Run
- `git worktree add -b worker/gpt/v030-s2 /home/enric/src/wrf_gpu2/.claude/worktrees/v030-s2-gpt worker/opus/v030-s0`
- `JAX_PLATFORM_NAME=cpu taskset -c 0-3 python3 -m pytest tests/init/test_static_geog.py tests/init/test_projection.py -q`
- Reran the same pytest command after the proof-report aggregation tweak.

## Proof Objects Produced
- `proofs/v030/s2_static_geog_report.json`
  - Cases: `20260428_18z_72h`, `20260429_18z_72h`, `20260521_18z_72h`
  - Domains: `d01`, `d02`, `d03`
  - Result: `geo_em_static_fields_pass=true`, `met_em_static_block_pass=true`,
    `projection_derivation_pass=true`, `category_bit_exact_pass=true`
  - Worst independent projection checks: `XLAT_M` max abs `2.2311e-05` deg,
    `XLONG_M` max abs `2.2013e-05` deg, `MAPFAC_M` max rel `2.4787e-07`,
    `F` max abs `5.2481e-11`, `E` max abs `3.9811e-11`,
    `SINALPHA` max abs `2.1484e-07`, all within predeclared tolerances.

## Projection Parameters Used
- WRF/WPS Lambert: `MAP_PROJ=1`
- `TRUELAT1=25.0`, `TRUELAT2=30.0`, `STAND_LON=-16.4`
- WRF geogrid constants: earth radius `6370000.0 m`, omega `7.292e-5 s^-1`
- Domain center/spacing/dims from each `geo_em`: `CEN_LAT`, `CEN_LON`, `DX`,
  `DY`, `west_east`, `south_north`, and nesting attrs.

## Public API
- `gpuwrf.init.static_geog.load_static_geog(path) -> StaticGeogData`
- `gpuwrf.init.static_geog.static_geog_specs()`
- `gpuwrf.init.static_geog.static_geog_field_names()`
- `gpuwrf.init.static_geog.metgrid_projection_from_dataset(dataset)`
- `gpuwrf.init.projection.LambertGrid.from_wps_dataset(dataset)`
- `LambertGrid.latlon(stagger)`, `map_factor(stagger)`, `coriolis()`,
  `rotation()`, `derive_fields()`
- Scalar/vector helpers: `lambert_forward`, `lambert_inverse`,
  `lambert_map_factor`, `coriolis_from_lat`, `rotation_from_lon`

## Unresolved Risks / Notes
- The prompt mentioned `LANDSEA` and `SOILHGT` as static-geog outputs, but the
  frozen schema marks both `source="aifs_grib"` and the real local met_em files
  confirm they are not copied from `geo_em` (`LANDMASK` vs `LANDSEA` differs by
  1.0; `HGT_M` vs `SOILHGT` differs by up to kilometers). I did not add aliases
  that would poison met_em parity. The report records this under
  `non_s2_schema_alias_checks`.
- No GPU/profiler artifact is expected for this CPU-only initialization lane.

## Next Decision Needed
Manager should decide whether S0 schema text/contracts need a clarifying note that
`LANDSEA`/`SOILHGT` are S1/S3 forcing-derived fields, while `LANDMASK`/`HGT_M`
are S2 static-geog fields.
