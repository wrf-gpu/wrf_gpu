# GPT v0.6.0 Kain-Fritsch Cumulus Handoff

## Objective

Port/finish the WRF Kain-Fritsch cumulus lane (`cu_physics=1`) in JAX against the frozen v0.6.0 S0 physics interface, reuse the prior P0-4 work, and produce WRF savepoint parity evidence for triggering, tendencies, rain, and cumulus carry (`w0avg`, `nca`).

## Files Changed

- `src/gpuwrf/physics/cumulus_kf.py`
  - Enabled x64 for parity.
  - Added the v0.6.0 `step_kf_column` adapter returning `PhysicsStepResult`, `PhysicsTendency`, `PhysicsCarry.cumulus`, and `PhysicsDiagnostics.cumulus`.
  - Added WRF-faithful `w0avg`/`nca` carry handling and accumulator increment for `rainc_acc`.
  - Added WRF QG-negative moisture-borrow fixup in the closure iteration instead of a clamp.
- `tests/test_v060_cumulus_kf.py`
  - Added CPU-only savepoint parity gate and adapter key contract check.
- `proofs/v060/oracle/*`
  - Added the reproducible Fortran oracle driver harness linked against pristine WRF `module_cu_kfeta.F`.
  - Added one non-triggering stable/dry case to the prior P0-4 deep/shallow cases.
- `proofs/v060/savepoints/kf_case_1.json` through `kf_case_5.json`
  - WRF-module oracle savepoints.
- `proofs/v060/kf_savepoint_parity_report.json`
  - Machine-readable PASS/FAIL report.
- `.agent/sprints/2026-06-02-v060-kf-cumulus/sprint-contract.md`
  - Sprint contract and predeclared parity tolerances.

## Commands Run

- `python -m py_compile src/gpuwrf/physics/cumulus_kf.py tests/test_v060_cumulus_kf.py`
- `taskset -c 0-3 bash proofs/v060/oracle/build_and_run.sh`
- `taskset -c 0-3 env JAX_PLATFORM_NAME=cpu JAX_PLATFORMS=cpu pytest -q tests/test_v060_cumulus_kf.py --tb=short`
- `taskset -c 0-3 env JAX_PLATFORM_NAME=cpu JAX_PLATFORMS=cpu pytest -q tests/test_kf_cumulus_oracle.py --tb=short`

## Proof Objects Produced

- `proofs/v060/kf_savepoint_parity_report.json`
  - Verdict: `PASS`.
  - Cases: 5 total: deep triggering, shallow triggering, and stable non-triggering coverage.
  - Trigger parity: exact `ISHALL` match for all cases.
  - Carry parity: `nca` exact for all cases; `w0avg` exact in savepoint replay mode.
  - Rain parity: worst absolute residual `1.8636e-06` mm, under the predeclared `max(1e-4, 3e-3*abs(ref))` tolerance.
  - Tendency parity: all six scalar tendency arrays pass the predeclared `abs <= 1e-7 or rel <= 2e-3` tolerance.
  - Non-triggering case: zero tendency/rain outputs pass `1e-12` absolute tolerance.

## Unresolved Risks

- The oracle is a real WRF-module oracle linked against unmodified `/home/enric/src/wrf_pristine/WRF/phys/module_cu_kfeta.F`, but it is not a full coupled `wrf.exe` real-case run. Repository search did not expose a current cumulus-capable M20 oracle factory.
- The adapter is column-level and returns the frozen S0 tendency/carry/accumulator contract. Dispatcher-level coupling, domain vectorization, and `rainc_acc` state accumulation remain manager/integrator work.
- No GPU performance claim is made. All gates here ran on CPU as requested.

## Next Decision Needed

Decide whether the WRF-module oracle is acceptable for the v0.6.0 KF lane close, or whether a follow-up must add full `wrf.exe` cumulus savepoint extraction before merge.
