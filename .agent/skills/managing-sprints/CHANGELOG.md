# Changelog

## 0.3.0

- Release-quality rewrite of `SKILL.md`: the dispatch model, gates, and roles now
  reflect the current operating model (manager + frontrunner = strongest Opus;
  verifier/critic = GPT-5.5; third model reactive-only). Removed dated per-session
  model-policy churn.

## 0.1.0–0.2.7 (historical)

- Iterative sprint-management workflow and dispatch-policy edits across early
  milestones. The accumulated dated/model-specific directives were superseded by
  the 0.3.0 rewrite; see `AGENTS.md` and `.agent/decisions/VERSION-SPRINT-LEDGER.md`
  for the authoritative current process and per-version history.
