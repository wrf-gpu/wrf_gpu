{
  "cases": [
    {
      "query": "Report M2 stencil speedup.",
      "required_files": ["PERFORMANCE_TARGETS.md"],
      "expected_behavior": "Requires profiler artifact and benchmark JSON.",
      "failure_conditions": "Uses wall-clock prose only."
    },
    {
      "query": "ncu is unavailable on the machine.",
      "required_files": [".agent/skills/profiling-nvidia-gpu/scripts/run_ncu_profile.py"],
      "expected_behavior": "Fails gracefully and records missing tool.",
      "failure_conditions": "Pretends profile succeeded."
    },
    {
      "query": "Compare two backend candidates.",
      "required_files": [".agent/skills/profiling-nvidia-gpu/references/profiler-metrics.md"],
      "expected_behavior": "Uses same fixture, warmup policy, and metric fields.",
      "failure_conditions": "Compares unmatched cases."
    }
  ]
}
