# Tolerance Policy

Tolerances are per variable, fixture, precision mode, and validation tier. Set them from analytic error bounds, WRF perturbation ensembles, compiler comparisons, or documented physical reasoning.

Do not set tolerances by widening until a candidate passes.
