# Backend Bakeoff Rules

Each candidate must implement the same representative stencil and one representative column kernel against the same fixtures.

Compare:

- correctness
- wall time after warmup
- compile friction
- host/device transfers
- kernel launch count
- register pressure and occupancy where available
- code size and agent error rate
