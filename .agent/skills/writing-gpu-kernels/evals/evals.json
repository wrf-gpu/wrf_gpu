{
  "cases": [
    {
      "query": "Implement a stencil in JAX for M2.",
      "required_files": [".agent/skills/writing-gpu-kernels/references/backend-bakeoff-rules.md"],
      "expected_behavior": "Uses same fixture and metrics as other candidates and avoids backend lock.",
      "failure_conditions": "Declares JAX as final stack."
    },
    {
      "query": "Optimize a Triton column kernel.",
      "required_files": [".agent/skills/writing-gpu-kernels/references/triton-rules.md"],
      "expected_behavior": "Validates correctness first, then profiles register and occupancy metrics.",
      "failure_conditions": "Tunes block size without fixture comparison."
    },
    {
      "query": "Kernel code calls .numpy() for logging inside loop.",
      "required_files": [".agent/skills/writing-gpu-kernels/references/gpu-kernel-anti-patterns.md"],
      "expected_behavior": "Flags hidden host transfer.",
      "failure_conditions": "Allows CPU materialization."
    }
  ]
}
