# CARRYOVER — standalone 24h near-surface westerly bias (cross-version forecast-skill item)

**Opened:** 2026-06-03 (at v0.4.0 close). **Status: OPEN / TRACKED. NOT a fidelity bug. NOT fixed.**
**Origin:** [`.agent/decisions/V0.4.0-CLOSE.md`](../decisions/V0.4.0-CLOSE.md) ·
proof [`proofs/v040/v040_close_proof.json`](../../proofs/v040/v040_close_proof.json).

## The item

A **domain-uniform near-surface WESTERLY excess** in the 24 h **standalone** native-init forecast:

- **+1.2 m/s** (20260429), **+0.75 m/s** (20260521).
- T2 correct; column-wide; **stable and finite** (forecast gate verdict
  `STABLE_BUT_CORE_FIELD_MISMATCH`, all scored cases stable/finite/physical).
- Ruled out, vs unmodified WRF, against **every faithful ported operator and scheme** over 10 debug
  rounds (mass-continuity, PGF al/dpn, s_aw floor, php-freeze, PBL/surface-stress, horizontal
  diffusion, GWD, nest feedback, and decisively Kain-Fritsch cumulus — both JAX and an independent
  unmodified-CPU-WRF cu0-vs-cu1 oracle). See the round table in `V0.4.0-CLOSE.md`.
- **Conclusion:** dynamical, not traceable to any ported component; possibly sample/config-specific.

## Why it is not yet resolved

**Expand-dates falsification is DATA_BLOCKED:** only **2 native-runnable dates** remain; the rest
of the MAM corpus has **purged met_em**, so the bias cannot be characterized across a powered sample.

## Next steps (when unblocked)

1. **Re-run CPU-WRF with met_em PRESERVED** for ≥ a powered n (n≥10–15) of MAM dates — this is the
   0.9.0 backfill gate (runner PID 133927, currently SIGSTOPPED). Stop the met_em purge.
2. With ≥ powered n, re-run the standalone native-init 24 h gate and test whether the westerly
   excess is (a) systematic across dates (→ a real residual to root-cause) or (b) sample-specific
   (→ within forecast-skill scatter, close as a non-issue).
3. If systematic: the prime remaining suspects are config/sample-level, not ported-operator-level —
   candidate angles are the standalone LBC update cadence vs CPU-WRF, the native base-state /
   reference-profile assembly, and the trade-wind subsidence inversion representation. (NOT more
   diffusion; NOT cumulus — both falsified.)

## Do / Do-not

- **Do** keep the 4 WRF-faithful dycore fixes (they are real corrections, each savepoint-PASS).
- **Do not** loosen any tolerance, add a wind clamp, or claim the bias is fixed.
- **Do not** re-investigate the 10 already-ruled-out angles without new (powered) data.
