# GPT-5.5 xhigh Dispatch: FP32 Acoustic R0/R1 Contract + Default-Inert Prototype

You are a GPT-5.5 xhigh implementation/de-risk worker for `/home/enric/src/wrf_gpu2`.

Read in this order:
1. `PROJECT_CONSTITUTION.md`
2. `AGENTS.md`
3. `.agent/sprints/2026-06-08-v014-fp32-acoustic-derisk/sprint-contract.md`
4. `.agent/decisions/V0140-FP32-ACOUSTIC-ROADMAP.md`
5. `.agent/reviews/2026-06-08-gpt-fp32-acoustic-refresh.md`
6. Relevant acoustic files under `src/gpuwrf/dynamics/`, `src/gpuwrf/contracts/`, and `src/gpuwrf/runtime/`.

Do not use the old global `wrf-gpu-port` skill. Repo-local rules are authoritative.

Context:
- Base must be `b4901a907948623a7781b6cfc10fbbf42f64dd73` or a direct descendant.
- The manager has an active fp64 TOST run; do not consume GPU.
- FP32 acoustic is the highest-priority v0.14 memory/performance lane and possible v0.13 only if low-risk evidence appears.

Task:
1. Produce an R0/R1 ADR-quality plan for `acoustic_precision_mode = "mixed_perturb_fp32"`.
2. Audit exact source points where base/reference fields are reconstructed from totals, where fp64 casts are hard-coded, and where precision mode/cache keys would need plumbing.
3. If and only if it is narrow and default-inert, implement a prototype scaffold that cannot affect current fp64 production. Examples: precision-mode enum/config key, report labels, static source audit helper, or explicit-base API overload with default preserving current behavior.
4. Do not attempt a full mixed acoustic implementation in this lane.

Required gates if source is edited:
- `JAX_PLATFORMS=cpu PYTHONPATH=src python -m py_compile <touched py files>`
- focused unit tests or a new proof under `proofs/v014/`
- explicit proof that default fp64 production behavior is unchanged.

Deliverables:
- `.agent/reviews/2026-06-08-gpt-fp32-r0r1.md`
- optional `.agent/decisions/ADR-0xx-mixed-perturb-fp32-acoustic.md` draft
- optional `proofs/v014/` proof artifacts
- commit your branch only if gates pass
- final tmux line: `GPT FP32 R0R1 DONE`
