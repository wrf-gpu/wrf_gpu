# Pending Memory: Cross-Model Debug Cadence

Status: pending promotion after the next complex debug sequence.

Principal confirmation:

- Enric confirmed on 2026-06-09 that the Opus critique was useful and should be
  used more often for deep debug checkpoints: after two focused GPT/debug
  sprints on the same problem, if the solution is still not proven, run one
  Opus xhigh critic/debugger to test conclusions, methods, hypotheses, and the
  candidate bug itself.

Lesson:

- Opus xhigh critique on 2026-06-09 was materially useful: it caught that the
  live-nest base source fix was a real base-state correction but not proven as
  the V10/grid-field symptom closer.
- Manager cadence for complex correctness bugs: run up to two focused GPT/debug
  sprints on the same problem; if the solution is still not proven or the
  conclusion chain is uncertain, dispatch one Opus xhigh critic/debugger to
  challenge method, hypotheses, evidence, performance implications, and
  candidate bug before committing.
- This is targeted escalation, not routine double-agenting. It should save
  context and time by preventing confident but under-proven pivots.

Evidence:

- `.agent/reviews/2026-06-09-v014-debug-method-critic.md`
- `proofs/v014/live_nest_base_source_fix.json`
- `.agent/skills/managing-sprints/SKILL.md`
