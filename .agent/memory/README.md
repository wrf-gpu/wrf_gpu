# Memory

Stable memory is approved and default-loadable. Pending memory is proposed but not yet approved. Archive stores raw notes and old sprint summaries that should not be loaded by default.

Updates require `memory-patch.md`, validation, and reviewer approval.
