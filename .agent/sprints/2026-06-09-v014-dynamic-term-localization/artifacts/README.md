# Artifacts

The formal proof artifacts for this sprint live in `proofs/v014/`:

- `wrf_dynamic_term_localization.py`
- `wrf_dynamic_term_localization.json`
- `wrf_dynamic_term_localization.md`
- `wrf_dynamic_term_localization_patch.diff`

The worker review is `.agent/reviews/2026-06-09-v014-dynamic-term-localization.md`.
This directory exists so `scripts/close_sprint.py` can verify the sprint has a
complete closeout envelope without duplicating the large proof files.
