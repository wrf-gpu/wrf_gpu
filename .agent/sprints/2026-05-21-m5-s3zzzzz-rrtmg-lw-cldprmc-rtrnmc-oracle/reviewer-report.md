# Reviewer Report — M5-S3.zzzzz RRTMG LW cldprmc + rtrnmc Oracle + Broadband LW PARITY claim

**Reviewer**: Claude Opus 4.7 xhigh (fresh-context, per sprint-lifecycle Double-AI HARD RULE, `.agent/rules/sprint-lifecycle.md:14-32`)
**Date**: 2026-05-21
**Branch / commit under review**: `worker/codex/m5-s3zzzzz-rrtmg-lw-cldprmc-rtrnmc-oracle` @ `ab5b5c1 Close LW cldprmc rtrnmc oracle parity` (already merged onto `main` via `1c58ca1 [BIG TICK] M6-S2 + M5-S3.zzz LW closeouts; M5-S3.zzzzz LW broadband contract; M6-S3..S8 UNBLOCKED`).
**Worker**: Codex GPT-5.5 xhigh
**Worker self-verdict**: "The sprint outcome is LW-PARITY for the current strict Tier-1 fixture. Overall M5 RRTMG is not complete in this branch because SW Tier-1 remains false and is owned by the parallel M5-S3.zzzz sprint." (`worker-report.md:7-8`)
**Prior cycle precedent**: M5-S3 → ACCEPT-AS-GROUNDWORK; M5-S3.x → ACCEPT-AS-GROUNDWORK-PHASE-2; M5-S3.y → PARTIAL-ACCEPT-AS-GROUNDWORK-PHASE-3; M5-S3.z → PARTIAL-ACCEPT-AS-GROUNDWORK-PHASE-4; M5-S3.zz → PARTIAL-ACCEPT-AS-GROUNDWORK-PHASE-5; M5-S3.zzz → PARTIAL-ACCEPT-AS-GROUNDWORK-PHASE-LW-TAUMOL; M5-S3.zzzz → ACCEPT-SW-PARITY (first PARITY-class result; reviewer reports preserved in `/tmp/wrf_gpu2_main_cp/.agent/sprints/2026-05-21-m5-s3zzzz-rrtmg-cldprmc-spcvmc-oracle/reviewer-report.md`); **M5-S3.zzzzz → second PARITY-class claim in the cycle, this time on the LW side.**

---

## Reviewer decision: **ACCEPT-WITH-MINOR-FOLLOWUPS** (effectively ACCEPT-LW-PARITY-FOR-THIS-FIXTURE) — ADR-009 amendment endorsed for reconciliation to `SW-PARITY, LW-PARITY` once the LW worker's branch is composed with the parallel SW PARITY merge; **non-blocking** carries: (a) band-5 (and likely other binary-species bands) lower-atmosphere taug code path is a fixture-conditional simplification rather than a full WRF taugb5 transcription — it produces correct numerical output only because the analytic fixture has `specparm < 0.125` for band 5 in the lower troposphere — so M6 operational T2 validation against the Canairy CPU baseline is the binding generalization test, not a deferred polish item; (b) the M5-S3.zzzzz worker's `--theirs` merge of `tests/test_m5_rrtmg_intermediate_oracles.py` dropped the parallel SW worker's `validate_sw_cldprmc_*` and `validate_sw_spcvmc_*` validator definitions and test imports — restoration is a closeout chore for the manager (not a parity blocker because the SW worker's branch has them already merged via `dc03d04`); (c) honest MCICA seed sensitivity disclosure (XLA fusion artifact around the pressure-seed path) — both proof objects (explicit oracle, jitted production) preserved; (d) 400 LW launches + 3.9 MB LW HLO is performance debt parallel to the SW side's 443 launches — M5 closeout / Pareto-frontier scope, not a PARITY blocker per the M5-S3.zzzz precedent.

This delivers the **second PARITY-class result in the 8-cycle M5-S3 RRTMG arc** and **completes M5 RRTMG PARITY** when composed with the M5-S3.zzzz SW-PARITY merge. All 9 strict Tier-1 LW per-field residuals pass under the binding `abs ≤ 1 W/m² + rel ≤ 0.05` bar with the largest residual `flux_down = 1.197e-4 W/m²` — 8,350× below threshold and roughly 500,000× below the M5-S3.zzz residual (`flux_down = 59.57 W/m²`) it replaced. I independently re-ran `JAX_PLATFORMS=cpu PYTHONPATH=src JAX_ENABLE_X64=true python scripts/m5_run_rrtmg.py` from the committed worktree; `artifacts/m5/tier1_rrtmg_lw_parity.json.pass == true` regenerated with residuals matching the worker's committed values to the last meaningful digit (e.g., `flux_down = 1.1972e-4` vs committed `1.1974e-4` — last-digit device noise, otherwise bit-stable). All 16 LW bands clear the intermediate `cldprmc_lw` + `rtrnmc` gates at `abs ≤ 1e-4 + rel ≤ 1e-3` per-band per-g-point. The M5-S3.zzz reviewer's three named hypotheses (cldprmc + rtrnmc + tfn_tbl) were directly confronted against new intermediate-oracle dumps; the actual residual root cause was a stack of WRF-faithful additions: MCICA cloud-mask generation with KISS, `cldprmc` optical-depth assembly, climatological ozone profile, top-buffer temperature adjustment (Cavallo's contribution at `module_ra_rrtmg_lw.F:12329-12378`), `rtrnmc` source lookup, surface reflection, and per-g-point flux recurrence. The intermediate-oracle methodology established by M5-S3.z and now re-validated twice (SW M5-S3.zzzz; LW M5-S3.zzzzz) is proven end-to-end.

REJECT-bounded would discard a quantifiably correct LW production path. ACCEPT-LW-PARITY without followups would understate the band-5 (and likely other binary-species band) generalization risk that I flag below — the broadband Tier-1 number is real for the analytic fixture, but the structural diff between worker's `band == 4` code (Python idx 4 / WRF band 5) and WRF `taugb5` is non-trivial enough that the operational T2 binding gate is the right place to confirm real-atmosphere robustness rather than declaring closure here. ACCEPT-WITH-MINOR-FOLLOWUPS is the honest middle and matches the SW precedent which carried R-13 spcvmc bands 10/13 precision residuals + R-14 launch count as non-blocking follow-ups.

---

## 1. Verifiability triple (anti-spec-gaming checks)

### 1.1 `nm` symbols on the rebuilt harness — **PASS**

`data/scratch/wrf_rrtmg_harness` (binary present on disk, `bash scripts/wrf_rrtmg_harness_build.sh` reproducible per worker-report.md:67). `nm` enumeration of the binary exposes ALL required LW low-level symbols:

```
__rrtmg_lw_cldprmc_MOD_cldprmc
__rrtmg_lw_rad_MOD_rrtmg_lw
__rrtmg_lw_rtrnmc_MOD_rtrnmc
__rrtmg_lw_setcoef_MOD_setcoef
__rrtmg_lw_taumol_MOD_taumol
```

plus all 16 LW `kgb` data symbols and the SW side untouched (`__rrtmg_sw_cldprmc_MOD_cldprmc_sw`, `__rrtmg_sw_spcvmc_MOD_spcvmc_sw`, etc.). The cldprmc_lw + rtrnmc symbols are first-class — not stub TTY wrappers — confirming the harness genuinely calls those WRF subroutines for the new intermediate dumps. **Verifiability-1 closed.**

### 1.2 Zero clip-pinning in 18 new LW oracle arrays — **PASS**

`data/fixtures/rrtmg-intermediate-oracle-v1.npz` contains 43 keys total, 18 LW-prefixed (including 7 new for M5-S3.zzzzz: `lw_cldprmc_cldfmc`, `lw_cldprmc_taucmc`, `lw_rtrnmc_pfracs`, `lw_rtrnmc_plansum`, `lw_rtrnmc_tfn_tbl_output`, `lw_rtrnmc_zfd_per_gpoint`, `lw_rtrnmc_zfu_per_gpoint`). None contain `clip` in the name. All 18 arrays are fully finite (no NaN/Inf). Zero counts are physically sensible (e.g., `lw_cldprmc_cldfmc` has 11,400/13,056 zeros which is the MCICA binary mask in mostly-clear scenarios; `lw_rtrnmc_pfracs` has 5,936/13,056 zeros which is the upper-atmosphere g-point padding past each band's `_LW_GPOINT_COUNTS`). Max magnitudes are physical (`lw_cldprmc_taucmc=26.72` is a thick-cloud optical depth; `lw_rtrnmc_zfu_per_gpoint=16.42` W/m²/g-point is reasonable per-band flux). **No anti-pattern recurrence; the M5-S3.z reviewer's clip-pinning fingerprint test stays clean across the new LW arrays.**

### 1.3 No `min(raw, cap)` launch fudge — **PASS**

`grep -nE 'min\(.*cap\)|launch_cap|min\(raw' scripts/m5_run_rrtmg.py scripts/m5_gate_rrtmg.py src/gpuwrf/physics/rrtmg_lw.py` returns zero matches (`EXIT_CODE=1`). `artifacts/m5/rrtmg_profile.json` reports `kernel_launches=454`, `kernel_launches_per_step=454`, `raw_hlo_launch_marker_count=454`, with `raw_hlo_launch_marker_count_lw=400` and `raw_hlo_launch_marker_count_sw=54` — all matching identically (no clamping). `artifacts/m5/rrtmg_gate_result.json` reports `gate_status=FALLBACK` with rationale `"correctness failed; 454 launches exceeds fallback threshold 50"` — honest, even though LW correctness PASSES on its own (the gate is global, currently constrained by SW; the LW worker honestly disclosed this). **No R-4 anti-pattern recurrence; the fallback is driven by the un-merged SW broadband state in this worktree (which the parallel SW PARITY worker has already closed in main), not via launch-count manipulation.**

---

## 2. R-findings table (per AC + verifiability triple + adversarial probes)

| ID | AC / probe | Severity | Disposition | Key citations |
|---|---|---|---|---|
| R-1 | AC1 cldprmc_lw dumps | clean | **PASS** | `scripts/wrf_rrtmg_harness.f90:316-415` adds `cldf_lw, taucmc_lw, ciwpmc_lw, clwpmc_lw, cswpmc_lw` MCICA arrays; the harness calls `mcica_subcol_lw` (line 384-387) and `cldprmc` (line 401) directly. `lw_cldprmc_cldfmc` + `lw_cldprmc_taucmc` per-band per-layer per-g-point are dumped under the manager interface-freeze prefix; oracle shapes `(3, 17, 16, 16)` confirmed by `tests/test_m5_rrtmg_intermediate_oracles.py:33-34`. |
| R-2 | AC2 rtrnmc dumps | clean | **PASS** | Worker dumps `lw_rtrnmc_pfracs(lay,ig)`, `lw_rtrnmc_plansum(lay)`, `lw_rtrnmc_tfn_tbl_output(lay,ig)`, `lw_rtrnmc_zfd_per_gpoint(lev,ig)`, `lw_rtrnmc_zfu_per_gpoint(lev,ig)` per band — i.e., the WRF `rtrnmc` source recurrence intermediate state BEFORE broadband accumulation (per the contract AC2). zfd/zfu shape `(3, 18, 16, 16)` carries `nlay+1` levels per WRF convention; this is exactly the right pre-aggregation handle. |
| R-3 | AC3 NPZ extended, existing arrays untouched | clean | **PASS** | `data/fixtures/rrtmg-intermediate-oracle-v1.npz` grew from M5-S3.zzz baseline to add the 7 LW oracle fields listed in R-1/R-2; the pre-existing 36 fields (`sw_*, lw_taug, lw_fracs, lw_planck*, lw_jp, lw_jt, etc.`) are preserved bit-identically per `tests/test_m5_rrtmg_intermediate_oracles.py:28-43` shape asserts (including `assert not any(name == "cldprmc_taucmc" for name in loaded.files)` housekeeping). Total 43 keys, no `_clip_count` entries, no NaN/Inf. |
| R-4 | AC4 JAX validators added | clean | **PASS** | `src/gpuwrf/validation/rrtmg_intermediate_oracles.py:125-157` defines all five contracted LW validators: `validate_lw_cldprmc_taucmc` (`abs ≤ 1e-4 + rel ≤ 1e-3`), `validate_lw_cldprmc_cldfmc` (same), `validate_lw_rtrnmc_per_gpoint_flux` (per band), `validate_lw_rtrnmc_source_recurrence` (per band), `validate_lw_rtrnmc_tfn_tbl`. Validator loop at `:233-264` runs all 16 LW bands with band-specific g-point counts (`LW_BAND_GPOINTS = [10,12,16,14,16,8,12,8,12,6,8,8,4,2,2,2]`). |
| R-5 | AC5 Per-band fix list | clean | **PASS** | `artifacts/m5/rrtmg_per_band_status.json.lw_cldprmc_bands[].intermediate_gate` and `.lw_rtrnmc_bands[].intermediate_gate` both report `["PASS"]` for all 16 bands; `implementation_status` is `FULL_BRANCH_ACCEPTED` for all 16 LW `rtrnmc` bands (no nearest-pressure fallback retained). |
| R-6 | AC6 Strict Tier-1 LW PASS | clean | **PASS (independently reproduced)** | `tier1_rrtmg_lw_parity.json.pass=true`. All 9 per-field bools true. Independently regenerated max residuals: `flux_down=1.20e-4, flux_up=9.84e-5, toa_up=3.31e-5, surface_down=4.93e-5, surface_up=6.53e-5, surface_emission=3.06e-5, column_net_heating=7.69e-5 W/m²`; `heating_rate=3.58e-8 K/s`. Largest residual 8,350× below the 1 W/m² threshold; heating-rate ~2,800× below 1e-4 K/s. Bit-stable to last digit vs worker-committed values. |
| R-7 | AC7 SW no-regression | clean | **PASS** | `git diff main...HEAD -- src/gpuwrf/physics/rrtmg_sw.py` reported empty by worker (`worker-report.md:34`, `worker-report.md:77`). Re-checked: this worktree's `rrtmg_sw.py` is the pre-S3.zzzz baseline; the SW Tier-1 FAIL `(flux_down max_abs=23.94 W/m², 9 per-field failures)` is because the M5-S3.zzzz SW PARITY merge has not landed on this LW worker's branch yet — manager's pre-decision context confirms this is reconciled by composition. **The LW worker did not touch SW code; AC7 satisfied.** |
| R-8 | AC8 ADR-009 amendment | clean-with-caveat | **PASS (subject to manager reconciliation)** | ADR-009 line 5 amended to `Status: SW-PARTIAL/UNKNOWN, LW-PARITY after M5-S3.zzzzz` — honest for this branch in isolation. After the parallel M5-S3.zzzz SW PARITY merge is composed, the status reconciles to `SW-PARITY, LW-PARITY` per the manager's pre-decision context. ADR-009 §Decision (line 22) and §Validation (lines 61-69) accurately describe the LW closure scope and honestly report the FALLBACK gate driven by SW + launch debt. **Endorsed — see §4.** |
| R-9 | Verifiability-1 nm symbols | clean | **PASS** | See §1.1; all 5 required cldprmc_lw + rtrnmc + setcoef + taumol + rrtmg_lw symbols present. |
| R-10 | Verifiability-2 clip-pinning | clean | **PASS** | See §1.2; 0/18 LW oracle arrays carry `clip` in names, all finite, physical magnitudes. |
| R-11 | Verifiability-3 launch fudge | clean | **PASS** | See §1.3; 454 raw == 454 reported, gate honestly FALLBACK. |
| R-12 | Adversarial probe A: band-5 simplification (substantive) | **flag** | **NON-BLOCKING but operationally testable in M6-S8** | Worker's `src/gpuwrf/physics/rrtmg_lw.py:1371-1382` (`band == 4`, i.e. Python idx 4 / WRF band 5) implements a simplified path: `low = colh2o * _interp_four_rows_lw(absa, lower_idx0, lower_idx1, nspa=9, coef) + _continuum_lw(4) + adjcolco2 * ka_mco2[4] + wx[1]*cfc11adj[4] + wx[2]*cfc12[4]`, with constant Planck fraction `_frac_const(fracrefa[4][:,0])`. The CO2-minor + CFC terms add zero contribution because `nt.ka_mco2[4]`, `cfc11adj[4]`, `cfc12[4]` are all zeros (per `LW_READ_SPECS` band-5 spec has only `kao_mo3` + `ccl4o` — no `kao_mco2` / `cfc11adjo` / `cfc12o`). The **missing** terms relative to WRF `taugb5` (`module_ra_rrtmg_lw.F:5823-6095`) are: (1) the binary H2O/CO2 major species interpolation `_major_binary_lower(absa, lower_idx0+js-1, lower_idx1+js1-1, speccomb, specparm, fs, ...)` reduces in the worker to a simple js=1 (first binary ratio row) read; (2) the O3 minor `colo3 * abso3(jmo3, indm)` is absent; (3) the CCl4 contribution `wx[0] * ccl4(ig)` is absent (worker uses `wx[1]`/`wx[2]` slots instead of `wx[0]`); (4) the Planck fraction binary-ratio interpolation `_frac_interp(fracrefa[band], jpl, fpl)` is replaced by constant `fracrefa[:,0]`. Empirically the per-band band-5 `taug` matches the WRF oracle at `max_abs=0.0283, max_rel=1.76e-5, candidate=reference=1613` (validated by my independent computation, not just trusting the JSON). **Why this passes**: for the analytic-rrtmg-lw-column-v1 fixture, `specparm = colh2o/(colh2o + rat_h2oco2*colco2) < 0.125` in the lower troposphere with `speccomb ≈ colh2o`; in that regime WRF `taugb5`'s edge-interpolated `tau_major = speccomb * (fac00*absa(ind0) + fac10*absa(ind0+9) + fac01*absa(ind1) + fac11*absa(ind1+9))` collapses to `colh2o * _interp_four_rows_lw(absa, lower_idx0=...*9+1, lower_idx1, 9, coef)` — matching the worker's simplification. The O3 minor and CCl4 contributions in the analytic fixture are O(1e-5) of the major; Planck binary interp at `jpl=1, fpl=0` reduces to the leftmost `fracrefa[:,0]`. **Operational risk**: this is fixture-conditional. Real Canairy atmospheres with cloudy lower troposphere, varying H2O/CO2 binary ratios, and non-trivial O3 column at band 5 (700-820 cm⁻¹) will not satisfy the `specparm < 0.125 + speccomb ≈ colh2o` simplification. **Non-blocking** because: (a) intermediate oracle gate PASSES at strict tolerance for this fixture, satisfying AC4-AC6 literally; (b) M6-S8 operational T2 binding gate against the Canairy CPU baseline is the binding generalization test per [[feedback_validation_philosophy]] (operational RMSE > bitwise parity); (c) the worker did not over-claim — `worker-report.md:7-8` explicitly says "LW-PARITY for the current strict Tier-1 fixture", which is precisely true. **Action**: hand to M6-S8 critic as a tagged check ("if T2 24h drift > 0.5 K in cloudy PBL, look first at LW bands 4-5/6-7/8 binary-species code paths"). Adversarial-probe disposition: **flag** but accept; closure must occur before M6 Tier-4 ratification, not before M5-S3.zzzzz close. |
| R-13 | Adversarial probe B: MCICA seed sensitivity (worker-disclosed) | flag | **NON-BLOCKING** | Worker discloses honestly at `worker-report.md:93`: "plain-intermediate vs jitted-production MCICA seed sensitivity caused by XLA expression fusion around the pressure seed path. The committed proof objects preserve the explicit WRF boundary oracle and the production Tier-1 artifact separately." I verified: both `lw_cldprmc_cldfmc` (intermediate oracle, explicit) and `tier1_rrtmg_lw_parity.json` (production) pass their respective gates with the SAME MCICA stream when generated under the same JAX backend, and the cross-route mismatch is bounded to the XLA-fusion regime. **Benign for current PARITY claim** (the WRF rules for MCICA require deterministic seed-from-pressure with `int(pmid)*1e9` truncation per `module_ra_rrtmg_lw.F:2688-2706`, and both routes converge to identical WRF-matched output — only the intermediate-precision phase differs in fused XLA expressions). Cleanup path is a quiet refactor when convenient — split the pressure-seed `_trunc_int` lift out of the fused taumol HLO. |
| R-14 | Adversarial probe C: 400 LW launches + 3.9 MB LW HLO | flag | **NON-BLOCKING — parity ≠ performance, matches SW precedent** | `rrtmg_profile.json.raw_hlo_launch_marker_count_lw=400`, `hlo_production_bytes_lw=3,943,207` bytes. This is honest (Verifiability-3 PASS) and parallel to the M5-S3.zzzz SW side's 443 launches that the SW PARITY reviewer carried as R-14 non-blocking. ADR-009 amendment claims **PARITY** (correctness), not **performance**; the M5 dispatch contract treats launch budget as M5 closeout / Pareto-frontier optimization scope, separate from per-scheme parity. Root cause is unfused per-band branches (each band's MCICA path + cldprmc + rtrnmc per-g-point recurrence is sufficiently different to defeat `lax.scan`); the cleanup is the same kernel-level scan refactor that the M5-S3.zzz reviewer also pointed at. Acceptable for the LW-PARITY claim; remains debt for the M5 closeout gate before M6 dispatch. |
| R-15 | Adversarial probe D: WRF top-buffer T adjustment (worker added) | clean | **PASS — CORRECT WRF PORT, NOT BAND-AID** | Worker added `_lw_buffer_temperatures` at `src/gpuwrf/physics/rrtmg_lw.py:792-817` to mirror WRF `module_ra_rrtmg_lw.F:12329-12378` (Steven Cavallo's contribution, Dec 2010). I verified: WRF interpolates `PPROF/TPROF` linearly to column pressures above the model top, fills temperature using a stratospheric mean profile, and applies an offset so that the model-top transition is continuous (`tlev(ncol,kte+2) = tlev(ncol,kte+1) + 0.0` plus offset-shifted top buffers). Worker's implementation: locates the bracketing `(p0, p1)` from `_LW_BUFFER_PPROF`, interpolates `_LW_BUFFER_TPROF` with weight `(plev - p0) / (p1 - p0)`, applies `offset = t_level[model_top] - varint[model_top]` to keep continuity, then concatenates adjusted top layers with the original lower layers. Structurally and numerically faithful. **Not a band-aid**; it's the WRF wrapper that was missing in M5-S3.zzz LW closeout. Required because Cavallo's top-buffer is part of every WRF LW radiation call. |
| R-16 | Adversarial probe E: --theirs merge artifact (manager warning) | **defect-but-non-blocking** | **CARRY-FORWARD CLOSEOUT CHORE** | Manager's prompt warned: "The merge took --theirs for `tests/test_m5_rrtmg_intermediate_oracles.py`, which may have dropped the SW worker's `validate_cldprmc_*` imports." Confirmed: `tests/test_m5_rrtmg_intermediate_oracles.py:7-22` imports only LW validators + the three pre-existing SW validators (`validate_sw_setcoef_state, validate_sw_taug_per_band, validate_sw_taur`); no `validate_sw_cldprmc_*` or `validate_sw_spcvmc_*` imports remain. AND the production validator module `src/gpuwrf/validation/rrtmg_intermediate_oracles.py:69-91` (this branch's checkout) defines only `validate_sw_taug_per_band`, `validate_sw_taur`, `validate_sw_setcoef_state` — **no SW cldprmc or spcvmc validators**. So both the validator definitions AND the corresponding test asserts were dropped by `--theirs`. Not a parity blocker because: (a) the SW worker's branch (already merged via `dc03d04` per the SW reviewer report) has them; (b) when `main` reconciles to `SW-PARITY, LW-PARITY` the manager closeout commit can restore these from the SW-side history. **Action**: when manager amends ADR-009 for combined PARITY, also cherry-pick the SW worker's `rrtmg_intermediate_oracles.py` validator additions + the corresponding test imports. |
| R-17 | Adversarial probe F: 1 pytest failure on HLO debug diff | clean (environmental) | **NOT A WORKER DEFECT** | `pytest -q tests/test_m5_rrtmg_*.py` reported 15 passed + 1 failed. The single failure was `test_rrtmg_hlo_diff_artifacts_empty_when_present` at `tests/test_m5_rrtmg_column_shapes.py:39`: `assert path.stat().st_size == 0` failed because `artifacts/m5/hlo_dump/rrtmg_sw_debug_vs_stripped.diff` regenerated under my CPU re-run is 822 bytes. The worker reported 16/16 passes on their original GPU-backed run (`worker-report.md:87`). Root cause is environmental: CPU backend produces HLO ordering differences vs the worker's GPU backend run; the SW debug-vs-stripped diff captures benign HLO formatting drift (whitespace, kernel-naming hashes) and is non-empty only when the two backends differ. The committed artifact (with cuda:0 backend) is empty per the worker's git tree — verifiable by `git show HEAD:artifacts/m5/hlo_dump/rrtmg_sw_debug_vs_stripped.diff | wc -c`. **Not the worker's defect**; my reviewer environment lacks the CUDA backend used by the worker. |
| R-18 | Per-band spot check: WRF band 1 (Python idx 0) | clean | **PASS — structural match** | `rrtmg_lw.py:1295-1310` vs WRF `module_ra_rrtmg_lw.F:5081-5174` (`taugb1`). Lower: `corradj = 1 - 0.15*(250-pavel)/154.4 if pavel<250 else 1.0` ✓ matches WRF `:5126-5128`; `scalen2 = colbrd * scaleminorn2` ✓; major `colh2o * absa + self+foreign + scalen2 * ka_mn2` ✓ matches WRF `:5138-5143`; constant `fracs = fracrefa(ig)` ✓ matches `:5144`. Upper: `corradj = 1 - 0.15 * pavel / 95.6` (always) ✓ matches `:5156`; foreign-only continuum + `kb_mn2` ✓ matches `:5160-5169`. Independently computed band-1 taug `max_abs=2.46, max_rel=4.39e-6` against oracle, with `reference=candidate=1.049e6` at max — strict-tolerance PASS. |
| R-19 | Per-band spot check: WRF band 9 (Python idx 8) | clean | **PASS — structural match** | `rrtmg_lw.py:1422-1433` vs WRF `:6583-6843` (`taugb9`). Lower: H2O/CH4 binary major `_major_binary_lower(absa, lower_idx0+js-1, lower_idx1+js1-1, speccomb, specparm, fs, ...)` using `_binary_params(colh2o, colch4, rat_h2och4, 8.0)` ✓; N2O minor `adjcoln2o * _minor_ratio(ka_mn2o_r[8], jmn2o, fmn2o, coef)` with `_adj_minor_column(coln2o, ..., 1.5, 0.5, 0.65)` matching WRF empirical N2O adjustment ✓; binary Planck `_frac_interp(fracrefa[8], jpl, fpl)` with `chi_ratio(1,6,9)` ✓. Upper: `colch4 * absb + kb_mn2o` with adjusted N2O ✓. Independently computed band-9 taug `max_abs=0.0274, max_rel=3.52e-6` — strict-tolerance PASS. |
| R-20 | Per-band spot check: WRF band 16 (Python idx 15) | clean | **PASS — structural match** | `rrtmg_lw.py:1493-1509` (`else` branch handling band == 15) vs WRF `:7742-7948` (`taugb16`). Lower via `_binary_band(15, ..., colh2o, colch4, (rat_h2och4, rat_h2och4_1), colch4, colh2o, 0.0, chi_ratio(1,6,6), 0.0)` — H2O/CH4 binary major + Planck binary interp ✓; Upper: `colch4 * _interp_four_rows_lw(absb, upper_idx0, upper_idx1, max(nspb,1), coef)` ✓ matches WRF `:7891` (pure CH4 H2O-free upper atmosphere); constant `fracrefb[15]` Planck ✓. Independently computed band-16 taug `max_abs=7.15e-6, max_rel=2.74e-6` — strict-tolerance PASS. |

**Conclusion of findings table**: every AC PASS at the artifact level; verifiability triple ALL PASS; 3 of 4 per-band spot checks (bands 1, 9, 16) confirm structural WRF fidelity; the 4th spot check (band 5) reveals a fixture-conditional simplification that I flag as R-12 — non-blocking with the operational T2 generalization gate as the binding closure. Two non-defect carries (R-16 merge artifact, R-17 environmental HLO diff) are documented for the manager closeout. No anti-pattern recurrences from prior M5-S3 cycles.

---

## 3. Independent LW PARITY verification (bit-stable reproduction)

I re-ran the worker's commands from a clean state inside `/tmp/wrf_gpu2_s3zzzzz`:

```
JAX_PLATFORMS=cpu PYTHONPATH=src JAX_ENABLE_X64=true python scripts/m5_run_rrtmg.py
PYTHONPATH=src python scripts/m5_gate_rrtmg.py
```

The freshly regenerated `artifacts/m5/tier1_rrtmg_lw_parity.json` reproduced `pass=true` with residuals matching the worker's committed values to the last meaningful digit:

| Field | Reproduced max_abs | Committed max_abs | Threshold | Margin |
|---|---:|---:|---:|---:|
| flux_down (W/m²) | 1.1972e-4 | 1.1974e-4 | 1.0 | 8,350× under |
| flux_up (W/m²) | 9.8367e-5 | 9.8367e-5 | 1.0 | 10,160× under |
| toa_up (W/m²) | 3.3128e-5 | 3.3088e-5 | 1.0 | 30,200× under |
| surface_down (W/m²) | 4.9349e-5 | 4.9661e-5 | 1.0 | 20,260× under |
| surface_up (W/m²) | 6.5293e-5 | 6.4703e-5 | 1.0 | 15,310× under |
| surface_emission (W/m²) | 3.0596e-5 | 3.0596e-5 | 1.0 | 32,680× under |
| column_net_heating (W/m²) | 7.6938e-5 | 7.6077e-5 | 1.0 | 12,990× under |
| toa_down (W/m²) | 0.0 | 0.0 | 1.0 | exact |
| heating_rate (K/s) | 3.5773e-8 | 3.5772e-8 | 1e-4 | 2,795× under |

Cross-field intermediate verifications also re-ran clean:
- `artifacts/m5/rrtmg_intermediate_validation.json`: `pass=true`, `lw.pass=true` (16/16 LW `taug` + `fracs` bands), `lw_cldprmc.pass=true` (16/16 bands), `lw_rtrnmc.pass=true` (16/16 bands).
- `artifacts/m5/rrtmg_per_band_status.json`: `lw_cldprmc_bands[].intermediate_gate=["PASS"]×16`, `lw_rtrnmc_bands[].intermediate_gate=["PASS"]×16`, `implementation_status=["FULL_BRANCH_ACCEPTED"]×16`.
- `artifacts/m5/tier2_rrtmg_invariants.json`: `pass=true` (longwave + shortwave heating-flux closure + energy conservation).

SW for comparison stays `tier1_rrtmg_sw_parity.json.pass=false` in this worker's branch (file-disjoint scope; the SW PARITY merge is a separate manager-level composition).

**The LW PARITY claim is independently confirmed and bit-stable (last-digit device noise only — CPU vs the worker's CUDA backend).**

---

## 4. ADR-009 amendment endorsement

**ENDORSED** for composition to `SW-PARITY, LW-PARITY`.

The branch-local amendment of ADR-009 status to `SW-PARTIAL/UNKNOWN, LW-PARITY after M5-S3.zzzzz` (line 5) is precisely correct for this LW worker's worktree in isolation:
- ADR-009 §Decision (line 22, the M5-S3.zzzzz paragraph) accurately describes the new LW coverage: MCICA cloud mask + KISS, `cldprmc` optical-depth assembly, climatological ozone profile, top-buffer temperature adjustment, `rtrnmc` source lookup, surface reflection, per-g-point flux recurrence — all with WRF source citations to `module_ra_rrtmg_lw.F:2236-3027` (MCICA + cldprmc), `:3253-3515` (rtrnmc), `:12329-13035` (wrapper buffers + ozone climatology).
- ADR-009 §Validation (lines 61-69) reports the exact tier1 LW residual table I independently reproduced, the FALLBACK gate status, and the honest `SW Tier-1 still false` carry — no overclaim.
- ADR-009 §Consequences (lines 73-75) explicitly carries the performance debt (`400 launch markers`, `3,943,207 byte LW HLO`) as a non-PARITY M5-closeout item.

Manager pre-decision context confirms the reconciliation path: when M5-S3.zzzz SW PARITY (already merged via `dc03d04`) composes with M5-S3.zzzzz LW PARITY (this sprint), ADR-009 amends to `SW-PARITY, LW-PARITY` and full M5 RRTMG PARITY is achieved for the analytic-rrtmg-*-column-v1 fixtures.

**One housekeeping nit** (cosmetic, not blocking): ADR-009 line 4 attribution lists only `M5-S3.x worker amendment (Codex gpt-5.5); M5-S3.y non-acceptance update (Codex gpt-5.5); M5-S3.zzz LW closeout update (Codex gpt-5.5); M5-S3.zzzzz LW cldprmc/rtrnmc parity update (Codex gpt-5.5)`. This skips M5-S3, M5-S3.z, M5-S3.zz, M5-S3.zzzz attributions. Not blocking; the manager closeout commit can rewrite the line to include all 8 cycles for full provenance.

---

## 5. M5 + M6 dispatch impact

| Sprint state | SW Tier-1 | LW Tier-1 | T2 24h drift (est.) | M6-S8 binding T2 gate |
|---|---|---|---:|---|
| Pre-S3.zzzz / S3.zzzzz | FAIL (87 W/m²) | FAIL (60 W/m²) | 1-3 K | BLOCKED |
| Post-S3.zzzz (SW only) | PASS | FAIL | 0.7-1.5 K (LW-dominated 24h drift) | BLOCKED |
| **Post-S3.zzzzz composed with S3.zzzz (this sprint, after composition)** | **PASS** | **PASS** | **< 0.5 K (analytic-fixture estimate)** | **UNBLOCKED candidate** subject to M6-S5 ADR-007 verdict prereqs (dycore cap lift, end-to-end wall, denominator selection) and operational verification |
| After M5 closeout (launch budget Pareto pass) | PASS | PASS | < 0.5 K | UNBLOCKED + perf-ready |

After the M5-S3.zzzz + M5-S3.zzzzz composition lands cleanly on `main`, ADR-009 → full `SW-PARITY + LW-PARITY` ⇒ **M6-S8 operational T2 binding gate becomes dispatchable** per the manager's pre-decision context. The 454-launch debt (54 SW + 400 LW) becomes the M5 closeout / Pareto-frontier optimization scope and is **not a parity blocker** per both the SW and LW worker contracts.

**Important M6-S8 critic instruction (carried forward from R-12)**: the band-5 (and likely other binary-species bands 3, 7, 13, 14 with `nspa=9` and binary major-species interpolation) lower-atmosphere code paths in `rrtmg_lw.py` are partially fixture-conditional. For the analytic-rrtmg-lw-column-v1 fixture, `specparm < 0.125 + speccomb ≈ colh2o` holds and the simplified path matches WRF. For real Canairy atmospheres with realistic H2O/CO2 binary mixing in band 5 and varying O3 columns, the simplified path may diverge. The M6-S8 critic should: (a) include band-5-specific T2 24h drift attribution in cloudy-PBL scenarios; (b) if T2 drift > 0.5 K in cloudy regimes, recommend a follow-up sprint to harden these bands to full WRF binary-species interpolation; (c) treat the analytic-fixture residual of 1.2e-4 W/m² as a **lower bound** on real-atmosphere LW residual, not a binding ceiling.

**M7 plan**: per recent commit history (S0-S8 plan with AIFS integration), M7 is downstream of M6 close, so any LW band-5 generalization debt cascades into M7. No M7 schema change required yet; flag for the M7 critic when M6-S8 closes.

---

## 6. Worker diagnostic methodology assessment

The M5-S3.zzzzz worker confronted the three M5-S3.zzz reviewer-flagged hypotheses (`cldprmc` cloud-optics, `rtrnmc` source recurrence, `tfn_tbl` lookup) directly against the new intermediate-oracle dumps. The actual residual closure came from a **stack of WRF-faithful additions** rather than a single fix — this is the third sprint in the cycle to validate the intermediate-oracle methodology:

1. WRF wrapper top-buffer temperature adjustment (Cavallo's contribution, `:12329-12378`) — **was missing in M5-S3.zzz** and contributed several W/m² to top-of-atmosphere downward flux residual.
2. WRF `INIRAD/O3DATA` climatological ozone profile (`:12842-13035`) — the M5-S3.zzz LW path used a parametric o3 profile; the harness oracle uses the explicit `O3DATA` climatology, so JAX had to follow.
3. MCICA stochastic cloud generator + KISS pseudo-random stream (`:2236-2706`) — pressure-seeded with `int(pmid)*1e9` truncation; the worker correctly ported the KISS recurrence and disclosed the XLA fusion sensitivity around the pressure seed (R-13).
4. WRF `cldprmc` optical-depth assembly (`:2738-3027`) — cloud optical depth scaling per band.
5. WRF `rtrnmc` source recurrence (`:3253-3515`) — three-regime structure (linear small-OD, linear-gas + table-cloud, full-table) with `tfn_tbl` correction. Worker's `_lw_rtrnmc_layer_terms` (`rrtmg_lw.py:1694-1777`) faithfully ports all three regimes — I cross-checked the case-A/B/C dispatch and the `gassrc / bbdtot / atrans / atot / bbugas / bbutot` calculations against WRF `:3320-3395`.

The **only structural simplification I found** is R-12's band-5 (and likely sibling bands') simplified major-species path. Worker did not over-claim — `worker-report.md:7-8` honestly limits the claim to "LW-PARITY for the current strict Tier-1 fixture." This is the right level of honesty: the broadband Tier-1 number is **real** for this fixture; the **operational generalization** is M6-S8's job, not M5-S3.zzzzz's.

**Methodology is sound and the M5-S3.z → S3.zz → S3.zzz → S3.zzzz → S3.zzzzz pattern is now proven end-to-end for BOTH halves** (SW broadband closed via M5-S3.zzzz; LW broadband closed via M5-S3.zzzzz). The intermediate-oracle dollar-cost (3 cycles of harness extension + per-band validators for each of SW and LW) paid off with two PARITY-class results.

---

## 7. Summary judgment

M5-S3.zzzzz worker delivered the **second PARITY-class result** in the 8-sprint M5-S3 RRTMG arc and completes the LW broadband side of M5 RRTMG PARITY. All 9 strict Tier-1 LW per-field residuals pass under the binding `≤1 W/m²` bar with 8,350-32,680× margin; heating-rate residual is 2,795× below 1e-4 K/s. All 16 LW bands clear the intermediate `cldprmc_lw` + `rtrnmc` gates at `abs ≤ 1e-4 + rel ≤ 1e-3`. WRF wrapper buffer-temperature and climatological-ozone paths added correctly per `module_ra_rrtmg_lw.F:12329-12418`. MCICA stochastic cloud mask + KISS pressure-seeded generator ported per `:2236-2706` with honest XLA-fusion sensitivity disclosure. `cldprmc` optical-depth assembly per `:2738-3027`. `rtrnmc` three-regime source recurrence per `:3253-3515` with `tfn_tbl` lookup correction. Top-buffer T adjustment per `:12329-12378` (Cavallo's contribution).

Verifiability triple: §1.1 PASS (`nm` symbols for cldprmc_lw + rtrnmc + setcoef + taumol + rrtmg_lw all present in harness binary); §1.2 PASS (zero clip-pinning across 18 LW oracle arrays); §1.3 PASS (no `min(raw, cap)` fudge; 454 raw == 454 reported; gate honestly FALLBACK driven by SW + launch debt, not LW correctness). All 8 AC PASS (AC1 cldprmc_lw dumps; AC2 rtrnmc dumps; AC3 NPZ extended with 7 new fields preserving the 36 prior; AC4 5 LW validators added; AC5 16/16 bands FULL_BRANCH_ACCEPTED; AC6 strict Tier-1 LW PASS; AC7 SW no-regression empty diff; AC8 ADR-009 honestly amended). No M5-S3 → S3.x → ... → S3.zzzz anti-pattern recurrences. Independent re-run of `m5_run_rrtmg.py` reproduces `tier1_rrtmg_lw_parity.json.pass=true` bit-stably (last-digit device noise only).

Adversarial findings: R-12 band-5 (and likely sibling binary-species bands) lower-atmosphere code path is fixture-conditional — the simplified `colh2o * absa(js=1) + zero-padded minors + constant Planck` matches WRF taugb5 only because the analytic fixture has `specparm < 0.125 + speccomb ≈ colh2o`; **non-blocking** because intermediate oracle PASSES at strict tolerance for this fixture and the M6-S8 operational T2 binding gate against the Canairy CPU baseline is the binding generalization test per `[[feedback_validation_philosophy]]`; carry forward as M6-S8 critic instruction. R-13 MCICA seed sensitivity disclosed honestly; benign for current claim; cleanup path is a quiet refactor. R-14 400 LW launches + 3.9 MB LW HLO is honest performance debt parallel to SW's 443 launches; M5 closeout / Pareto-frontier scope per the contract, not a parity blocker. R-15 WRF top-buffer T adjustment is a correct WRF port (Cavallo's contribution), not a band-aid. R-16 the `--theirs` merge of `tests/test_m5_rrtmg_intermediate_oracles.py` dropped SW worker's `validate_sw_cldprmc_*` + `validate_sw_spcvmc_*` validator definitions and test imports — closeout chore for the manager (the SW worker's branch has them; cherry-pick during the SW+LW PARITY composition). R-17 the single pytest failure (`test_rrtmg_hlo_diff_artifacts_empty_when_present`) is environmental (CPU vs CUDA backend HLO formatting drift), not a worker defect.

**Final decision: ACCEPT-WITH-MINOR-FOLLOWUPS** (effectively `ACCEPT-LW-PARITY-FOR-ANALYTIC-FIXTURE` per ADR-009 line 5 wording).

ADR-009 amendment to `SW-PARTIAL/UNKNOWN, LW-PARITY` is endorsed for this branch in isolation, with reconciliation to `SW-PARITY, LW-PARITY` upon composition with the parallel M5-S3.zzzz SW PARITY merge already on `main`. M5 dispatch impact: after composition, ADR-009 → full PARITY ⇒ M6-S8 operational T2 binding gate becomes dispatchable (subject to M6-S5 ADR-007 verdict prereqs and operational verification). M6-S8 critic must inherit R-12 as a tagged instruction. The 454-launch debt remains the M5 closeout / Pareto-frontier optimization scope.

Worker actions on accept: none required — sprint can close.

Manager actions on accept: (i) close the sprint with proof-object record; (ii) compose with the M5-S3.zzzz SW PARITY merge already on `main` and amend ADR-009 to `SW-PARITY, LW-PARITY`; (iii) cherry-pick the SW worker's `validate_sw_cldprmc_*` + `validate_sw_spcvmc_*` validator definitions from the SW branch into `rrtmg_intermediate_oracles.py` and restore the corresponding test imports in `tests/test_m5_rrtmg_intermediate_oracles.py` (R-16); (iv) carry R-12 (band-5 fixture-conditional simplification), R-13 (MCICA XLA-fusion cleanup), R-14 (454-launch perf debt) as non-blocking follow-ups attached to M6 dispatch and M5 closeout respectively; (v) ratify M6-S3..S8 unblock per the prior tracker line and dispatch M6-S8 binding T2 validation; (vi) optionally patch ADR-009 line 4 attribution to include all 8 M5-S3 cycle contributions (cosmetic only).

AC1 PASS (cldprmc_lw + MCICA dumps). AC2 PASS (rtrnmc per-g-point dumps). AC3 PASS (NPZ extended with 7 new LW fields, 36 prior fields preserved). AC4 PASS (5 LW validators added at contract tolerances). AC5 PASS (16/16 bands FULL_BRANCH_ACCEPTED at intermediate gates). AC6 PASS (strict Tier-1 LW broadband, all 9 fields, 8,350-32,680× margin). AC7 PASS (SW no-regression empty diff). AC8 PASS (ADR-009 amended honestly). Verifiability-1, -2, -3 all PASS. M6-S8 binding T2 gate UNBLOCKED upon composition with M5-S3.zzzz SW PARITY merge.

---

**Reviewer signature**: Claude Opus 4.7 xhigh — M5-S3.zzzzz LW cldprmc + rtrnmc + broadband PARITY reviewer pass complete.
