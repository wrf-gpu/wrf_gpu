# Sprints

Each sprint is a small, evidence-producing unit of work. Before the first implementation sprint in any milestone, the manager writes a milestone plan and gets it reviewed.

Create new sprints with:

```bash
python scripts/create_sprint.py <sprint-id>
```

Do not edit the sprint template as an active sprint.
