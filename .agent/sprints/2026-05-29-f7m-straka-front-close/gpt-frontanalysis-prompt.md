You are GPT-5.5 xhigh on an INDEPENDENT WRF-source bug-hunt for the wrf_gpu2 JAX dycore. The dycore is one localized residual from done: the **Skamarock warm bubble PASSES 6/6** (core dynamics validated), but the **Straka density current** still detonates ~270–300 s at the **descending sharp cold-front touchdown**. An Opus agent is building WRF `em_grav2d_x` ground truth + fixing in parallel; you are the second angle: find, from WRF source, what specific operator/coupling at the sharp cold front JAX is getting wrong.

READ + ANALYZE + RUN read-only probes in the detached worktree `/tmp/wrf_gpu2_straka_audit` (`PYTHONPATH=src taskset -c 0-3 python ...`, cuda:0, fp64). Do NOT edit source. ONLY write the findings file named below.

## Symptom (precise, from F7K/F7L)
Straka (dx=dz=100 m, −15 K cold pool, ν=75 m²/s const-K on u,v,w,θ now wired): finite to ~240 s then NaN by ~300 s. At 240 s: **max|w| = 23.9 m/s (Straka ν=75 reference is ~12–18)** AND the gust front has only reached ~2.65 km from center (it should be propagating faster — front-speed deficit). So: **excess vertical velocity + sluggish lateral cold-pool spreading** at the descending front. Already verified WRF-correct and NOT the cause: emdiv=0.01, smdiv=0.1, w_damping=1, damp_opt=3, acoustic CFL=0.035, and the benchmark ν=75 diffusion (incl. w). The warm bubble (smooth, weak +2K) passes — so the defect is specific to the STIFF sharp-front regime.

## Charter — find the cold-front operator/coupling defect (WRF source = ground truth)
WRF: `/home/enric/src/canairy_meteo/Gen2/artifacts/wrf_gpu_src/WRF/dyn_em/` and the pristine tree `/home/enric/src/wrf_pristine/WRF`.
1. **Gust-front horizontal PGF → cold-pool u-outflow.** The cold pool's density excess drives a horizontal pressure gradient that propagates the gust front (front speed ∝ √(g'·H)). A front-speed deficit suggests the horizontal PGF or the cold-pool mass/buoyancy coupling at the front is under-forcing u. Check JAX's `advance_uv` horizontal-PGF + the large-step PGF (rk_addtend_dry) against WRF for a strong horizontal θ' gradient. Is there a map-factor, mass-coupling, or `cqu` term that's right for smooth fields but wrong at a sharp front?
2. **Descending-front w / excess vertical velocity.** Why is max|w| ~2× the reference at the front? Is the vertical PGF / buoyancy (pg_buoy_w) over-forcing w at the sharp descending gradient, or is the w-diffusion / lower-BC at the descending front (w→0 at surface, free-slip) mis-set? Check the w lower boundary condition (`module_small_step_em.F` advance_w surface BC) and the `pg_buoy_w` behavior at a sharp Δθ.
3. **Sharp-front scalar advection.** Does WRF's scalar (θ) advection for this case use a monotonic/positive-definite flux limiter or a specific odd-order upwind that adds implicit dissipation at sharp gradients, which JAX's WS5/3 flux-form might be missing (→ dispersive over/undershoot at the front feeding the w runaway)? Check `module_advect_em.F` advect_scalar options + the em_grav2d_x namelist `moist_adv_opt`/`scalar_adv_opt`/`h_sca_adv_order`.
4. RUN a probe: integrate Straka, trace at the front column the u-outflow, w, θ', and the horizontal-PGF vs buoyancy terms over 0–280 s; identify which term diverges from physical expectation first.

## Output
Write to EXACTLY `/home/enric/src/wrf_gpu2/.agent/sprints/2026-05-29-f7m-straka-front-close/gpt-frontanalysis-findings.md`: (1) probe evidence at the front; (2) the single most likely cold-front operator/coupling defect with JAX file:line vs WRF file:line; (3) concrete fix; (4) a falsifiable check (e.g. "front speed matches √(g'H); max|w| drops to 12–18; finite to 900s"). End with `F7M_FRONTANALYSIS_COMPLETE`. Do not modify source.
