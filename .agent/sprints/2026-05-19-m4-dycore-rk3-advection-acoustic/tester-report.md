# Tester Report — M4 Dycore RK3 Advection Acoustic (attempt 2)

Role: sonnet-test-engineer (Claude Opus 4.7 xhigh, executed as Opus 4.7).
Sprint: `2026-05-19-m4-dycore-rk3-advection-acoustic`.
Branch: `tester/sonnet/m4-dycore-rk3-advection-acoustic`.
Attempt: 2 (worker fix-cycle after reviewer Reject on attempt 1).

## Summary

I re-ran every validation command in the amended sprint contract from a clean
shell, added 25 attempt-2-specific adversarial tests to
`tests/test_m4_tester_adversarial_attempt2.py`, and independently spot-checked
each of the 7 fix-cycle items (5 blocker-class, 2 major-class, 1 minor). The
implementation reproduces from cold, all 384 tests pass, the HLO debug-vs-stripped
diff is the canonical empty file, and every attempt-1 blocker that I or the
reviewer flagged is now resolved with non-trivial evidence rather than a
self-check.

## Validation commands re-run (clean shell)

| Command | Result |
|---|---|
| `python scripts/validate_agentos.py` | `ok: true`, 31 files, 13 skills |
| `python scripts/check_m1_done.py` | `ok: true`, 3 sprints closed |
| `python scripts/check_m2_done.py` | `ok: true`, 6/6 candidates, 7 sprints closed |
| `python scripts/check_m3_done.py` | `ok: false` — pre-existing missing M3 `reviewer-report.md` (not introduced by this sprint; out of tester scope) |
| `JAX_ENABLE_X64=True python scripts/m4_run_dycore.py` | dycore_profile / transfer_audit / spacetime_budget regenerated; `kernel_launches_per_step=24`, `wall_time_per_step_us≈632.3`, post-init transfers = 0, `temporary_bytes_per_step=0` |
| `JAX_ENABLE_X64=True python scripts/m4_run_validation.py` | tier1 `max_abs_err=0.0` against upwind5 sibling fixture; tier2 `mass_residual_relative=1.94e-16`, `final_state_differs_from_initial=true`, `max_theta_delta≈3.28 K`; tier3 `observed_order=4.65` vs expected 3.0 |
| `JAX_ENABLE_X64=True python scripts/m4_m5_gate_dryrun.py` | `gate_status: trip` on `kernel_launches_per_step` (24 > 10); local/register metrics emitted as JSON `null` (not 0) per AC #6.fix |
| `JAX_ENABLE_X64=True python scripts/m4_hlo_diff.py` | `dycore_step_debug_vs_stripped.diff` 0 bytes, SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` (canonical empty digest) |
| `ls -l artifacts/m4/hlo_dump/dycore_step_debug_vs_stripped.diff` | size 0 confirmed |
| `python -m json.tool` on all seven M4 JSON proof objects | all parse cleanly |
| `pytest -q` (full suite incl. 25 new attempt-2 adversarial tests) | `384 passed in 257.77s` |

## HLO Identity Verification (AC #10.2)

- Tester command: `JAX_ENABLE_X64=True XLA_PYTHON_CLIENT_PREALLOCATE=false python scripts/m4_hlo_diff.py` from a fresh shell.
- File: `artifacts/m4/hlo_dump/dycore_step_debug_vs_stripped.diff`.
- Byte size produced by the tester run: **0**.
- SHA-256 produced by the tester run: **`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`** (canonical empty-file digest, matches the worker report and the attempt-1 tester result).
- Cross-check: the on-disk `dycore_step_production.txt` (header line `HloModule jit_step`) is distinct from `dycore_step_debug_stripped.txt` (header line `HloModule jit_step_debug_stripped`), proving the attempt-1 `m4_hlo_diff.py:55` typo (which had written `prod` to both files) is fixed. The empty diff therefore certifies that two *distinct source files* compile to the same normalized HLO — exactly what AC #2.4 demanded.

## Blocker-class fix verification (independent spot-checks)

| Fix | Attempt-1 evidence | Attempt-2 reproduction (my run) |
|---|---|---|
| **AC #1.1.fix** RK3 constant tendency | reviewer saw delta=11 for dt=6 const=1 (expected 6) | I ran `step()` with zero velocities and constant theta tendency at dt∈{0.5, 2, 6, 10}, const∈{-1.25, 0.7, 1.0, 11.0}; max residual `1.07e-14`. Implementation now uses `rk3_stage(origin, stage_state, ...)` advancing from RK origin, not chained s_k. Multi-step run also linear. |
| **AC #3.fix** Real tier-1 oracle | reviewer: tier-1 ran the M1 generator against its own output (tautology) | Sibling fixture `analytic-stencil-3d-upwind5-v1` generated by `_upwind5_payload` in `scripts/generate_analytic_fixtures.py:121`, manifest checksum matches on-disk `.npz`. Tier-1 now calls `advect_mass_scalar` directly; `max_abs_err=0.0` against `phi_next_upwind5`. Operator string now reads "dycore 5th-order horizontal periodic and 3rd-order vertical no-wrap upwind". |
| **AC #4.fix** Tier-2 nontrivial trajectory | attempt-1 IC had `u=v=w=0` and uniform `p` — every prognostic frozen for 100 steps | New IC sets `u = 5 m/s` and seeds a Gaussian theta bump (300 + 8·exp(-...)). Over 10 steps my `run()` re-check produced `max(|theta_final - theta_initial|) = 1.559 K`; the 100-step contract artifact reports `3.28 K` and `mass_residual=1.94e-16`. `final_state_differs_from_initial=true` is now a JSON field and a pass-condition. |
| **AC #5.fix** Tier-3 through public dycore | attempt-1 used `ddx4_centered` + a local 1D RK helper, bypassing `step`/`run` | `tier3.py` now `from gpuwrf.dynamics.step import run` and runs the full public dycore at (dx,dt) = (2000,2), (1000,1), (500,0.5) on a 1D-flat 4×4 grid; observed order 4.65 (theory ≥ 3.0; smooth case + RK3 + 5H upwind beats the floor). |
| **AC #1.2.fix** Velocity cross-advection | attempt-1: `advect_u_face` only x self-advection, no v·∂u/∂y or w·∂u/∂z | `src/gpuwrf/dynamics/advection.py:202-238` now interpolates v_mass→u-face and w_mass→u-face (`_mass_to_u_face`, etc.) and applies the full 5H/3V operator. `test_velocity_advection_includes_cross_terms` asserts non-zero cross-term contributions. |
| **AC #2.4.fix** Hand-stripped HLO sibling | attempt-1: `step_stripped_reference` was inside `step.py` and dispatched through the same `_step_impl(..., False)` | `src/gpuwrf/dynamics/step_debug_stripped.py` now exists as a separate file with `assert_*` / `snapshot` calls physically deleted (grep confirms absence). `step_debug_stripped()` produces bitwise-identical output to `step(..., debug=False)` over 1- and 5-step runs (new tests). The sibling is NOT exported from `dynamics/__init__.py`. |
| **AC #5.1.fix** `m4_hlo_diff.py` typo | attempt-1: line 55 wrote `prod` to the stripped artifact | `m4_hlo_diff.py:58` now writes `stripped`. On-disk `dycore_step_debug_stripped.txt` contains `HloModule jit_step_debug_stripped`; the production text contains `HloModule jit_step`. New test `test_m4_hlo_diff_writes_stripped_hlo_to_stripped_artifact` locks the source-line invariant. |
| **AC #6.fix** M5 dry-run null metrics | attempt-1: `local=0, registers=0` hard-coded | `scripts/m4_m5_gate_dryrun.py:34-35` now sets `local=None, registers=None`; the JSON serialiser emits `null`. Rationale string documents "unavailable from HLO … pending ncu/cuobjdump follow-up". Trip remains on `kernel_launches_per_step=24>10` and is reporting-only per contract. |

## Adversarial tests added (25 tests in `tests/test_m4_tester_adversarial_attempt2.py`)

I left the attempt-1 `tests/test_m4_tester_adversarial.py` in place (the
worker already inverted the no-op-IC and tier-1 tautology asserts there).
New attempt-2 tests target each blocker fix directly:

**RK3 constant-tendency regression net (2 tests)**
- `test_rk3_constant_theta_tendency_integrates_to_dt_times_tendency` —
  parametrised over (dt, const) ∈ {(2,1), (6,0.7), (10,-1.25), (0.5,11)};
  asserts `max(|theta_after - theta_before - dt·const|) ≤ 1e-12` through
  the public `step()` API. Locks the reviewer's spot-check failure.
- `test_rk3_multistep_run_constant_theta_tendency_integrates_linearly` —
  same property through 7 steps of `run()`; asserts `N·dt·const` to 1e-10.

**Tier-1 sibling-fixture integrity (3 tests)**
- `test_upwind5_sibling_fixture_checksum_matches_manifest` — recomputes the
  sha256 of `analytic-stencil-3d-upwind5-v1.npz` and compares against the
  YAML manifest. Prevents silent drift between sample and manifest.
- `test_upwind5_sibling_phi_next_matches_dycore_kernel_zero_error` —
  directly evaluates `advect_mass_scalar` against the committed `phi_next_upwind5`;
  asserts `max_abs_err ≤ 1e-12`. Independent of the tier-1 wrapper.
- `test_tier1_artifact_advertises_dycore_operator_not_m1_reference` —
  locks the artifact operator string to contain "5th-order", "3rd-order",
  "upwind" and to NOT contain "centered" / "centred". Prevents regression
  to the attempt-1 M1-reference tautology.

**Tier-2 evidentiary strength (3 tests)**
- `test_tier2_ic_has_nonzero_u` — IC must have `max(|u|) > 0`, not the
  attempt-1 `u=v=w=0` no-op.
- `test_tier2_artifact_records_final_state_differs_from_initial` — locks
  `final_state_differs_from_initial=true`, `max_theta_delta>0.1`,
  `mass_residual≤1e-10`, `qv_violations=0`, `nan_inf=0`, `pass=true`.
- `test_tier2_qv_violation_counter_accumulates_over_trajectory` — drives
  `qv` strongly negative via a constant tendency over 3 steps and asserts
  the trajectory-wide `qv_positivity_violations` counter is non-zero. Confirms
  the counter is a trajectory accumulator (`jax.lax.scan` carries it through
  each body call), not a final-state-only count.

**Tier-3 public-API enforcement (2 tests)**
- `test_tier3_source_uses_public_run_and_not_centered_helper` — static
  source-text guard: `tier3.py` must import `from gpuwrf.dynamics.step
  import run` and must NOT mention `ddx4_centered` or `advect_mass_scalar`
  (no private-kernel bypass).
- `test_tier3_convergence_record_observed_order_meets_expected_minus_half`
  — runs `convergence_record()` and asserts errors monotonically decrease
  across levels AND `observed_order ≥ expected_order - 0.5`.

**Literal hand-stripped sibling (3 tests)**
- `test_step_debug_stripped_is_a_separate_source_file` — file existence
  and grep absence of `assert_finite` / `assert_physical_bounds` /
  `snapshot(` / `from gpuwrf.debug`. The "intent-equivalent same-code-path
  sibling" attempt-1 escape hatch is now closed at the source level.
- `test_step_debug_stripped_matches_step_debug_false_bitwise` — single step
  output equality between the production `step(..., debug=False)` and
  `step_debug_stripped(...)`; the stripped sibling is a *real* fork, not
  a syntactic alias, so the bitwise equality is non-trivial evidence.
- `test_run_debug_stripped_matches_run_debug_false_bitwise` — same over 5
  jit-scanned steps via `run_debug_stripped(...)`.

**HLO diff artifact integrity (2 tests)**
- `test_m4_hlo_diff_writes_stripped_hlo_to_stripped_artifact` — static
  source guard on `scripts/m4_hlo_diff.py`: every line writing to
  `dycore_step_debug_stripped.txt` must pass the `stripped` variable.
- `test_production_and_stripped_hlo_artifacts_are_distinct_files` —
  on-disk `dycore_step_debug_stripped.txt` must contain
  `jit_step_debug_stripped` (and the production file must not), which
  was untrue in attempt 1 because of the typo.

**M5 gate null-vs-zero distinction (2 tests)**
- `test_m5_gate_dryrun_unknown_metrics_are_json_null_not_zero` —
  `local_memory_bytes_per_kernel` and `registers_per_kernel` MUST be `None`
  (JSON `null`), not `0` (which would be falsely read as "measured zero" by
  a downstream gate).
- `test_m5_gate_dryrun_rationale_documents_unavailable_metrics` — the
  rationale string must contain "unavailable" or "pending" or "ncu".

**Constitutional gate (debug branch absence) — re-verified at dycore scale (1 test)**
- `test_production_hlo_has_no_debug_branch_ops` — production HLO must not
  contain `is-finite`, `debug.callback`, `io_callback`, or `checkify`. I
  routed the compile through a *named-without-the-forbidden-substring*
  helper because JAX's source-info embeds the calling Python frame's
  qualname, which otherwise creates a self-reference false positive (a
  trap I hit on my first draft of this test — documented inline).

**Hot-path discipline (1 test)**
- `test_production_hlo_size_does_not_explode_with_grid` — order-of-magnitude
  guard against future regressions in lowered HLO size at unit scale.

**Vertical no-wrap boundary (1 test)**
- `test_vertical_upwind_uses_no_wrap_at_lid_and_floor` — a spike at z=0
  with upward velocity must NOT teleport to z=top within one step. Locks
  the no-wrap vertical advection contract; the rigid lid/floor cap was a
  worker fix in attempt 2.

**Determinism (1 test)**
- `test_step_is_deterministic_across_n_acoustic_values` — two compile
  caches (`n_acoustic=1` vs `n_acoustic=4`) must each produce bitwise-stable
  results across repeated calls.

**Scope discipline (1 test)**
- `test_step_debug_stripped_is_not_exported_in_dynamics_init` — AC #2.4
  required the stripped sibling to be internal-only.

**Total: 25 new tests added on top of the attempt-1 file's 29.** Full suite
size grew from 359 (worker baseline) to 384.

## Fixtures used

- `fixtures/manifests/analytic-stencil-3d-upwind5-v1.yaml` +
  `fixtures/samples/analytic-stencil-3d-upwind5-v1.npz` — the new sibling
  fixture introduced by attempt 2. I verified the sample sha256
  (`d356bcb90f2360b302f9a4821fc4edafdff00d9934a2fb6355df7c653f9dfaef`)
  matches the manifest's declared checksum and that the dycore kernel
  reproduces `phi_next_upwind5` bit-exact.
- `fixtures/manifests/analytic-stencil-3d-advdiff-v1.yaml` — still
  exercised by `test_fixture_reference_wrapper_matches_committed_phi_next`
  in `tests/test_m4_advection.py`; tier-1 itself no longer compares
  against this M1 centred-reference fixture.
- Worker's `density_current_state` IC in `gpuwrf.validation.tier2` — now
  produces a nontrivial trajectory (u=5 m/s, Gaussian theta bump).
- No new binary fixtures committed by tester (per the universal rule).
- `data/` symlink to `/mnt/data/wrf_gpu2/` confirmed; HLO scratch dumps land
  there per AC #7.2.

## Gaps and risks (residual)

1. **Mass conservation surrogate is `theta_total`, not `mu·rho` mass.** The
   tier-2 artifact uses `mass_field: theta_total` as the conservation
   diagnostic. For the chosen linear-tracer-translation IC with `u=5 m/s,
   v=w=0, p=0` this is a reasonable conserved scalar (the 5H/3V upwind
   operator is conservative in flux form), and the artifact records
   `1.94e-16` ≪ tolerance `1e-10`. But it is not the WRF-canonical
   `Σ μ` mass conservation. Worker acknowledged the simpler setup in
   `maintainability.md`; the reviewer should decide whether
   tracer-translation mass conservation is sufficient evidence for M4
   closure or whether a follow-up sprint should swap to a hydrostatic
   density-current IC.

2. **Acoustic substep uses reduced-proxy constants** (`c² = 1.0`,
   `pressure_coupling = 1e-3`). The forward-backward acoustic operator
   *runs* (and is exercised by the tier-2 100-step trajectory and the
   `n_acoustic=4` HLO dump), but its physical fidelity is *not* claimed by
   any tier here. Worker documented this in `maintainability.md`. Tier-3
   exercises only pure linear advection (n_acoustic=1 effectively a no-op
   acoustic). The M5 entry sprint should add a small-amplitude sound-wave
   phase-speed test to upgrade the acoustic substep's evidence.

3. **M5 gate trips on `kernel_launches_per_step=24 > 10`.** Per AC #6 this
   is reporting-only and routes to ADR-001's per-scheme Triton fallback if
   needed. Manager problem, not worker; flagged for the reviewer.

4. **`check_m3_done.py` failing** on a pre-existing missing M3
   `reviewer-report.md`. Out of tester and worker scope; manager
   lifecycle bookkeeping.

5. **Cross-term velocity advection is now present but is *not* directly
   validated against a manufactured solution.** `test_velocity_advection_includes_cross_terms`
   only asserts the cross-term magnitude is non-zero; it does not assert
   it equals a closed-form analytic reference. The 1D linear-advection
   tier-3 case happens to set `v=w=0`, so it does not exercise the new
   cross terms. A follow-up sprint should add a 2D shear-flow tier-3
   convergence test to lock the cross-term accuracy at order 5.

## What I did *not* do

- I did not modify `src/`, the M4 scripts, the worker's tests
  (`test_m4_*.py` other than the new `_attempt2.py`), or any governance
  file. Tester scope was respected.
- I did not patch the residual gaps in §1–§5 above — those are reviewer
  or follow-up-sprint decisions.
- I did not commit binary fixtures.
- I did not run `nsys` / `ncu` (unavailable on this workstation per
  worker report and as documented in `m4_m5_gate_dryrun.json` rationale).

## Decision

Decision: **Accept** — for the worker-owned implementation paths.

Justification: every blocker-class and major-class fix the attempt-1
reviewer required is now resolved with non-trivial, independently
reproducible evidence:

- RK3 constant-tendency integrates exactly (`max residual 1.07e-14` over
  the parameter sweep I ran).
- Tier-1 exercises the dycore's actual 5H/3V upwind kernel against a
  sibling NumPy oracle with matching sha256, not the M1 generator's
  self-image (`max_abs_err = 0.0`).
- Tier-2 IC is a non-trivial tracer translation: `max_theta_delta = 3.28 K`
  over 100 steps, mass residual `1.94e-16`, qv-positivity and finiteness
  counters trajectory-wide.
- Tier-3 runs through the public `run(...)` dycore (no centered-reference
  bypass), three resolution levels, observed order 4.65 ≥ expected − 0.5.
- The HLO debug-vs-stripped diff is generated from two *distinct source
  files*; the diff artifact is the canonical empty file and its sha
  matches; the stripped artifact on disk self-identifies as the stripped
  module (attempt-1 typo gone).
- The M5 gate emits JSON `null` for unavailable register/local-memory
  metrics, so a downstream reader cannot confuse "unknown" with
  "measured zero".
- All 384 tests pass; the 25 new attempt-2 adversarial tests lock each fix
  against future regression; existing attempt-1 adversarial tests have been
  inverted by the worker to assert the *new* correct behaviour.

Residual gaps (§1–§5) are evidentiary debt for a future M4.x or M5 sprint,
not blockers for M4 closeout: a single comprehensive density-current
hydrostatic-balanced IC + a small-amplitude sound-wave test + a 2D
shear-flow convergence test would close all of them, and none of them
contradict any current proof object.

Tester scope is complete. Exiting cleanly.
