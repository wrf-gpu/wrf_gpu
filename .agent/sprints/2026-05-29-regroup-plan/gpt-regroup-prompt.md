You are GPT-5.5 xhigh. Produce an INDEPENDENT, opinionated, concrete revised roadmap for the fastest, most-efficient path from the CURRENT state to the project goal. This is a "blind plan" — the manager (Opus 4.8) is drafting one in parallel and will synthesize a super-plan from both. Be specific and decisive; do not just validate the existing plan.

## The goal (binding, from PROJECT-RESET-PLAN-FINAL.md)
A JAX-native GPU port of WRF v4 delivering Canary L2/L3 24-72 h RMSE on T2/U10/V10 **statistically equivalent to CPU WRF v4 under TOST** at predeclared margins on a **≥30-case seasonal ensemble**, while preserving **≥10× speedup** vs 28-rank CPU WRF on a single RTX 5090. Endpoint: a real WRF v4 GPU port, near-identical RMSE on all values vs WRF, no shortcuts, GPU-efficient, massive local speedup.

## CURRENT STATE (changed materially from the reset plan)
The reset plan (`.agent/decisions/PROJECT-RESET-PLAN-FINAL.md`, milestones M8-M23) ASSUMED the dycore was done. It wasn't — the old "bitwise parity" was a JAX-vs-JAX self-compare; the operational dycore was missing ~7 WRF operators. So an unplanned dry-dycore rebuild (the "F7" series, A-N) consumed this session. Status NOW:
- **Dry dynamical core ~95% done, validated against WRF ground truth**: Skamarock warm bubble PASSES 6/6 vs published reference; Straka density current matches WRF to ~3% through 180s with ONE surgical residual at cold-pool touchdown (a per-substep WRF diff is closing it now). All major operators (acoustic small-step core, implicit w/ph solve, calc_coef_w/epssm, flux-form scalar+momentum advection, MUT/MUTS mass, calc_p_rho_phi, rhs_ph/ph_tend, ν=75 diffusion) verified WRF-faithful. Read `proofs/f7/DYCORE_STATUS.md`.
- **Reusable assets built this session**: (a) pristine WRF v4.7.1 built (gfortran serial, conda env `wrfbuild`) at `/home/enric/src/wrf_pristine` with a proven per-operator savepoint-dump + WRF-vs-JAX diff methodology (em_quarter_ss + em_grav2d_x done) — directly reusable as the validation oracle for EVERY physics scheme (= M9 per-operator parity). (b) A parallel multi-agent workflow that's been highly effective: Opus 4.8 frontrunner(s) + GPT-5.5 critic/bug-hunt + agy, all ground-truth-driven, honest gates (no self-compares). (c) The diagnostic harness (`comprehensive_harness.py`).
- **Performance**: the dycore grew many operators; the prior 22× (on the incomplete dycore) projects to ~10-15× post-rewrite; a dedicated perf sprint (XLA fusion + fp32 downcast) is needed (F7-perf), measured at M22.

## What remains (reset plan M8-M23, abridged)
M9 op-mode savepoint parity; M10 static-field/LU_INDEX parity; **Phase B**: M11 theta/mu guard accounting + physics-tendencies-into-RK1-bundle cadence, M17 Thompson microphysics, M12 surface flux + MYNN, M13 radiation + land-surface diurnal, M14 lateral boundary completeness; **Phase C**: M15 conservation/closure budgets, M18 idealized runner; **Phase D**: M16 prognostic Noah-MP (the big lift, ADR-030-conditional for v0.1.0); **Phase E**: M19 single-case L2/L3 skill recovery; **Phase F**: M20 validation corpus (≥27-30 cases, power analysis) + TOST design, M21 TOST equivalence; **Phase G**: M22 perf+transfer recert (≥10×), M23 v0.1.0 + arXiv.

## Your task — the fastest efficient path, concretely
Read `proofs/f7/DYCORE_STATUS.md`, `.agent/decisions/PROJECT-RESET-PLAN-FINAL.md`, `src/gpuwrf/diagnostics/comprehensive_harness.py`, and the current physics scheme code under `src/gpuwrf/physics/**` (Thompson/MYNN/RRTMG/Noah adapters). Then answer:
1. **Critical path + parallelization**: Given a now-correct dycore, the pristine-WRF savepoint methodology, and multiple concurrent Opus frontrunners — what is the optimal milestone SEQUENCE and what can run in PARALLEL? (The physics couplers — microphysics M17, surface/MYNN M12, radiation/land M13 — are column schemes; can they be ported+validated concurrently against per-scheme WRF savepoints?) What's the true critical path to M19 (single-case skill) → M21 (TOST)?
2. **Apply the savepoint methodology**: how to use the pristine-WRF + per-operator savepoint diff as the validation oracle for each physics scheme from the start (no blind debugging). What WRF savepoints to dump per scheme.
3. **Sequencing of perf (F7-perf) vs correctness**: when to do the XLA-fusion/fp32 perf sprint relative to Phase B and M22, to avoid both stale speed claims and re-optimizing churned code.
4. **What to cut/defer for v0.1.0**: is Noah-MP (M16, the big lift) on the critical path or deferrable (ADR-030)? What's the minimum to a defensible v0.1.0 (Canary skill equivalence)?
5. **Biggest risks + how to de-risk early**: the validation corpus (≥30 cases IC/BC on disk), the operational composition/boundary completeness, performance collapse after correctness, overfitting to one case.
6. **Concrete next 3-5 sprints** to dispatch the moment the dycore closes, with which can be parallel.

## Output
Write to EXACTLY `/home/enric/src/wrf_gpu2/.agent/sprints/2026-05-29-regroup-plan/gpt-regroup-plan.md`: a concrete revised roadmap (sequence + parallelization + per-scheme ground-truth + critical path + cut list + risks + the next 3-5 sprints). Be decisive and specific, with milestone-level estimates. End with `REGROUP_PLAN_COMPLETE`. Read-only on code; your only write is that plan file.
