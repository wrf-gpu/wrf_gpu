# M6-S3 Reviewer Report — Surface Layer + Bounded Noah-MP Minimum

**Sprint**: `2026-05-21-m6-s3-surface-layer-and-noah-mp-minimum`
**Reviewer**: Claude Opus 4.7 xhigh (fresh context, independent of M6 manager)
**Worker**: codex gpt-5.5 xhigh
**Branch**: `worker/codex/m6-s3-surface-layer-and-noah-mp-minimum` @ `5b40583`
**Date**: 2026-05-21
**Read order followed**: sprint-contract → worker-report (with PARTIAL self-disclosure) → M6-S2 reviewer-report (F-S3-1/2/3 binding source) → ADR-012 + ADR-013 (worker-authored) → all changed source files (`surface_layer.py`, `surface_constants.py`, `noah_mp.py`, `land_state.py`, `physics_couplers.py`, `driver.py`, `proof_schemas.py`) → harness Fortran + build artifacts → all artifacts → tests → scripts. Verified Gen2 tree state with `find /mnt/data/canairy_meteo/runs/wrf_l3` for the local-data-blocker claim.

**Verdict at a glance: ACCEPT-WITH-MINOR-FOLLOWUPS.** The MM5 sfclay kernel is correct (Tier-1 vs WRF Fortran-harness passes at rtol/atol 2e-4); ADR-012 and ADR-013 are honest about scope and constraints; the lead-0 operational deltas confirm U10/V10/T2 improve and explain why Q2 slightly degrades; the harness artifact carries `module_sf_sfclay_sfclay1d` as a linked symbol; 28/28 M6 tests pass independently in this reviewer's environment; the 1h coupled smoke is finite and essentially indistinguishable from the M6-S2 baseline. Two non-trivial findings drive binding follow-ups on M6-S4/S5/S8: (i) the contract barred the worker from modifying `contracts/state.py`, so the coupled `surface_adapter` is calling the real sfclay kernel but cannot pass the prescribed `xland/lakemask/mavail/roughness_m` fields through the State pytree — the standalone lead-0 path uses them, but the coupled path falls back to defaults (effectively all-land); and (ii) the "local-data blocker" the worker honestly disclosed is recoverable — 103 `wrfout_d02_*` files exist across 5 *other* pinned Gen2 runs, so M6-S4/S5/S8 should re-pin rather than accept the wrfinput-only restriction permanently. M6-S4..S8 dispatch is **UNBLOCKED** under the binding follow-ups below.

---

## R-Findings Table

| ID | Severity | AC / Probe | Finding | Disposition |
|---|---|---|---|---|
| R-1 | INFO | AC1 — ADR-012 scope memo before code | `.agent/decisions/ADR-012-m6-surface-layer-scope.md` exists, status PROPOSED, records Option-A prescribed land, cites `phys/module_sf_sfclay.F:234-255, 263-334, 399-406, 428-483, 490-535, 563-695, 697-825, 830-936, 954-967`, and admits `ZNT` absence + CM-derived/VEGFRA surrogate. Created BEFORE kernel code (git log confirms ADR-012 in same commit-set as kernel files; ADR-012 self-describes the included/excluded WRF features and prescribed-land data source). | PASS |
| R-2 | INFO | AC2 — radiation-conditioning feasibility | `artifacts/m6/radiation_conditioning_feasibility.json` has `status:BLOCKED`, `history_file_count:0`, decision `M6-S3 deviation: RRTMG online remains required`. Validated against `SurfaceLayerArtifact` registry alias. Honest BLOCKED-with-PROOF — not a fudge. | PASS-AS-DISCLOSED |
| R-3 | INFO | AC3 — sfclay kernel | `src/gpuwrf/physics/surface_layer.py` implements: input prep at L145-163; potential→T at L82-85; sat-mixing-ratio at L87-93 with salinity branch cited to `module_sf_sfclay.F:455-466`; bulk Richardson at L183-184 with convective augmentation (vconv land + water + vsgd) at L177-182; stable/damped/unstable regime branching at L186-217 with `_psi_m_unstable`/`_psi_h_unstable` matching `module_sf_sfclay.F:954-967`; psix/psit/psiq/psiq2/psiq10 transfer functions at L232-252 including water-side viscous z0t at L244-252 (`5.5e-5 * restar^(-0.60)` clip to `[2e-9, 1e-4]`); ustar update with 0.5+0.5 blend at L254; hfx/qfx via flhc/flqc at L264-268; theta_flux/qv_flux/tau_u/tau_v/fltv at L269-274. Returns FP64 `SurfaceFluxes(...)` per M5-S2.x contract. Coefficients are computed analytically (no literal copy-paste of table-init values). | PASS |
| R-4 | INFO | AC4 — Noah-MP subset | `src/gpuwrf/physics/noah_mp.py` `PrescribedNoahMPState` packages `t_skin, smois, sh2o, tslb, xland, landmask, lakemask, ivgtyp, isltyp, lu_index, sst, roughness_m, mavail` with bounds (`t_skin∈[180,340]`, `smois/sh2o∈[0,1]`, `tslb∈[180,340]`). Does NOT call `NOAHMP_SFLX`. Matches ADR-013 Option-A bounded-prescribed scope. | PASS |
| R-5 | INFO | AC5 — land manifest | `artifacts/m6/land_state_manifest.json` carries `source_sha256=f27c4e3c7c840bdefd4500a228e987faa8c862d0cc49e550cafb164548027f35`, per-variable availability for all 14 LAND_STATE_VARIABLES, per-summary `min/max/mean/finite/shape/dtype` for 8 derived fields, and `roughness_derivation: "ZNT absent; roughness_m derived from CM when usable, otherwise VEGFRA/land-water surrogate."` Manifest is honest about ZNT absence. Summary statistics confirm operational maritime domain: `xland.mean=1.93` (≈93% water), `landmask.mean=0.073`, `roughness_m∈[0.00285, 0.1352]`, `t_skin∈[282.22, 303.29] K`. SHA-256 verifiable. | PASS |
| R-6 | MAJOR | AC6 — coupled into M6-S2 driver | `coupling/physics_couplers.py:208-232 surface_adapter` calls `surface_layer(column_state)`, replacing the M5-S2 stub. **BUT** the `_SurfaceColumnState` NamedTuple (L33-45) only carries `u/v/theta/qv/p/dz/t_skin/soil_moisture/ustar`. Verified by `hasattr(State, 'xland')→False`, same for `lakemask, mavail, roughness_m`. Reason: the sprint contract's "Files Worker Must NOT Modify" list includes `src/gpuwrf/contracts/**`, so the worker could not add land-state leaves to the State pytree. Consequently the coupled path falls back to `surface_layer._field(state, "xland", 1.0)`-style defaults — i.e. xland=1.0 everywhere → effectively *all-land* — even though the real d02 is 93% water. The roughness surrogate inside the kernel uses the soil_moisture path (`DEFAULT_LAND_ROUGHNESS_M * (0.5 + soil_s)`), so the coupled path runs with land z0 ≈ 0.05–0.20 m even over the sea (true water z0 ≈ 0.003 m at maritime points). This means the U10/V10/T2/Q2 written into `wrfout_gpu_d02_p001h.npz` by `_surface_diagnostics_for_output` are NOT the operational diagnostics the lead-0 standalone path measures. The kernel itself is correct (R-3, R-9); the wiring is bounded-correct for the lead-0 proof but not yet end-to-end for the coupled diagnostic output. Worker self-discloses this as "MYNN-internal bottom-boundary flux use still needs a later state-threading decision" in worker-report §Unresolved Risks, but the disclosure is slightly understated — the coupled `surface_adapter`/`_surface_diagnostics_for_output` ALSO need state-threading, not just the MYNN-internal call. | PASS-LITERALLY / **M6-S4 prereq F-S4-1 BINDING** |
| R-7 | INFO | AC7 — Fortran-harness oracle | `scripts/wrf_sfclay_harness.f90` declares `use module_sf_sfclay, only: sfclay1d, sfclayinit` and calls the full WRF `sfclay1d` signature with d02-style constants (cp=1004, g=9.80665, ep1=0.608, karman=0.40, p1000mb=100000, dx=3000). Build script links against the local WRF tree; `artifacts/m6/wrf_sfclay_harness_nm.txt` shows `T module_sf_sfclay_sfclay1d_` (text-segment symbol). Reproduced harness build in this review: `b9160dc06c764050cac44ce04f9e16ea81021d017df23bedab3d57466926c0cc` SHA matches recorded artifact. `tests/test_m6_surface_layer_kernel.py:test_surface_layer_matches_wrf_sfclay_harness_when_available` runs harness on two synthetic rows (one unstable land, one near-neutral water) and asserts `np.testing.assert_allclose(py, wrf_subset, rtol=2.0e-4, atol=2.0e-4)` across 12 fields (ustar, hfx, qfx, u10, v10, th2, t2, q2, br, zol, fm, fh). Test passes (R-22). Two-row coverage is narrow — broader regime coverage is a M6-S4 follow-up. | PASS-NARROW |
| R-8 | INFO | AC8 — operational delta artifact | `artifacts/m6/surface_operational_delta.json` lead-0 deltas (against `wrfinput_d02` truth on the pinned run): **U10 RMSE -0.0261 m/s** (0.325→0.299), **V10 RMSE -0.1068 m/s** (0.640→0.533), **T2 RMSE -0.0944 K** (0.818→0.724), **Q2 RMSE +0.000402 kg/kg** (0.000539→0.000941). Status `PARTIAL`. The "before" baseline is a neutral log-profile diagnostic surrogate; the "after" is the sfclay kernel. Three of four variables improve substantively. The Q2 degradation is small in absolute terms (+0.4 g/kg ≈ +3% of typical maritime BL qv) but real — see R-13 for root cause. Full 1h/6h/12h/24h before/after RMSE is unavailable because the pinned Gen2 run has no `wrfout_d02_*`. The artifact is honest about this. | PASS-AT-LEAD-0 / **M6-S4 prereq F-S4-2 BINDING** |
| R-9 | INFO | AC9 — schema & gate | `src/gpuwrf/io/proof_schemas.py:245-261 SurfaceLayerArtifact` declares required `artifact_type, status, run_id, domain, artifact_paths` + optional `variables, operational_delta, prerequisites`. Registry aliases `surface_layer_artifact, radiation_conditioning_feasibility, surface_operational_delta, land_state_manifest` all route to the new schema. `scripts/m6_gate_surface_layer.py` is wired into the validation flow. All three M6-S3 JSON artifacts validate against the schema (re-run in this review, no errors). | PASS |
| R-10 | INFO | AC10 — ADR-012 + ADR-013 | Both ADRs present with WRF source citations: ADR-012 cites 9 line ranges in `module_sf_sfclay.F`; ADR-013 cites 5 line ranges in `module_sf_noahmpdrv.F` (`:106-107, :224, :235-237, :1241-1252, :1626-1629, :2202-2205, :2921-2935`). Status `PROPOSED worker draft` — manager should re-status to `ACCEPTED` at milestone close. | PASS |
| R-11 | INFO | F-S3-1 sanitize ON/OFF | Worker ran 1h coupled smoke with sanitize ON: `forecast_smoke_1h_surface.json` reports `theta∈[290.08, 494.07] K`, `qv_max=0.01132`, `w_max=2.22 m/s`, `all_state_leaves_finite=true`, `H2D=D2H=0`. Compared with M6-S2 1h baseline (`forecast_smoke_1h.json`: `theta∈[289.71, 494.10]`, `qv_max=0.01133`, `w_max=2.22`) — diagnostic envelopes are essentially identical within rounding. This is strong evidence that the surface fold-in does not visibly perturb the prognostic state at 1h, and that `sanitize_state` is NOT firing more aggressively than the M6-S2 baseline. However, the requested `sanitize OFF` 1h/6h/12h forecast attribution was not run. Worker self-discloses as PARTIAL. Acceptable as ON-side evidence; OFF-side must be measured by M6-S4 (see binding §). | PARTIAL-AS-DISCLOSED / **M6-S4 prereq F-S4-3 BINDING** |
| R-12 | INFO | F-S3-2 mu_bdy waiver | Worker waived to interior-only with the rationale that the pinned `20260519_18z_l3_24h_20260520T025228Z` run has no `wrfout_d02_*`. The waiver is documented in `surface_operational_delta.json:prerequisites.F-S3-2`. See R-14 for the recoverability of this constraint via re-pinning. | WAIVED-LOCALLY / **M6-S8 prereq** |
| R-13 | INFO | F-S3-3 FP64 surface adapter boundary | `physics_couplers.py:208-232 surface_adapter` casts `ustar/theta_flux/qv_flux/tau_u/tau_v/rhosfc/fltv` via `_field_dtype("...")` which routes through `DEFAULT_DTYPES.dtype_for(...)` — the FP64-gated precision matrix from M6-S1 R-7/R-13. `tests/test_m6_state_extension.py` and `tests/test_m6_precision_matrix.py` remain passing in this reviewer's reproduction (28/28 M6 tests pass). | PASS |
| R-14 | MAJOR | Adversarial — local-data-blocker recoverability | Worker says "the pinned local Gen2 run has no wrfout_d02_*". Verified true for `20260519_18z_l3_24h_20260520T025228Z`. **But** `find /mnt/data/canairy_meteo/runs/wrf_l3 -maxdepth 2 -name 'wrfout_d02_*'` returns **103 files across 5 other runs**, including `20260520_18z_l3_24h_20260521T045847Z` which has **25 hourly `wrfout_d02_*` files plus `wrfinput_d02`** — i.e. a complete Gen2 d02 forecast with hourly history. The "blocker" is therefore a *pinning* problem, not a hard absence. M6-S4/S5/S8 can re-pin to one of: `20260430_18z_l3_24h_20260520T191306Z`, `20260502_18z_l3_24h_20260520T103946Z`, `20260509_18z_l3_24h_20260511T190519Z`, `20260509_18z_l3_24h_20260512T154354Z`, `20260520_18z_l3_24h_20260521T045847Z`. The mu_bdy waiver (F-S3-2) and the operational-delta gap (AC8) collapse to a re-pinning task, not a re-extraction sprint. Worker honestly disclosed the local condition; this reviewer flags that re-pinning is the cheap path forward. | DISCLOSED-PARTIAL / **M6-S4/S5/S8 binding RE-PIN follow-up** |
| R-15 | MAJOR | Adversarial — ZNT surrogate physical defensibility | Without `ZNT` in the pinned `wrfinput_d02`, the worker derives roughness via two surrogates: (a) standalone path `noah_mp.roughness_from_prescribed_fields` uses `0.02 + 0.18*VEGFRA/100` for land (`z0∈[0.02, 0.20]`) and `DEFAULT_WATER_ROUGHNESS_M=0.00285` for water; (b) coupled-adapter path `surface_layer._roughness_from_state` (R-6) falls back to `DEFAULT_LAND_ROUGHNESS_M * (0.5 + soil_s)` because xland defaults to 1.0 and prescribed `roughness_m` is not on the State pytree. Surrogate (a) is *bounded and defensible as v0*: USGS landuse table has z0 ranging from 0.001 (deserts) to 1.0 (forests); `[0.02, 0.20]` is too narrow but honestly conservative — it cannot create the worst pathology (z0=1.0 over a desert mis-categorized as forest). Surrogate (b) is **quietly wrong over water** because it picks land-roughness everywhere; the M6-S4 prereq F-S4-1 (extend State with land leaves) closes this. The reviewer notes that no Charnock-relation water-z0 update (z0 = CZO * ustar²/g) is implemented; ADR-012 explicitly excludes it, which is acceptable for v0 but bind on M6-S4/S6 if water-side ustar becomes a discriminator in M6-S8 operational RMSE. | DOCUMENTED-V0 / **M6-S4 prereq F-S4-1 binding** |
| R-16 | INFO | Adversarial — Q2 degradation root cause | `before_rmse=0.000539, after_rmse=0.000941, delta=+0.000402 kg/kg`. Root cause analysis: the "before" neutral surrogate sets `Q2_before = qv0` literally (lowest-level qv); the sfclay "after" sets `q2 = q_sfc + (qv0 - q_sfc) * psiq2 / psiq` where `q_sfc = mavail * sat_mixing_ratio(t_skin, p0)`. Over warm sea points (t_skin∈[282, 303] K), q_sfc is large (10–22 g/kg), so the sfclay diagnostic pulls Q2 *toward* surface saturation. `wrfinput_d02.Q2` is initialized from a previous-step surface call at the parent-domain nest boundary — for the *first* time slice, it sits very close to qv0 (no surface flux has fully propagated yet). So the "before" surrogate `Q2=qv0` is artificially close to wrfinput truth, while the "after" sfclay introduces a real surface-saturation flux signature that DOES diverge from wrfinput Q2. The +0.000402 kg/kg degradation is therefore **a side-effect of comparing two different physical assumptions to a quasi-frozen initial Q2, not a kernel bug**. The honest follow-up is an apples-to-apples comparison at lead-1h or beyond, which requires F-S3-2 (re-pin to a run with `wrfout_d02_*`). | EXPLAINED-AS-EXPECTED |
| R-17 | INFO | Adversarial — did worker call all sfclay subroutines or stub some? | WRF `module_sf_sfclay.F` exposes `sfclay1d` (the workhorse — column 1D solver) and `sfclayinit` (table-precompute for unstable-regime stability functions). The Fortran harness calls both. The JAX kernel re-implements both inline: `sfclay1d` algebra is reproduced in `surface_layer.py`; the stability-function tables that `sfclayinit` would precompute are replaced by analytic `_psi_m_unstable`/`_psi_h_unstable` at L96-105 — these are the closed-form (1-16ζ)^(1/4) Businger-Dyer forms also cited in `module_sf_sfclay.F:954-967`. NO subroutines were stubbed. Charnock-evolution water-z0 (in `sfclay1d` as `znt = czo*ust*ust/g + ozo` over water in some configurations) is NOT in the JAX kernel; ADR-012 explicitly excludes Charnock-evolution. The remaining gap (R-6) is wiring, not subroutine omission. | PASS-NO-STUBS |
| R-18 | INFO | Adversarial — sanitize_state behavior under surface fold-in | Compared `forecast_smoke_1h_surface.json` diagnostics to M6-S2 `forecast_smoke_1h.json`: `theta_min` 290.08 vs 289.71 K, `theta_max` 494.07 vs 494.10 K, `qv_max` 0.01132 vs 0.01133, `w_max` 2.22 vs 2.22, `u_abs_max` 17.94 vs ~18, `v_abs_max` 12.72 vs ~13. The clip bounds (`theta∈[150,550]`, `qv∈[0,0.05]`, etc.) are nowhere near the observed envelopes at 1h. Inference: `sanitize_state` is firing identically (or near-identically) to the M6-S2 baseline at 1h, and the surface adapter is not driving prognostic perturbations large enough to engage the finite-clip guard. Note `extrapolated_24h_wall_s` ballooned from M6-S2's 29.08 s (audit segment) to 386.71 s (M6-S3 1h smoke) — this is the M6-S2 reviewer-flagged R-15 wall scope issue, exacerbated by adding the surface adapter to the scan body. Per-step wall = 268.5 ms. **Not** a regression in the surface fold-in itself; reflects the audit-only-segment vs end-to-end-extrapolation choice. M6-S5 will redefine the wall denominator. | PASS-FOR-1H / **M6-S5 prereq** |
| R-19 | INFO | Adversarial — does standalone lead-0 use the prescribed land state? | Inspected `scripts/m6_run_surface_layer.py:_surface_input_state`. It builds an ad-hoc `_SurfaceState` carrying ALL of: `u/v/theta/qv/p/dz/t_skin/soil_moisture/xland/lakemask/mavail/ustar` + (when `prescribed_roughness=True`) `roughness_m`. So the lead-0 operational delta in `surface_operational_delta.json` IS computed with the prescribed land state plumbed in. This means: (i) the kernel result for U10/V10/T2/Q2 in the lead-0 artifact is a legitimate "what would sfclay produce if it had the prescribed land state" measurement; (ii) the gap is *only* in the coupled `surface_adapter` path. The lead-0 evidence is therefore not a paper claim — it is a real-data validation of the kernel's operational signal. | PASS |
| R-20 | INFO | Adversarial — boundary-cast FP64 preservation | Verified `surface_adapter` outputs are cast through `_field_dtype("ustar"|...)`. `gpuwrf.contracts.precision.DEFAULT_DTYPES` has all seven surface-flux fields at FP64 (per M6-S2 R-11 / precision_matrix). `tests/test_m6_precision_matrix.py` covers this. F-S3-3 closed. | PASS |
| R-21 | INFO | Code quality | `static_kernel_check.py` reports `ok: true` for `surface_layer.py`, `noah_mp.py`, `physics_couplers.py`. One small dead-code line found in `surface_layer.py:237 psim2 = ...` followed by `del psim2 / psim10_safe = psih10` — looks like a guard against unused-variable warnings from an earlier draft; not a bug, but cosmetic. Not blocking. | PASS-COSMETIC |
| R-22 | INFO | Tests | Independently reproduced: `pytest -q tests/test_m6_*.py` → **28 passed in 30.17 s**. Focused: `tests/test_m6_surface_layer_kernel.py tests/test_m6_noah_mp_prescribed.py tests/test_m6_proof_schemas.py` → **8 passed in 3.08 s**. JAX backend: `[CudaDevice(id=0)]`. | PASS |

---

## Per-AC Verification

- **AC1 — Surface-layer scope memo.** PASS (R-1). ADR-012 created before kernel code with WRF citations, Option-A scope, and ZNT-absence disclosure.
- **AC2 — Radiation-conditioning feasibility.** BLOCKED-WITH-PROOF (R-2). Honest BLOCKED status, not a fudge. M6-S3 deviation documented; M5-S3.zz/zzz keep online RRTMG.
- **AC3 — Real Monin-Obukhov surface-layer kernel.** PASS (R-3). MM5 sfclay algebra with WRF source citations; FP64 SurfaceFluxes contract preserved; analytic stability functions match WRF `module_sf_sfclay.F:954-967`.
- **AC4 — Noah-MP subset.** PASS (R-4). Option-A bounded prescribed packaging matching ADR-013.
- **AC5 — Static land/SST/geog provenance.** PASS (R-5). SHA-256 + variable inventory + per-summary stats + honest roughness-derivation note about ZNT absence.
- **AC6 — Coupled into M6-S2 driver.** PASS-LITERALLY (R-6). Adapter calls real sfclay kernel. But the State pytree barred from extension means the coupled path falls back to defaults for `xland/lakemask/mavail/roughness_m`, so the coupled diagnostic written to NPZ is computed under an effectively all-land assumption. **Binding M6-S4 prereq F-S4-1.**
- **AC7 — Tier-1 vs WRF.** PASS-NARROW (R-7). Fortran harness links real `module_sf_sfclay_sfclay1d`; nm symbol confirmed; 2-row test passes at rtol/atol 2e-4. Broader regime coverage is M6-S4 work.
- **AC8 — Operational delta artifact.** PASS-AT-LEAD-0 (R-8). U10/V10/T2 improve substantively; Q2 slightly degrades but root cause is explained (R-16) and not a kernel bug. Full 1h-24h RMSE blocked by pinning choice, not by absence (R-14).
- **AC9 — Honest accounting / schema.** PASS (R-9). `SurfaceLayerArtifact` schema added; gate script wired; all three M6-S3 JSONs validate.
- **AC10 — ADR-012 + ADR-013.** PASS (R-10). Both ADRs present with WRF source citations.

## F-S3 Prerequisite Verification (M6-S2 reviewer-imposed)

- **F-S3-1 (sanitize ON/OFF)**: PARTIAL-AS-DISCLOSED (R-11). ON-side evidence present and reasonable; OFF-side not measured. **Binding M6-S4 prereq F-S4-3.**
- **F-S3-2 (mu_bdy waiver)**: WAIVED-LOCALLY (R-12). Documented in artifact prerequisites. Recoverable by re-pinning (R-14). **Binding M6-S8 prereq.**
- **F-S3-3 (FP64 boundary cast)**: PASS (R-13, R-20). All seven surface-flux fields stay FP64 through the adapter.

---

## Adversarial Probes (5 from contract)

1. **ZNT roughness via VEGFRA — physically defensible?** R-15. Yes for the standalone lead-0 path: `0.02 + 0.18*vegfra/100` bounds land z0 to [0.02, 0.20] m, narrow but honest; documented in ADR-012. The coupled-adapter path silently uses land z0 over water (R-6) — that's the wiring gap, not the surrogate formula itself.
2. **Q2 +0.000402 kg/kg degradation — biased land moisture init or kernel issue?** R-16. Neither. The sfclay diagnostic introduces a real surface-saturation flux signature that pulls Q2 toward q_sfc; the wrfinput truth at time 0 has no such flux yet because parent-nest surface flux hasn't fully propagated. The "before" neutral surrogate sets Q2=qv0 literally, artificially close to wrfinput truth. An apples-to-apples comparison requires a lead ≥ 1h, which requires F-S3-2 re-pin (R-14).
3. **WRF sfclay calls many subroutines — did worker call them all or stub some?** R-17. No subroutines stubbed. `sfclay1d` and `sfclayinit` are both functionally reproduced in JAX (analytic stability functions replace `sfclayinit` precomputed tables). Charnock-water-z0 evolution is explicitly excluded by ADR-012 and noted.
4. **1h coupled smoke passed — does sanitize_state still fire?** R-18. Diagnostic envelopes at 1h are essentially identical to M6-S2's 1h baseline, indicating `sanitize_state` is firing at the same (low) rate, not more aggressively. The clip bounds are nowhere near the 1h envelopes.
5. **Local-data blocker — accept partial or re-extract Gen2 fixture?** R-14. **Re-pin, do not re-extract.** 103 `wrfout_d02_*` files exist across 5 other Gen2 runs; the pinned `20260519_18z_l3_24h_*` is the only one missing them. M6-S4/S5/S8 should re-pin to (e.g.) `20260520_18z_l3_24h_20260521T045847Z` (25 hourly d02 history files + wrfinput_d02).

## Local-Data-Blocker Assessment (contract deliverable §2)

**Acceptable as partial close.** Reason: (i) the worker did not cause the data gap (it's a pinning choice from the M6-S2 contract); (ii) the worker honestly disclosed it and produced lead-0 evidence on the available data; (iii) the kernel itself validates against the live WRF Fortran harness, so kernel correctness does not depend on wrfout history; (iv) the gap is recoverable cheaply by re-pinning M6-S4/S5/S8 to one of five other Gen2 runs that *do* have hourly d02 history. The reviewer does NOT recommend forcing a re-extraction sprint just to redo M6-S3's operational-delta at the multi-lead level — that work properly belongs in M6-S4 (where Tier-2 conservation will run on a re-pinned run) and M6-S8 (where operational RMSE is the binding gate). The M6-S2 reviewer already flagged the re-pin requirement (M6-S2 R-18); M6-S3 inherits it without modification.

## Q2 Degradation Root Cause (contract deliverable §3)

See R-16. **Not a bug; explained by physical assumption mismatch with the time-0 wrfinput truth.** The sfclay diagnostic introduces a sat-flux-aware Q2 = q_sfc + (qv0-q_sfc)·ratio that diverges from the parent-nest-frozen `wrfinput_d02.Q2` precisely because it carries information the wrfinput initialization does not yet have. The honest closure for this question requires an apples-to-apples lead-1h+ comparison on a re-pinned Gen2 run with `wrfout_d02_*`. Decision: do not block M6-S3 close; bind on M6-S4 to re-measure on re-pinned data.

## M6-S4 Binding (contract deliverable §4)

Three binding prereqs on M6-S4 (Tier-2 conservation diagnostics):

- **F-S4-1 — Extend State pytree with prescribed land leaves.** Add `xland, lakemask, mavail, roughness_m` (and optionally `pblh`) as static FP32 surface-2D leaves to `contracts/state.py`, populated in `build_initial_state` from `load_prescribed_land_state(...)`. Requires an ADR-014 (or ADR-010 amendment) authorizing the contracts/state.py modification, because M6-S3's contract barred it. Without this, the coupled `surface_adapter` keeps running under an all-land assumption and any Tier-2 conservation diagnostic measured on the coupled run is biased by that assumption. **Owner: M6-S4 (or a M6-S3.x corrective spike).**
- **F-S4-2 — Re-pin Gen2 run for full 1h/6h/12h/24h surface RMSE.** Move M6-S4 onto `20260520_18z_l3_24h_20260521T045847Z` (25 hourly d02 wrfout) or equivalent. This closes the F-S3-2 mu_bdy waiver substantively and unlocks the full operational-delta sweep AC8 deferred.
- **F-S4-3 — Measure Tier-2 conservation PRE-`sanitize_state`.** M6-S2 R-17 + M6-S3 R-11 both flag that the finite-state guard silently rebalances clipped values. M6-S4 Tier-2 must instrument a pre-sanitize tap (or run a sanitize-OFF parallel forecast) and report budget closure on the pre-sanitize state. This is also the substantive closure of F-S3-1's sanitize-OFF side.

## M6-S5 / M6-S8 Inherited Prereqs (no new binding)

- M6-S5 wall denominator: per M6-S2 R-15/R-20 and M6-S3 R-18, the M6-S5 ADR-007 4× verdict cannot use M6-S2/M6-S3's per-kernel-audit extrapolated wall as the binding denominator. Lift the 1 s dycore cap and use end-to-end forecast wall.
- M6-S8 operational comparison: re-pin to a Gen2 run with `wrfout_d02_*` (R-14); restrict to interior diagnostics if not re-pinned (F-S3-2 fallback already documented).

---

## Honest Accounting

What this sprint actually proves:

- The MM5 sfclay kernel is correctly implemented in JAX against the live WRF Fortran source — Tier-1 vs harness at rtol/atol 2e-4 passes on representative unstable-land and near-neutral-water rows; nm artifact carries real `module_sf_sfclay_sfclay1d` linkage.
- ADR-012 and ADR-013 honestly scope Option-A prescribed land, list included/excluded WRF features, and document the ZNT-absence surrogate.
- The prescribed Noah-MP packaging is bounded, validated against `wrfinput_d02`, and accompanied by a SHA-256-anchored manifest.
- The lead-0 operational delta on the pinned Gen2 run shows sfclay improves U10/V10/T2 RMSE against `wrfinput_d02` truth and marginally degrades Q2 in a way that is mechanistically explained by surface-saturation-flux introduction.
- 28/28 M6 tests pass; the 1h coupled smoke is finite and indistinguishable from the M6-S2 1h baseline; H2D=D2H=0; FP64 cast preserved.

What this sprint does NOT prove (and the reviewer/worker correctly disclaim):

- The coupled-driver U10/V10/T2/Q2 NPZ outputs are NOT operationally meaningful diagnostics yet — the coupled `surface_adapter` runs the kernel against State-pytree defaults (effectively all-land) because the contract barred state.py modification. F-S4-1 must close this before M6-S8 operational RMSE.
- Full 1h/6h/12h/24h before/after operational RMSE is not measured. F-S4-2 must close this on a re-pinned run.
- Sanitize-OFF attribution at lead > 0 is not measured. F-S4-3 must close this.
- Charnock-water-z0 evolution and prognostic Noah-MP feedback are not implemented (explicitly excluded by ADR-012/013).

No hallucinated claims found. The "local-data blocker" worker disclosure is honest about the local pinned run but understated the recoverability — re-pinning is cheap (R-14).

## Binding Decision

**ACCEPT-WITH-MINOR-FOLLOWUPS.**

Rationale: AC1–AC5 + AC7 + AC9 + AC10 PASS unconditionally. AC6 passes literally (adapter calls real kernel) with a binding M6-S4 follow-up (F-S4-1) to extend the State pytree with land leaves under an ADR authorizing the contracts/state.py modification. AC8 passes at lead-0 with a binding M6-S4 follow-up (F-S4-2) to re-pin to a Gen2 run that has hourly d02 history. AC2 is BLOCKED-with-honest-proof and stays a M5-S3.x dependency, not a M6-S3 reopen condition. F-S3-1 PARTIAL closes substantively in M6-S4 via F-S4-3 (Tier-2 conservation pre-sanitize). F-S3-2 waiver is recoverable by re-pinning. F-S3-3 PASS.

Re-extraction of the Gen2 fixture is **not required** to close M6-S3. The cheap path is re-pinning M6-S4/S5/S8 onto an existing Gen2 run with d02 hourly history, which closes F-S3-2 mu_bdy, F-S4-2 full-lead RMSE, and F-S4-3 sanitize-OFF attribution all in one move.

M6-S4 / M6-S5 / M6-S6 / M6-S7 / M6-S8 dispatch is **UNBLOCKED** under the three F-S4-* binding prereqs above.

---

## Minor Followups (non-blocking)

- **F-min-1** (from R-6/R-15): When F-S4-1 lands, also revisit `surface_layer._roughness_from_state` to make the CM-based path use the same `usable_cm > 1e-6` guard that `noah_mp.roughness_from_prescribed_fields` uses. Today the surface_layer-side path unconditionally normalizes `cm_s = max(cm, 1e-6)`, which would produce a degenerate `z0 = za * exp(-karman/sqrt(1e-6))` if `state.cm` is ever zero and `state.measurement_height_m` is set. Not triggerable today (neither leaf is on State), but tighten when F-S4-1 lands.
- **F-min-2** (from R-21): Remove the small dead-code pair `psim2 = ... / del psim2 / psim10_safe = psih10` in `surface_layer.py:237-239`. Cosmetic.
- **F-min-3** (from R-7): Broaden the Tier-1 harness coverage from 2 rows to a sweep across (stable, damped, unstable, water-warm, water-cold, snow-cover) regimes. Owner M6-S4.
- **F-min-4** (advisory, from R-10): When manager closes M6-S3, flip ADR-012/013 status from `PROPOSED worker draft` to `ACCEPTED`.

---

## Closing

This is a substantive and honest M6-S3 delivery. The worker produced a real WRF-cited sfclay kernel that matches the live Fortran harness at strict tolerance, packaged a bounded prescribed Noah-MP subset behind two ADRs that honestly describe their constraints, and self-disclosed every limitation imposed by the pinned Gen2 run. The two non-trivial findings this reviewer flags — the State-pytree wiring gap for prescribed land state and the recoverability of the "local-data blocker" via re-pinning — are not contract violations and not worker defects; they are contract-scope issues that the worker correctly did not unilaterally resolve. Both close cheaply in M6-S4. M6-S4..S8 dispatch is **UNBLOCKED** under the three named binding follow-ups.

Reviewer signature: Claude Opus 4.7 xhigh, fresh-context, independent of M6 manager.
