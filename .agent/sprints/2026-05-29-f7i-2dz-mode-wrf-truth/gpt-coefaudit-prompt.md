You are GPT-5.5 xhigh on a focused, INDEPENDENT bug-hunt for the wrf_gpu2 JAX dycore (WRF v4 port, mass coordinate, RK3 + acoustic small-step). The big runaway is FIXED (the large-step PGF / pg_buoy_w and the stage-boundary grid%p now use the WRF calc_p_rho_phi physical perturbation pressure; warm-bubble max|w|@100s dropped 10× and the thermal now rises). ONE residual remains and it is specific: a **2Δz vertical acoustic mode at the bubble-center column** (w alternating ±9 m/s between adjacent vertical levels) that grows and detonates ~180 s in the implicit w/ph solve, before the 500 s gate. Find what in the implicit vertical solve admits/undamps this 2Δz mode.

READ + ANALYZE + RUN read-only probes in the detached worktree `/tmp/wrf_gpu2_coefaudit` (snapshot, `PYTHONPATH=src taskset -c 0-3 python ...`, cuda:0, fp64). Do NOT edit source. ONLY write the findings file named below (absolute path in main repo).

## What a growing 2Δz vertical mode means
In WRF's compressible nonhydrostatic core, vertically-propagating acoustic modes (incl. the 2Δz grid mode) are stabilized by **time off-centering `epssm`** in the IMPLICIT vertical w/geopotential solve — the new-time-level terms are weighted (1+epssm)/2 and old-level (1−epssm)/2. If JAX's implicit solve is effectively centered (epssm not applied, applied to the wrong term, wrong sign, or the tridiagonal coefficients are off), the 2Δz mode is undamped and grows. This is the prime suspect.

## Charter — pinpoint it against WRF source
WRF ground truth `/home/enric/src/canairy_meteo/Gen2/artifacts/wrf_gpu_src/WRF/dyn_em/module_small_step_em.F`: `calc_coef_w` (608-649, the tridiagonal coef assembly with c2a/cqw/rdn/rdnw/dts/g/epssm), `advance_w` implicit w + geopotential (1178-1271, 1315-1395, 1435-1502, 1581-1586, the Thomas forward/back and the off-centering).
1. Does JAX's `calc_coef_w` (a/alpha/gamma) and `advance_w` apply `epssm` off-centering in the implicit w/ph terms EXACTLY as WRF? Compare term by term: the `(1+epssm)/2`/`(1−epssm)/2` weighting on w and on the geopotential/pressure coupling. JAX files: `src/gpuwrf/dynamics/acoustic_wrf.py` (calc_coef_w_wrf_coefficients), `src/gpuwrf/dynamics/core/advance_w.py`, `src/gpuwrf/dynamics/tridiag_solve.py`, `src/gpuwrf/dynamics/core/acoustic.py`.
2. Is `epssm` even threaded into the implicit solve, and at what value (WRF Registry default 0.1; operational d02 uses 0.5)? Is it applied to BOTH the w update and the ph/pressure-gradient coupling (WRF applies it to both)?
3. Are the tridiagonal coefficients (the c2a·alt·g²·dts²·rdn·rdnw vertical-coupling terms) sign- and metric-correct (signed dnw/rdnw)? A sign error in the off-diagonal admits the 2Δz mode.
4. RUN a probe: integrate the warm bubble, trace w(k) at the center column over ~150 s; confirm the 2Δz alternating-sign growth; then test (read-only, e.g. monkeypatch in a probe script) whether correctly applying/raising epssm in the implicit solve damps it — if so, that localizes the bug to the off-centering.

## Output
Write to EXACTLY `/home/enric/src/wrf_gpu2/.agent/sprints/2026-05-29-f7i-2dz-mode-wrf-truth/gpt-coefaudit-findings.md`: (1) probe evidence of the 2Δz mode; (2) the single most likely cause with JAX file:line vs WRF file:line; (3) concrete fix; (4) falsifiable check (e.g. "with the fix, w(k) center column shows no 2Δz alternating growth; warm bubble finite to 500s"). End with `F7I_AUDIT_COMPLETE`. Do not modify source.
