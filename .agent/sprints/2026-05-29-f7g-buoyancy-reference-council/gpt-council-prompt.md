You are GPT-5.5 xhigh, WRF-source authority for the wrf_gpu2 project (JAX GPU port of WRF v4 dycore). After 6 sprints the dry dynamical core is WRF-faithful: acoustic small-step core, WRF damping, flux-form advection (5th order verified), MUT/MUTS mass semantics, large-step PGF + rk_addtend_dry (flow circulates), and (just fixed in F7F) `calc_p_rho_phi` now includes the `rdnw·(ph'(k+1)−ph'(k))` geopotential term + full-θ EOS. The idealized cases (Skamarock warm bubble, Straka density current) STILL fail, and the residual is precisely characterized as a discrete-hydrostatic-balance consistency problem. I need you to resolve the exact WRF mechanism so the next sprint fixes it correctly rather than guessing a 7th time.

This is a VERIFY/DESIGN task. Do NOT edit source. Your ONLY write is the findings file named at the end.

## The precisely-characterized open item (from `proofs/f7f/ic_balance_proof.md`)
- The idealized IC is hydrostatically balanced using WRF's `module_initialize_ideal.F` per-column recurrence (`:1124-1129`, `:1308-1313`): `ph'(k+1) = ph'(k) − dnw(k)·[(c1h·mub+c2h + c1h·mu')·al(k) + c1h·mu'·alb(k)]`, with mu'=0, ph'(1)=0, `al = alt_full − alb`, `alt_full = EOS(θ_full, p+pb)`.
- The dycore diagnoses perturbation pressure with `calc_p_rho_phi` (`module_big_step_utilities_em.F:1029,:1083-1087`): `al(k) = −1/(c1·muts+c2)·( alb(k)·c1·mu' + rdnw(k)·(ph'(k+1)−ph'(k)) )`; `p(k) = p0·(Rd·(t0+θ')/(p0·(al+alb)))^cpovcv − pb(k)`.
- Symptom: for the balanced IC (mu'=0, ph'≠0), `calc_p_rho_phi` yields a `grid%p` such that `pg_buoy_w(grid%p)/analytic ≈ 19×` (should be ~1× for the physical buoyancy, or ~0 net at rest). So the IC's ph'-recurrence is NOT the discrete inverse of `calc_p_rho_phi` → residual discretization imbalance → over-forcing → NaN (~190 s warm, ~30 s Straka).
- Additional: WRF's small-step `advance_w` buoyancy term is `c2a·alt·t_2ave − c1f·muave`; for mu'=0, `muave=0`, so nothing balances the perturbation column weight. The large-step `pg_buoy_w(grid%p)` (once per RK stage) and the small-step `advance_w` term must share a hydrostatic reference to cancel at rest.

## Questions (answer from WRF source, ground truth at /home/enric/src/canairy_meteo/Gen2/artifacts/wrf_gpu_src/WRF/)
1. **How does WRF guarantee the idealized IC is in DISCRETE hydrostatic balance with the dycore's own pressure/geopotential operator?** Does WRF's idealized init use the SAME discrete geopotential/pressure relation as `calc_p_rho_phi`/`rk_step_prep` (so the initial `grid%p` is the genuine tiny residual, not a 19× artifact)? Or does WRF rely on the model relaxing the small initial imbalance? Quote the init recurrence vs the `calc_p_rho_phi` discrete form and state whether they are mutually consistent by construction. Is the JAX bug that the init recurrence (`(c1h·mub+c2h)·al` with `dnw`) is NOT the algebraic inverse of `calc_p_rho_phi`'s `rdnw·(ph'(k+1)−ph'(k))/(c1·muts+c2)` (note `rdnw=1/dnw`, sign, and the `muts` vs `(c1h·mub+c2h)` denominators)?
2. **How do the large-step `pg_buoy_w(grid%p)` and small-step `advance_w` buoyancy (`c2a·alt·t_2ave − c1f·muave`) stay consistent?** What is `t_2ave` and `muave` at rest and for a mu'=0 perturbation? Where does the perturbation column-weight balance come from in `advance_w` when mu'=0 (is it the `c2a·alt·t_2ave` term carrying the θ' signal, with `muave` only the small-step mass increment)? Does WRF's `advance_w` actually need a nonzero `muave` to balance, or does the geopotential/`ph` evolution close it?
3. **Spec the discrete-consistent fix** for the next Opus sprint: precisely what must change so (a) the balanced IC gives `pg_buoy_w(grid%p) ≈ analytic` (not 19×), and (b) the large/small-step buoyancy references cancel at rest. Options to evaluate: balance the IC by iterating the dycore's OWN `calc_p_rho_phi` to a discrete rest state; or correct the init recurrence to be the exact discrete inverse of `calc_p_rho_phi`; or reconcile `t_2ave`/`muave`/`c2a` references. Recommend the WRF-correct one with file:line.

Read the JAX side too: `src/gpuwrf/dynamics/acoustic_wrf.py` (`diagnose_pressure_al_alt`/`calc_p_rho_phi`, `pg_buoy_w` builders), `src/gpuwrf/dynamics/core/advance_w.py`, `src/gpuwrf/ic_generators/idealized.py`, and `proofs/f7f/ic_balance_proof.md`.

## Output
Write to EXACTLY: `/home/enric/src/wrf_gpu2/.agent/sprints/2026-05-29-f7g-buoyancy-reference-council/gpt-council-findings.md`
Structure: (1) WRF answers to Q1-Q3 with file:line; (2) the decisive root cause (discrete-inverse mismatch? reference mismatch? both?); (3) exact fix spec for the next sprint; (4) a falsifiable check (e.g. "balanced IC → pg_buoy_w(grid%p)/analytic ∈ [0.9,1.1]"). End with `F7G_GPT_COMPLETE`. Do not modify any other file.
