You are Gemini (agy), brought in as a FRESH independent architect on a hard problem in the wrf_gpu2 project: a JAX-native, GPU-resident reimplementation of the WRF v4 compressible nonhydrostatic dynamical core (mass/hydrostatic-pressure vertical coordinate, C-grid, Runge-Kutta 3 + acoustic small-step split-explicit time integration). You have NOT been involved in this debugging loop, which is exactly why you are here: two models (Claude Opus and GPT-5.5) have iterated 6 times, each fixing a real bug, but the idealized validation cases still fail. I want your independent architectural judgment on whether the approach has a systematic flaw, not another incremental patch.

CONTEXT — what is already correct and verified (do not re-litigate these):
- Acoustic small-step core in WRF cadence: small_step_prep -> calc_p_rho(step=0) -> calc_coef_w -> {advance_uv -> advance_mu_t -> advance_w -> calc_p_rho(step=iter)} -> small_step_finish.
- WRF damping (w_damping, Rayleigh, divergence smdiv/emdiv), flux-form WS5/3 advection (verified 5th order), MUT/MUTS total-mass semantics (mut = MUB+MU_current, muts = mut+mu_work), large-step PGF + rk_addtend_dry so the flow circulates.
- Rest-state oracle: a hydrostatically balanced base column is EXACTLY stable (machine-zero tendencies). Mass conserved to machine zero over 300+ steps. So the integrator and the base-state balance are sound.
- Just fixed: the perturbation-pressure diagnostic calc_p_rho_phi had dropped the rdnw*(ph'(k+1)-ph'(k)) geopotential term; restored, so a warm bubble now produces a physical grid%p (~1.5e3 Pa) instead of 0.

THE PERSISTENT FAILURE (warm bubble NaN ~190s, Straka ~30s), precisely characterized:
The idealized initial condition (theta' bubble, dry, mu'=0) is hydrostatically balanced using WRF's module_initialize_ideal.F per-column recurrence:
    ph'(k+1) = ph'(k) - dnw(k)*(c1h*mub+c2h)*al(k),   al = EOS(theta_full,p+pb) - EOS(theta0,p+pb)
But the dycore diagnoses perturbation pressure with a DIFFERENT discrete operator, calc_p_rho_phi:
    al(k) = -[ alb*c1*mu' + rdnw(k)*(ph'(k+1)-ph'(k)) ] / (c1*muts+c2);   p = p0*(Rd*(t0+theta')/(p0*(al+alb)))^cpovcv - pb
Symptom: for the balanced IC, pg_buoy_w(grid%p) / analytic_buoyancy(g*theta'/theta0) is about 19x, not ~1x. So the IC's balancing recurrence is not the discrete inverse of the dycore's own pressure operator -> residual discretization imbalance -> the perturbation column is NOT at discrete rest -> over-forcing -> blow-up. Separately, WRF's small-step advance_w buoyancy term is c2a*alt*t_2ave - c1f*muave, and for mu'=0 muave=0, so the question is what carries the perturbation column-weight balance in the vertical implicit solve.

THE QUESTIONS I want your fresh take on:
1. Is the right fix to balance the IC by ITERATING the dycore's OWN discrete operators (calc_p_rho_phi / the advance_w vertical operator) to a discrete rest state, rather than using a separately-derived analytic/midpoint-z hydrostatic recurrence? I.e., make the IC discretely consistent-by-construction with the operator that will integrate it.
2. Is there a systematic architectural flaw in the split-explicit buoyancy assembly — e.g., the large-step pg_buoy_w(grid%p) being frozen once per RK stage while the small-step advance_w recomputes its own buoyancy from t_2ave/muave/c2a, with the two using inconsistent hydrostatic references — that would over-force ANY perturbation regardless of IC balancing? If so, what is the structurally correct way to reference them?
3. A 19x factor is suspiciously specific. From your knowledge of mass-coordinate nonhydrostatic dynamics (WRF/MPAS/ARW), what discretization mismatch produces an ~order-of-magnitude (not 2x, not 100x) buoyancy error of this kind? (e.g. full-level vs half-level geopotential differencing, rdnw vs dnw confusion, c1*muts vs c1*mub+c2 denominator mismatch, EOS linearization point.)
4. If you were to break this 6-iteration cycle in ONE decisive move, what would it be — the discrete-consistent IC balance, the buoyancy-reference reconciliation, or something structural the others have missed?

You may read these files if you have file access: /home/enric/src/wrf_gpu2/proofs/f7f/ic_balance_proof.md, /home/enric/src/wrf_gpu2/src/gpuwrf/dynamics/acoustic_wrf.py, /home/enric/src/wrf_gpu2/src/gpuwrf/dynamics/core/advance_w.py, /home/enric/src/wrf_gpu2/src/gpuwrf/ic_generators/idealized.py. WRF Fortran reference is under /home/enric/src/canairy_meteo/Gen2/artifacts/wrf_gpu_src/WRF/dyn_em/.

Give a decisive, opinionated architectural verdict and a concrete recommended fix. Be specific about WRF mass-coordinate numerics. End your response with the line F7G_AGY_COMPLETE.
