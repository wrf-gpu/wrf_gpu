# c1-A3 Manager Closeout — All 4 Bughunt3 Fixes Failed; 3-Way Parallel Push

**Sprint**: c1-A3 Long-Time Integration Fix
**Status**: **CLOSED A3 — All 4 bughunt3 fixes applied + tested; ZERO measurable improvement; some slightly worse → 3-way parallel push dispatched**
**Date**: 2026-05-22 ~03:50

## Headline

c1-A3 worker honestly applied bughunt3's complete 5-step fix sequence (sanitize-disable diagnostic + FIX #A periodic→edge-mirror + FIX #B dz floor + FIX #C1 smdiv damper + FIX #C2 ph horizontal advection). All passed unit tests. None made the 1h coupled probe pass.

| Probe | nonfinite | fired_steps | firing_rate |
|---|---|---|---|
| c1-A2 baseline | 793M | 319/360 | 88.6% |
| sanitize-DISABLED | first NaN at step 45 | 316 | (no clip) |
| FIX #A (periodic→edge-mirror) | 791M | 319 | 88.6% |
| FIX #AB (+dz floor) | 791M | 319 | 88.6% |
| FIX #ABC1 (+smdiv damper) | 787M | 322 | 89.4% |
| FIX #ABCfull (+ph advection) | **821M (WORSE)** | 326 | **90.6%** |

## The hard truth

**3 opus bug-hunts + 5 codex iterations have NOT found the dominant accumulator.**
- bug-hunt #1: PH g + asymmetric damping → FIX #2 made it WORSE
- bug-hunt #2: buoyancy + diagnostic-p + ρ factor → c1 implemented all; still failed
- bug-hunt #3: periodic wrap + dz floor + smdiv + ph advection → all 4 applied; zero improvement

Each bug-hunt was confident; each was at least partially wrong about the dominant accumulator.

**bughunt3 §4 explicitly noted with 70% confidence: "buoyancy may still be missing in `_vertical_implicit_w`"** — bughunt #2's Hypothesis A may not have been actually addressed by c1-A1's tridiagonal solve.

## c1-A3 worker also flagged (new info)

1. `derivative5_upwind` STILL uses `jnp.roll` in velocity advection — NOT in the 7 FIX #A sites
2. H2D transfer rose from 167KB → 7.3MB after FIX #C1+C2 (regression)
3. PH overflow warning during float32 output (data integrity concern)

## 3-way parallel max push dispatched

Per user feedback `[[feedback_parallel_bug_angles_and_plan_critique]]` (after 2 iterations stuck), but at 5 iterations now and Gemini OAuth still expired:

| Window | Sprint | Worker | Wall |
|---|---|---|---|
| 0:14 | **c1-A4 add buoyancy to `_vertical_implicit_w`** (bughunt3 §4 unresolved) | codex xhigh | 2-4h |
| 0:15 | **Empirical bisection** (systematically disable components to isolate step-45 nonfinite source) | codex xhigh | 2-4h |
| 0:16 | **Bug-hunt #4 meta-analysis** (what 3 bug-hunts DIDN'T audit: setup, init, contracts, n_acoustic=86) | Opus xhigh | 30-90min |

## Strategic position

This is the **maximum parallel angle attack** authorized by user directives:
- 2/3 codex slots used (1 spare)
- 1/3 opus slots used (2 spare)
- Different angles: theoretical bug-fix (c1-A4), empirical isolation (bisection), meta-analysis (bughunt4)

If ALL 3 fail → **mandatory user escalation** to:
- Re-frame end-goal (Canary 3km via different dycore architecture; e.g., FV3, GFDL-FMS, or ML-emulator c3.C)
- c2 semi-implicit (10-20 days, bug-hunt-prone like c1)
- Buy a working dycore (E3SM/HOMMEXX port? SCREAM port?)

## Branch state

- `worker/codex/m6x-c1-klemp-skamarock` at `eaeaec9` (c1-A3 fixes committed by worker; NOT merged to main)
- worker-report copied to main for tracking

## Honest accounting

The c1 path produced:
- An acoustic core that works in isolation (verified 60-step probe stable)
- 4 verified operator fixes (mass-conservation, dz floor, smdiv, ph advection)
- 32 passing unit tests
- 5 detailed worker reports + 3 bug-hunt reports + 5 ADRs
- **But NO 1h coupled probe pass after 5 iterations**

The pattern strongly suggests c1 has a fundamental architectural mismatch with how the d02 boundary forcing was generated by Gen2 CPU WRF. Either:
- The bug is in setup/coupling (bughunt #4 territory)
- Or the architecture is wrong (user-decision territory)

## Decision logic

- If bughunt #4 finds NEW hypothesis → c1-A5 dispatch
- If empirical bisection isolates a single component → c1-A5 dispatch with surgical fix
- If c1-A4 (buoyancy add) makes 1h probe pass → M6.x closes
- If ALL 3 fail → user escalation required (multiple architectural options to consider)

— Manager (Claude Opus 4.7 1M-context), 2026-05-22 03:50
